---
phase: 28.1-api-key-format-fix
verified: 2026-04-06T00:00:00Z
status: passed
score: 4/4 must-haves verified
re_verification: false
---

# Phase 28.1: API Key Format Fix Verification Report

**Phase Goal:** Fix raw API key format to follow PREFIX_UUID specification (AKEY-01). generateSecureKey() must return keyPrefix + "_" + UUID. Fix ApiKeyBuilder test builder and ApiKeyAuthenticationFilter debug log.
**Verified:** 2026-04-06
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Raw API key returned at create/rotate/reactivate follows PREFIX_UUID format | VERIFIED | `ApiKeyService.generateSecureKey(String keyPrefix)` returns `keyPrefix + "_" + UUID.randomUUID().toString()` (line 88-90). `reactivate()` in TenantService calls `generateAndStore()` which calls this method, covering all three paths. |
| 2 | ApiKeyBuilder test builder derives keyPrefix from tenant table, not from rawKey substring | VERIFIED | `ApiKeyBuilder.create()` queries `SELECT key_prefix FROM main.tenant WHERE id = ?` (line 63-65) then builds `keyPrefix + "_" + UUID.randomUUID().toString()` (line 66). Old `rawKey.substring(0, 8)` is absent. |
| 3 | ApiKeyAuthenticationFilter auth-failure log extracts prefix from underscore-delimited format, not first 8 chars | VERIFIED | Line 115: `rawKey.contains("_") ? rawKey.substring(0, rawKey.indexOf("_")) : "[unknown]"`. Old `rawKey.substring(0, 8)` is absent. |
| 4 | All existing integration tests pass with the new key format | VERIFIED | SUMMARY reports 6/6 TenantProvisioningIT tests pass. TenantProvisioningIT.java contains AKEY-01 format assertions at lines 101-102 including regex match and `startsWith("ACM_")`. Commits dc751ec and 0d42461 both exist in git log. |

**Score:** 4/4 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/main/java/com/softropic/payam/tenant/service/ApiKeyService.java` | PREFIX_UUID key generation | VERIFIED | Contains `private String generateSecureKey(String keyPrefix)` at line 88. Contains `return keyPrefix + "_" + UUID.randomUUID().toString()` at line 89. `String prefix = tenant.getKeyPrefix()` declared before `generateSecureKey(prefix)` call at lines 40-41. Imports `java.util.UUID`; no `SecureRandom` or `Base64` imports. |
| `src/test/java/com/softropic/payam/e2e/builder/ApiKeyBuilder.java` | Test builder with tenant-derived prefix | VERIFIED | Contains `SELECT key_prefix FROM main.tenant WHERE id = ?` at line 64. Contains `keyPrefix + "_" + UUID.randomUUID().toString()` at line 66. No `rawKey.substring(0, 8)`. |
| `src/main/java/com/softropic/payam/tenant/config/ApiKeyAuthenticationFilter.java` | Safe prefix extraction from underscore-delimited key | VERIFIED | Contains `rawKey.indexOf` at line 115 within `rawKey.contains("_") ? rawKey.substring(0, rawKey.indexOf("_")) : "[unknown]"`. Javadoc at lines 54-56 updated to describe underscore-delimited extraction. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `ApiKeyService.generateSecureKey(String)` | `ApiKeyService.generateAndStore(Tenant, ApiKeyEnvironment)` | `generateSecureKey(prefix)` called with `tenant.getKeyPrefix()` | WIRED | Line 40: `String prefix = tenant.getKeyPrefix();` Line 41: `String rawKey = generateSecureKey(prefix);` — prefix declared first, passed as argument. |
| `ApiKeyBuilder.create(JdbcTemplate)` | `main.tenant` table | JDBC `SELECT key_prefix FROM main.tenant WHERE id = ?` | WIRED | Lines 63-65: `jdbc.queryForObject("SELECT key_prefix FROM main.tenant WHERE id = ?", String.class, tenantId)` — result assigned to `keyPrefix`, used in rawKey construction and INSERT. |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `ApiKeyService.java` | `rawKey` | `generateSecureKey(prefix)` where `prefix = tenant.getKeyPrefix()` | Yes — `tenant.getKeyPrefix()` reads the DB-backed entity field | FLOWING |
| `ApiKeyBuilder.java` | `keyPrefix` | `jdbc.queryForObject("SELECT key_prefix FROM main.tenant WHERE id = ?", ...)` | Yes — live JDBC query against test DB | FLOWING |
| `ApiKeyAuthenticationFilter.java` | `keyPrefix` (log only) | `rawKey.substring(0, rawKey.indexOf("_"))` | Yes — parsed from real incoming key | FLOWING |

---

### Behavioral Spot-Checks

Step 7b: SKIPPED for production files (requires running integration test environment). SUMMARY documents test run result: 6/6 TenantProvisioningIT tests pass with BUILD SUCCESS. Commits dc751ec and 0d42461 verified to exist in git log.

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| AKEY-01 | 28.1-01-PLAN.md | API keys follow format `PREFIX_UUID` where prefix is derived from first 3 chars of tenant name at creation time, immutable | SATISFIED | `generateSecureKey(String keyPrefix)` returns `keyPrefix + "_" + UUID.randomUUID().toString()`. `TenantProvisioningIT` asserts `rawKey.matches("^[A-Z0-9]{2,3}_[0-9a-f]{8}-...")` and `rawKey.startsWith("ACM_")`. REQUIREMENTS.md marks AKEY-01 `[x]` with `(gap closure: Phase 28.1)`. Requirements table at line 93 shows `| AKEY-01 | Phase 28.1 | Complete |`. |

**Orphaned requirements check:** REQUIREMENTS.md maps only AKEY-01 to Phase 28.1. No additional requirement IDs are mapped to this phase that are unaccounted for in the plan.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

Scanned all four modified files for: TODO/FIXME, placeholder comments, `return null/[]/{}`, `rawKey.substring(0, 8)`, `SecureRandom`, `Base64`. No issues found.

---

### Human Verification Required

None. All observable truths can be verified programmatically for this phase (code structure, wiring, and test assertions are sufficient).

---

### Gaps Summary

No gaps. All four must-have truths are satisfied:

1. `generateSecureKey(String keyPrefix)` produces `PREFIX_UUID` format — the single change closes AKEY-01 at the generation point used by create, rotate, and reactivate paths.
2. `ApiKeyBuilder` now queries the tenant table for `key_prefix`, eliminating the false substring-based prefix.
3. `ApiKeyAuthenticationFilter` uses `indexOf("_")` to extract the prefix safely for any prefix length.
4. `TenantProvisioningIT` has an AKEY-01 format assertion that guards against regression.

---

_Verified: 2026-04-06_
_Verifier: Claude (gsd-verifier)_

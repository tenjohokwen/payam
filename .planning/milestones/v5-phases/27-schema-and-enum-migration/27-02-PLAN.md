---
phase: 27-schema-and-enum-migration
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - src/test/java/com/softropic/payam/e2e/builder/TenantBuilder.java
  - src/test/java/com/softropic/payam/e2e/builder/ApiKeyBuilder.java
  - src/test/java/com/softropic/payam/tenant/TenantProvisioningIT.java
  - src/test/java/com/softropic/payam/tenant/TenantFilterChainIT.java
  - src/test/java/com/softropic/payam/tenant/TenantAdminResourceIT.java
  - src/test/java/com/softropic/payam/orange/OrangeMoneyPortIT.java
  - src/test/java/com/softropic/payam/mtn/MtnMoMoPortIT.java
  - src/test/java/com/softropic/payam/payment/PaymentOrchestratorIT.java
  - src/test/java/com/softropic/payam/fraud/FraudEngineIT.java
  - src/test/java/com/softropic/payam/fee/FeeEngineIT.java
  - src/test/java/com/softropic/payam/transaction/TransactionStateMachineIT.java
  - src/test/java/com/softropic/payam/transaction/PaymentEventLogIT.java
  - src/test/java/com/softropic/payam/transaction/LedgerServiceIT.java
  - src/test/java/com/softropic/payam/transaction/IdempotencyServiceIT.java
  - src/test/java/com/softropic/payam/webhook/WebhookDeliveryIT.java
  - src/test/java/com/softropic/payam/webhook/WebhookDoubleCheckIT.java
autonomous: true
requirements: [AKEY-01, AKEY-03]

must_haves:
  truths:
    - "All tests compile after LIVE-to-PROD migration"
    - "Full test suite passes with Flyway V18+V19 running on Testcontainers PostgreSQL"
    - "No occurrence of string LIVE remains in any Java source file"
  artifacts:
    - path: "src/test/java/com/softropic/payam/e2e/builder/TenantBuilder.java"
      provides: "Updated default environment from LIVE to PROD with ApiKeyEnvironment type"
      contains: "ApiKeyEnvironment"
    - path: "src/test/java/com/softropic/payam/e2e/builder/ApiKeyBuilder.java"
      provides: "Updated default environment from LIVE to PROD"
      contains: "PROD"
  key_links:
    - from: "src/test/java/com/softropic/payam/e2e/builder/TenantBuilder.java"
      to: "src/main/java/com/softropic/payam/tenant/service/TenantService.java"
      via: "createTenant() call with ApiKeyEnvironment parameter"
      pattern: "createTenant.*ApiKeyEnvironment"
---

<objective>
Migrate all test call sites from "LIVE" to "PROD" (or ApiKeyEnvironment.PROD) and update test builders so the full test suite passes against the new schema.

Purpose: Prevent IllegalArgumentException from Hibernate when existing tests pass "LIVE" to a field now typed as ApiKeyEnvironment enum. This is the companion to Plan 01 — schema is migrated, now all consumers must match.
Output: All test files updated, full test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-schema-and-enum-migration/27-RESEARCH.md
@.planning/phases/27-schema-and-enum-migration/27-01-SUMMARY.md

@src/test/java/com/softropic/payam/e2e/builder/TenantBuilder.java
@src/test/java/com/softropic/payam/e2e/builder/ApiKeyBuilder.java
@src/test/java/com/softropic/payam/tenant/TenantProvisioningIT.java

<interfaces>
<!-- After Plan 01, these are the updated signatures the executor needs -->

From src/main/java/com/softropic/payam/tenant/contract/ApiKeyEnvironment.java (new in Plan 01):
```java
public enum ApiKeyEnvironment { PROD, DEV, SANDBOX }
```

From src/main/java/com/softropic/payam/tenant/service/TenantService.java (updated in Plan 01):
```java
public TenantCreationResult createTenant(String name, ApiKeyEnvironment environment)
// was: createTenant(String name, String environment)
```

From src/main/java/com/softropic/payam/tenant/service/ApiKeyService.java (updated in Plan 01):
```java
public ApiKeyAndRawKey generateAndStore(Tenant tenant, ApiKeyEnvironment environment)
// was: generateAndStore(Tenant tenant, String environment)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update test builders (TenantBuilder + ApiKeyBuilder)</name>
  <files>
    src/test/java/com/softropic/payam/e2e/builder/TenantBuilder.java,
    src/test/java/com/softropic/payam/e2e/builder/ApiKeyBuilder.java
  </files>
  <read_first>
    src/test/java/com/softropic/payam/e2e/builder/TenantBuilder.java,
    src/test/java/com/softropic/payam/e2e/builder/ApiKeyBuilder.java,
    src/main/java/com/softropic/payam/tenant/service/TenantService.java
  </read_first>
  <action>
**TenantBuilder.java** — 3 changes:
1. Add import: `import com.softropic.payam.tenant.contract.ApiKeyEnvironment;`
2. Change the `environment` field type and default:
   - Before: `private String environment = "LIVE";`
   - After: `private ApiKeyEnvironment environment = ApiKeyEnvironment.PROD;`
3. Change `withEnvironment` method signature:
   - Before: `public TenantBuilder withEnvironment(String environment)`
   - After: `public TenantBuilder withEnvironment(ApiKeyEnvironment environment)`
4. The `create()` method calls `tenantService.createTenant(name, environment)` — this now passes `ApiKeyEnvironment` matching the updated `TenantService.createTenant(String, ApiKeyEnvironment)` signature. No change needed in the method body.
5. Update Javadoc example if present: change `"LIVE"` to `ApiKeyEnvironment.PROD`.

**ApiKeyBuilder.java** — 1 change:
1. Change the `environment` field default:
   - Before: `private String environment = "LIVE";`
   - After: `private String environment = "PROD";`
   Note: ApiKeyBuilder uses raw JDBC INSERT with String values, so the field stays as String type. Only the default value changes from "LIVE" to "PROD".
2. Update Javadoc example: change `.withEnvironment("LIVE")` to `.withEnvironment("PROD")`.
  </action>
  <verify>
    <automated>cd /Users/mokwen/dev/gitrepos/bluegithub/payam && grep -n "LIVE" src/test/java/com/softropic/payam/e2e/builder/TenantBuilder.java src/test/java/com/softropic/payam/e2e/builder/ApiKeyBuilder.java || echo "No LIVE occurrences — OK"</automated>
  </verify>
  <acceptance_criteria>
    - TenantBuilder.java contains `private ApiKeyEnvironment environment = ApiKeyEnvironment.PROD`
    - TenantBuilder.java contains `import com.softropic.payam.tenant.contract.ApiKeyEnvironment`
    - TenantBuilder.java does NOT contain the string `"LIVE"`
    - TenantBuilder.java `withEnvironment` method accepts `ApiKeyEnvironment` parameter
    - ApiKeyBuilder.java contains `private String environment = "PROD"`
    - ApiKeyBuilder.java does NOT contain `"LIVE"` as a value (Javadoc may mention it in "was LIVE" context, but the default field must be "PROD")
  </acceptance_criteria>
  <done>Both test builders default to PROD, TenantBuilder uses typed ApiKeyEnvironment</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all "LIVE" call sites in integration tests to "PROD" / ApiKeyEnvironment.PROD</name>
  <files>
    src/test/java/com/softropic/payam/tenant/TenantProvisioningIT.java,
    src/test/java/com/softropic/payam/tenant/TenantFilterChainIT.java,
    src/test/java/com/softropic/payam/tenant/TenantAdminResourceIT.java,
    src/test/java/com/softropic/payam/orange/OrangeMoneyPortIT.java,
    src/test/java/com/softropic/payam/mtn/MtnMoMoPortIT.java,
    src/test/java/com/softropic/payam/payment/PaymentOrchestratorIT.java,
    src/test/java/com/softropic/payam/fraud/FraudEngineIT.java,
    src/test/java/com/softropic/payam/fee/FeeEngineIT.java,
    src/test/java/com/softropic/payam/transaction/TransactionStateMachineIT.java,
    src/test/java/com/softropic/payam/transaction/PaymentEventLogIT.java,
    src/test/java/com/softropic/payam/transaction/LedgerServiceIT.java,
    src/test/java/com/softropic/payam/transaction/IdempotencyServiceIT.java,
    src/test/java/com/softropic/payam/webhook/WebhookDeliveryIT.java,
    src/test/java/com/softropic/payam/webhook/WebhookDoubleCheckIT.java
  </files>
  <read_first>
    src/test/java/com/softropic/payam/tenant/TenantProvisioningIT.java,
    src/test/java/com/softropic/payam/tenant/TenantFilterChainIT.java,
    src/test/java/com/softropic/payam/tenant/TenantAdminResourceIT.java
  </read_first>
  <action>
There are two categories of "LIVE" call sites:

**Category A: Direct `tenantService.createTenant(name, "LIVE")` calls** — These must change to `tenantService.createTenant(name, ApiKeyEnvironment.PROD)` because the method parameter is now `ApiKeyEnvironment`. Add `import com.softropic.payam.tenant.contract.ApiKeyEnvironment;` to each file.

Files and approximate line numbers (executor should grep to confirm exact positions):
- `TenantProvisioningIT.java`: ~7 occurrences (lines 68, 104, 115, 127, 145, 172, 173) — all `createTenant("...", "LIVE")` → `createTenant("...", ApiKeyEnvironment.PROD)`
- `TenantAdminResourceIT.java`: ~3 occurrences (lines 140, 169, 194)
- `OrangeMoneyPortIT.java`: 1 occurrence (line 71)
- `MtnMoMoPortIT.java`: 1 occurrence (line 78)
- `PaymentOrchestratorIT.java`: 1 occurrence (line 102)
- `FraudEngineIT.java`: 1 occurrence (line 113)
- `FeeEngineIT.java`: 1 occurrence (line 258)
- `TransactionStateMachineIT.java`: 1 occurrence (line 70)
- `PaymentEventLogIT.java`: 1 occurrence (line 69)
- `LedgerServiceIT.java`: 1 occurrence (line 76)
- `IdempotencyServiceIT.java`: 1 occurrence (line 76)
- `WebhookDeliveryIT.java`: 1 occurrence (line 173)
- `WebhookDoubleCheckIT.java`: 1 occurrence (line 153)

**Category B: HTTP request body Map values `"environment", "LIVE"`** — These are JSON payloads sent via RestTemplate to the REST endpoint. The REST endpoint still accepts a String in the JSON body and does `ApiKeyEnvironment.valueOf()`. Change `"LIVE"` to `"PROD"` in these Map literals.

Files:
- `TenantFilterChainIT.java`: ~5 occurrences (lines 106, 135, 156, 219, 238) — all `Map.of("name", "...", "environment", "LIVE")` → `Map.of("name", "...", "environment", "PROD")`
- `TenantFilterChainIT.java`: ~3 direct `createTenant("...", "LIVE")` calls (lines 97, 210, 212, 265, 266) → change to `createTenant("...", ApiKeyEnvironment.PROD)`

**Process:**
1. Run `grep -rn '"LIVE"' src/test/` to get the definitive list of ALL occurrences.
2. For each file with `createTenant(..., "LIVE")` calls: add the `ApiKeyEnvironment` import and change `"LIVE"` to `ApiKeyEnvironment.PROD`.
3. For each file with Map.of(..., "LIVE") HTTP body values: change `"LIVE"` to `"PROD"` (these remain Strings since they are JSON values).
4. After all changes, run `grep -rn '"LIVE"' src/` to confirm ZERO occurrences remain in the entire source tree.
  </action>
  <verify>
    <automated>cd /Users/mokwen/dev/gitrepos/bluegithub/payam && mvn test -Dspring.profiles.active=dev -q 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '"LIVE"' src/` returns zero results (no occurrence of "LIVE" in any Java source file)
    - Every test file that calls `tenantService.createTenant()` imports `com.softropic.payam.tenant.contract.ApiKeyEnvironment`
    - Every `createTenant(..., "LIVE")` call is changed to `createTenant(..., ApiKeyEnvironment.PROD)`
    - Every `Map.of(..., "environment", "LIVE")` is changed to `Map.of(..., "environment", "PROD")`
    - `mvn test -Dspring.profiles.active=dev` exits 0 (full test suite passes)
  </acceptance_criteria>
  <done>Zero "LIVE" occurrences in source tree, full test suite green, Flyway V18+V19 migrations verified via Testcontainers</done>
</task>

</tasks>

<verification>
- `grep -rn '"LIVE"' src/` returns zero results
- `mvn test -Dspring.profiles.active=dev` exits 0
- Flyway V18 and V19 are applied successfully (verified by test suite running on Testcontainers with fresh schema)
</verification>

<success_criteria>
- No occurrence of "LIVE" remains in any Java source file (production or test)
- Full test suite passes including all Flyway migrations on a fresh Testcontainers PostgreSQL database
- TenantBuilder uses typed ApiKeyEnvironment.PROD default
- ApiKeyBuilder uses "PROD" string default
- All 14+ integration test files compile and pass with the updated createTenant signature
</success_criteria>

<output>
After completion, create `.planning/phases/27-schema-and-enum-migration/27-02-SUMMARY.md`
</output>

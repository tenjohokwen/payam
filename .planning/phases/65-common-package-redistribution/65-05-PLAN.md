---
phase: 65-common-package-redistribution
plan: "05"
type: execute
wave: 5
depends_on: ["65-04"]
files_modified:
  - src/main/java/com/softropic/payam/common/ (delete — empty parent directory shell)
  - src/test/java/com/softropic/payam/common/ (delete IF AND ONLY IF empty — any test stragglers must be relocated/decided here)
  - src/test/java/com/softropic/payam/common/AdminLogin.java (relocate to src/test/java/com/softropic/payam/e2e/AdminLogin.java per RESEARCH.md — used by e2e and platform admin tests)
  - src/test/java/com/softropic/payam/common/HttpTestClient.java (relocate to src/test/java/com/softropic/payam/e2e/HttpTestClient.java per RESEARCH.md — used by SecurityFilterChainIT, SecurityIT; update its import of common.client.Client → infrastructure.client.Client per Pitfall 8)
  - src/test/java/com/softropic/payam/common/TransactionExceptionSimulator.java (relocate to src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java per RESEARCH.md — test-only Spring component)
  - src/test/java/com/softropic/payam/common/configtest/JacksonTest.java (relocate to src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java per RESEARCH.md — tests CommonConfig now at infrastructure.config)
autonomous: true
requirements: [CMN-04]

must_haves:
  truths:
    - "src/main/java/com/softropic/payam/common/ directory does not exist (production common fully removed)"
    - "src/test/java/com/softropic/payam/common/ directory does not exist (all test common files relocated to their final destinations)"
    - "find src -name '*.java' | xargs grep 'com.softropic.payam.common' returns zero results (CMN-04 primary success criterion)"
    - "find src -type d -name 'common' -path '*/payam/common' returns zero results (CMN-04 secondary success criterion — no payam.common directory anywhere)"
    - "find src -type d -name 'common' returns results ONLY for platform/security/common (Pitfall 2 — this is a DIFFERENT package and must remain)"
    - "Test stragglers relocated to their owner destinations: AdminLogin.java → test/e2e/; HttpTestClient.java → test/e2e/ with its `common.client.Client` import retargeted to `infrastructure.client.Client`; TransactionExceptionSimulator.java → test/infrastructure/; configtest/JacksonTest.java → test/infrastructure/config/"
    - "All Plan 01-04 work preserved: zero common.{exception,message,config,logging,client,threadpool,util,validation,dto,payment,refund,consumer,enums}.* references AND zero common.{ClockProvider,Constants,Gender,Predicate,TimeGuru,TransactionIdProvider} references"
    - "Phase 64 work preserved: zero com.softropic.payam.{mtn,orange}.* (flat-package) references"
    - "platform.security.common.* files remain INTACT (Pitfall 2 — package declarations under platform/security/common/event/ and platform/security/common/util/ unchanged)"
    - "mvn -q test-compile exits 0 (BUILD-01 compile gate)"
    - "mvn -q verify exits 0 (BUILD-01 full suite green — final v12 quality gate)"
    - "REST API contracts, DB schema, Flyway migrations unchanged (BUILD-02)"
    - "Spring component-scan, Flyway configuration, and security filter registration verified functional (BUILD-03)"
  artifacts:
    - path: "src/test/java/com/softropic/payam/e2e/AdminLogin.java"
      provides: "AdminLogin test utility at final destination (RESEARCH.md test-file mapping)"
      contains: "package com.softropic.payam.e2e;"
    - path: "src/test/java/com/softropic/payam/e2e/HttpTestClient.java"
      provides: "HttpTestClient test utility at final destination, with import of infrastructure.client.Client (Pitfall 8 — retargeted from common.client.Client)"
      contains: "package com.softropic.payam.e2e;"
    - path: "src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java"
      provides: "TransactionExceptionSimulator test Spring component at final destination"
      contains: "package com.softropic.payam.infrastructure;"
    - path: "src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java"
      provides: "JacksonTest at final destination (tests CommonConfig now living in infrastructure.config — co-located)"
      contains: "package com.softropic.payam.infrastructure.config;"
  key_links:
    - from: "Final state — src/main/java/com/softropic/payam/common/"
      to: "(does not exist)"
      via: "directory deletion — CMN-04 primary success criterion"
      pattern: "directory absence"
    - from: "Final state — find src -name *.java | xargs grep com.softropic.payam.common"
      to: "(zero results)"
      via: "grep verification — CMN-04 primary success criterion (RESEARCH.md final gate)"
      pattern: "grep returns nothing"
    - from: "test/e2e/HttpTestClient.java"
      to: "infrastructure.client.Client"
      via: "import statement (retargeted per Pitfall 8)"
      pattern: "import com\\.softropic\\.payam\\.infrastructure\\.client\\.Client"
    - from: "test/infrastructure/config/JacksonTest.java"
      to: "infrastructure.config.CommonConfig"
      via: "co-located test (same package as production CommonConfig from Plan 01)"
      pattern: "package com\\.softropic\\.payam\\.infrastructure\\.config;"
---

<objective>
Fifth and final wave of Phase 65: close out CMN-04 by (1) verifying production `common/` is empty, (2) relocating any remaining test stragglers to their owner destinations, (3) deleting the empty `common/` shell, and (4) running the definitive grep gate plus `mvn verify`.

This plan has minimal file changes — by the time Plan 04 completes, all production .java files have been moved out of `common/`. Plan 05 is primarily a verification gate:

1. **Assert production `common/` has no .java files**: After Plans 01-04, every production file under `src/main/java/com/softropic/payam/common/` should be gone. This plan fails immediately if any stragglers exist — a failure here means Plans 01-04 missed something.

2. **Relocate the 4 remaining test common files**: Per RESEARCH.md test-file mapping, these go to their owner destinations:
   - `AdminLogin.java` → `test/e2e/AdminLogin.java`
   - `HttpTestClient.java` → `test/e2e/HttpTestClient.java` (also: retarget its import of `common.client.Client` to `infrastructure.client.Client` per Pitfall 8)
   - `TransactionExceptionSimulator.java` → `test/infrastructure/TransactionExceptionSimulator.java`
   - `configtest/JacksonTest.java` → `test/infrastructure/config/JacksonTest.java` (co-located with production CommonConfig from Plan 01)

3. **Delete the empty `common/` shells**: Both `src/main/java/com/softropic/payam/common/` and `src/test/java/com/softropic/payam/common/`.

4. **Run the definitive CMN-04 success criteria**:
   - `find src -name "*.java" | xargs grep "com.softropic.payam.common"` returns ZERO results
   - `find src -type d -path '*/payam/common'` returns ZERO results
   - `mvn verify` exits 0 (BUILD-01 final v12 quality gate)

CRITICAL pitfall invariants checked at this stage:

- **Pitfall 2 — `platform.security.common.*` remains**: The grep `find src -type d -name 'common'` will match `src/main/java/com/softropic/payam/platform/security/common/`. This is correct — that directory is part of `platform.security` and is NOT removed by Phase 65. The CMN-04 success grep excludes it via the explicit path filter `-path '*/payam/common'` (matches only the direct `payam/common` directory, not nested ones).

- **Pitfall 8 — `HttpTestClient.java` import of `common.client.Client`**: This file is a test utility that imports the production `Client` class from `common.client`. Plan 02 moved `Client` to `infrastructure.client`. Plan 02's sweep may not have touched `HttpTestClient.java` because the file was at `test/common/` and excluded by Plan 02's filter. Plan 05 retargets this import as part of the relocation.

After Plan 05 completes, the v12 milestone (Phases 61-65) is fully shipped:
- Phase 61: infrastructure layer created
- Phase 62: platform layer reorganized
- Phase 63: payment domain consolidated
- Phase 64: provider infrastructure encapsulated
- **Phase 65: common package redistributed and removed** ← this is the closure

Output: 4 test files relocated with package declarations rewritten; 1 import retarget in HttpTestClient.java; empty `common/` shell deleted from both production and test trees; CMN-04 grep gate passes (zero `com.softropic.payam.common` references anywhere); `mvn verify` green (BUILD-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-common-package-redistribution/65-RESEARCH.md
@.planning/phases/65-common-package-redistribution/65-VALIDATION.md
@.planning/phases/65-common-package-redistribution/65-01-SUMMARY.md
@.planning/phases/65-common-package-redistribution/65-02-SUMMARY.md
@.planning/phases/65-common-package-redistribution/65-03-SUMMARY.md
@.planning/phases/65-common-package-redistribution/65-04-SUMMARY.md

<interfaces>
<!-- Authoritative test-file inventory verified by `ls`/`grep` on 2026-05-12. Plans 01-04 must complete first. -->

Test files to relocate (4 files per RESEARCH.md):

  src/test/java/com/softropic/payam/common/AdminLogin.java
    → src/test/java/com/softropic/payam/e2e/AdminLogin.java
    Reason: Used by e2e/ and platform admin tests.
    Package rewrite: `package com.softropic.payam.common;` → `package com.softropic.payam.e2e;`

  src/test/java/com/softropic/payam/common/HttpTestClient.java
    → src/test/java/com/softropic/payam/e2e/HttpTestClient.java
    Reason: Used by SecurityFilterChainIT, SecurityIT (research mapping).
    Package rewrite: `package com.softropic.payam.common;` → `package com.softropic.payam.e2e;`
    SPECIAL — Import retarget per RESEARCH.md Pitfall 8: `import com.softropic.payam.common.client.Client;` → `import com.softropic.payam.infrastructure.client.Client;`
    Note: This import may have already been retargeted by Plan 02 IF Plan 02's sed sweep visited test/common/ files. The CMN-04 grep gate (`grep com.softropic.payam.common` returns zero) will catch any miss — Plan 05 fixes it defensively.

  src/test/java/com/softropic/payam/common/TransactionExceptionSimulator.java
    → src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java
    Reason: Test-only Spring component (research mapping).
    Package rewrite: `package com.softropic.payam.common;` → `package com.softropic.payam.infrastructure;`

  src/test/java/com/softropic/payam/common/configtest/JacksonTest.java
    → src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java
    Reason: Tests CommonConfig — moved to infrastructure.config in Plan 01 (research mapping).
    Package rewrite: `package com.softropic.payam.common.configtest;` → `package com.softropic.payam.infrastructure.config;`

External callers of these test utility files (test code may import them — verify by grep):

  AdminLogin: imported by tests in payment.{disbursement, core, webhook} and e2e/ (varies by build state)
  HttpTestClient: imported by SecurityIT.java, SecurityFilterChainIT.java, possibly others
  TransactionExceptionSimulator: imported by tests that simulate transaction failures (typically a few callbacks/concurrency ITs)
  JacksonTest: standalone test class, likely no external imports

The actual caller list MUST be captured in Step A of the task — the planner cannot enumerate test-test imports definitively without running grep at execution time.

Directories to delete (must be EMPTY before deletion):

  src/main/java/com/softropic/payam/common/                  ← after Plans 01-04, all .java moved; expected empty
  src/test/java/com/softropic/payam/common/                  ← after this plan's relocations, expected empty
  src/test/java/com/softropic/payam/common/configtest/       ← after JacksonTest moved, expected empty parent

CRITICAL — directories that MUST NOT be touched (Pitfall 2):

  src/main/java/com/softropic/payam/platform/security/common/event/**     (package: com.softropic.payam.platform.security.common.event)
  src/main/java/com/softropic/payam/platform/security/common/util/**      (package: com.softropic.payam.platform.security.common.util)
  These are NOT removed by Phase 65. The CMN-04 grep gate excludes them via path filter `-path '*/payam/common'` (matches `payam/common` directly under payam, not nested under `payam/platform/security/common`).

Definitive CMN-04 success grep (from RESEARCH.md):

  Primary:    find src -name "*.java" | xargs grep "com.softropic.payam.common"
              → MUST return zero results

  Secondary:  find src -type d -path '*/payam/common'
              → MUST return zero results (the `payam/common` directory under either main or test is absent)

  Pitfall 2 exclusion check (these MUST STILL EXIST):
              find src -type d -name "common"
              → Must return ONLY paths containing `platform/security/common` (not `payam/common`)
              find src -name "*.java" | xargs grep -l "com.softropic.payam.platform.security.common"
              → MUST return a non-empty list (platform.security.common files are untouched)

Test infrastructure runtime check: After moving the 4 test files, run `mvn -q verify` to verify Spring's test ApplicationContext can still locate the moved Spring components (TransactionExceptionSimulator is a `@Component`; check that its discovery in `src/test/java/com/softropic/payam/infrastructure/` is included in the `@SpringBootTest` component scan — it is, because `@SpringBootApplication` at `com.softropic.payam` automatically discovers all sub-packages).

macOS sed limitation (research + STATE.md): BSD sed `\b` is unsupported. Use explicit per-file sed patterns since only 4 files move and they go to different destinations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate 4 test stragglers, delete empty common/ shells, run definitive CMN-04 grep gate, run mvn verify</name>
  <files>
    src/test/java/com/softropic/payam/e2e/AdminLogin.java (new),
    src/test/java/com/softropic/payam/e2e/HttpTestClient.java (new — with retargeted Client import),
    src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java (new),
    src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java (new),
    src/test/java/com/softropic/payam/common/AdminLogin.java (delete — moved),
    src/test/java/com/softropic/payam/common/HttpTestClient.java (delete — moved),
    src/test/java/com/softropic/payam/common/TransactionExceptionSimulator.java (delete — moved),
    src/test/java/com/softropic/payam/common/configtest/JacksonTest.java (delete — moved),
    src/test/java/com/softropic/payam/common/configtest (delete empty parent),
    src/test/java/com/softropic/payam/common (delete empty shell),
    src/main/java/com/softropic/payam/common (delete empty shell — should already be empty after Plan 04),
    All test files that import any of the 4 relocated test utilities (caller list captured in Step A)
  </files>
  <read_first>
    src/test/java/com/softropic/payam/common/AdminLogin.java,
    src/test/java/com/softropic/payam/common/HttpTestClient.java,
    src/test/java/com/softropic/payam/common/TransactionExceptionSimulator.java,
    src/test/java/com/softropic/payam/common/configtest/JacksonTest.java,
    .planning/phases/65-common-package-redistribution/65-RESEARCH.md,
    .planning/phases/65-common-package-redistribution/65-01-SUMMARY.md,
    .planning/phases/65-common-package-redistribution/65-02-SUMMARY.md,
    .planning/phases/65-common-package-redistribution/65-03-SUMMARY.md,
    .planning/phases/65-common-package-redistribution/65-04-SUMMARY.md
  </read_first>
  <action>
    Pre-flight — Confirm Plans 01-04 completed and the codebase is in the expected pre-Plan-05 state.

    1. All grep results MUST return 0 (every common.* subpackage moved):
        grep -rn 'com\.softropic\.payam\.common\.\(exception\|message\|config\|logging\)\.' src --include='*.java' | wc -l
        grep -rn 'com\.softropic\.payam\.common\.\(client\|threadpool\|util\|validation\|dto\)\.' src --include='*.java' | wc -l
        grep -rn 'com\.softropic\.payam\.common\.\(ClockProvider\|Constants\|Predicate\|TimeGuru\|TransactionIdProvider\)' src --include='*.java' | wc -l
        grep -rn 'com\.softropic\.payam\.common\.\(payment\|refund\)\.' src --include='*.java' | wc -l
        grep -rn 'com\.softropic\.payam\.common\.\(consumer\|enums\|Gender\)' src --include='*.java' | wc -l
        grep -rn 'com\.softropic\.payam\.\(mtn\|orange\)\.' src --include='*.java' | wc -l
      Any non-zero result means a prior plan is incomplete — STOP and reconcile.

    2. Production common/ directory should contain NO Java files at the start of Plan 05:
        find src/main/java/com/softropic/payam/common -type f 2>/dev/null
      Expected: empty output. If any file is present, STOP — Plans 01-04 missed something.

    3. Test common/ directory should still contain the 4 expected test files (Plan 02 already moved TestClockProvider, BodySanitizerTest, CamMobileValidatorTest, InputValidatorTest):
        find src/test/java/com/softropic/payam/common -type f -name '*.java' 2>/dev/null
      Expected: 4 files — AdminLogin.java, HttpTestClient.java, TransactionExceptionSimulator.java, configtest/JacksonTest.java.
      If any are missing OR extra files exist, STOP and reconcile against Plan 02's SUMMARY.

    Step A — Capture authoritative test-test caller list BEFORE any move. Find which test files import the 4 relocating utilities:
      # Combined caller list across the 4 moving files
      grep -rln 'com\.softropic\.payam\.common\.\(AdminLogin\|HttpTestClient\|TransactionExceptionSimulator\)' src/test --include='*.java' \
        | grep -v '^src/test/java/com/softropic/payam/common/' \
        | sort -u > /tmp/65-05-external-callers.txt
      # JacksonTest is typically a standalone test class with no external imports — verify:
      grep -rln 'com\.softropic\.payam\.common\.configtest\.JacksonTest' src/test --include='*.java' \
        | grep -v '^src/test/java/com/softropic/payam/common/' \
        | sort -u >> /tmp/65-05-external-callers.txt
      # Deduplicate
      sort -u -o /tmp/65-05-external-callers.txt /tmp/65-05-external-callers.txt
      cat /tmp/65-05-external-callers.txt
      Count check: `wc -l /tmp/65-05-external-callers.txt` will vary (likely 5-15 callers based on test infrastructure). Record actual count in summary.

      ALSO check whether HttpTestClient.java still has a stale `common.client.Client` import (Pitfall 8 — may have been retargeted by Plan 02 or may need fix here):
        grep -n 'com\.softropic\.payam\.common\.client\.Client' src/test/java/com/softropic/payam/common/HttpTestClient.java
        # If this outputs a line, Plan 05 must retarget the import. If empty, Plan 02 already swept it — that's fine, Step F is a no-op for that import.

    Step B — Bulk move the 4 test files. Destinations: e2e/, infrastructure/, infrastructure/config/. The infrastructure/ test directory may or may not exist — `mkdir -p` first:
      mkdir -p src/test/java/com/softropic/payam/e2e                            # likely exists from earlier phases
      mkdir -p src/test/java/com/softropic/payam/infrastructure
      mkdir -p src/test/java/com/softropic/payam/infrastructure/config

      git mv src/test/java/com/softropic/payam/common/AdminLogin.java                   src/test/java/com/softropic/payam/e2e/AdminLogin.java
      git mv src/test/java/com/softropic/payam/common/HttpTestClient.java               src/test/java/com/softropic/payam/e2e/HttpTestClient.java
      git mv src/test/java/com/softropic/payam/common/TransactionExceptionSimulator.java   src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java
      git mv src/test/java/com/softropic/payam/common/configtest/JacksonTest.java       src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java

      # Remove the now-empty test common directories
      rmdir src/test/java/com/softropic/payam/common/configtest 2>/dev/null || true
      rmdir src/test/java/com/softropic/payam/common            2>/dev/null || true

      Verify:
        test -f src/test/java/com/softropic/payam/e2e/AdminLogin.java                                    must succeed
        test -f src/test/java/com/softropic/payam/e2e/HttpTestClient.java                                must succeed
        test -f src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java      must succeed
        test -f src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java                 must succeed
        test ! -d src/test/java/com/softropic/payam/common                                               must succeed (test common shell gone)

    Step C — Rewrite package declarations in the 4 moved test files. Use explicit per-file sed (4 files, 3 distinct destinations):

      # AdminLogin and HttpTestClient → e2e
      sed -i '' \
        -e 's|^package com\.softropic\.payam\.common;|package com.softropic.payam.e2e;|' \
        src/test/java/com/softropic/payam/e2e/AdminLogin.java \
        src/test/java/com/softropic/payam/e2e/HttpTestClient.java

      # TransactionExceptionSimulator → infrastructure (root, not a sub-package)
      sed -i '' \
        -e 's|^package com\.softropic\.payam\.common;|package com.softropic.payam.infrastructure;|' \
        src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java

      # JacksonTest → infrastructure.config
      sed -i '' \
        -e 's|^package com\.softropic\.payam\.common\.configtest;|package com.softropic.payam.infrastructure.config;|' \
        src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java

      Verify spot-checks:
        head -1 src/test/java/com/softropic/payam/e2e/AdminLogin.java                                    must output `package com.softropic.payam.e2e;`
        head -1 src/test/java/com/softropic/payam/e2e/HttpTestClient.java                                must output `package com.softropic.payam.e2e;`
        head -1 src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java      must output `package com.softropic.payam.infrastructure;`
        head -1 src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java                 must output `package com.softropic.payam.infrastructure.config;`

    Step D — Retarget HttpTestClient.java's import of `common.client.Client` (Pitfall 8). Apply sed even if the import was already retargeted by Plan 02 — idempotent:
      sed -i '' \
        -e 's|com\.softropic\.payam\.common\.client\.Client|com.softropic.payam.infrastructure.client.Client|g' \
        src/test/java/com/softropic/payam/e2e/HttpTestClient.java

      Also defensively sweep any other lingering common.* references in the 4 moved test files (none expected, but ensures CMN-04 grep gate passes):
        sed -i '' \
          -e 's|com\.softropic\.payam\.common\.exception\.|com.softropic.payam.infrastructure.exception.|g' \
          -e 's|com\.softropic\.payam\.common\.message\.|com.softropic.payam.infrastructure.message.|g' \
          -e 's|com\.softropic\.payam\.common\.config\.|com.softropic.payam.infrastructure.config.|g' \
          -e 's|com\.softropic\.payam\.common\.client\.|com.softropic.payam.infrastructure.client.|g' \
          -e 's|com\.softropic\.payam\.common\.util\.|com.softropic.payam.infrastructure.util.|g' \
          src/test/java/com/softropic/payam/e2e/AdminLogin.java \
          src/test/java/com/softropic/payam/e2e/HttpTestClient.java \
          src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java \
          src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java

      Verify the 4 moved test files have ZERO residual common.* refs:
        grep -c 'com\.softropic\.payam\.common\.' \
          src/test/java/com/softropic/payam/e2e/AdminLogin.java \
          src/test/java/com/softropic/payam/e2e/HttpTestClient.java \
          src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java \
          src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java \
          | grep -v ':0$'
        must output nothing (every file 0 hits)

    Step E — Update all external callers' imports. The 4 test utilities have different destinations; use specific sed patterns:
      while IFS= read -r f; do
        if [ -f "$f" ]; then
          sed -i '' \
            -e 's|com\.softropic\.payam\.common\.AdminLogin|com.softropic.payam.e2e.AdminLogin|g' \
            -e 's|com\.softropic\.payam\.common\.HttpTestClient|com.softropic.payam.e2e.HttpTestClient|g' \
            -e 's|com\.softropic\.payam\.common\.TransactionExceptionSimulator|com.softropic.payam.infrastructure.TransactionExceptionSimulator|g' \
            -e 's|com\.softropic\.payam\.common\.configtest\.JacksonTest|com.softropic.payam.infrastructure.config.JacksonTest|g' \
            "$f"
        fi
      done < /tmp/65-05-external-callers.txt

    Step F — Delete the empty production `common/` shell. After Plan 04, no .java files should remain; the directory itself is an empty shell:
      find src/main/java/com/softropic/payam/common -type f 2>/dev/null
        # MUST output nothing — pre-flight already verified this, but re-check before deletion.
      find src/main/java/com/softropic/payam/common -type d 2>/dev/null
        # May output `src/main/java/com/softropic/payam/common` itself (the empty parent shell).

      rmdir src/main/java/com/softropic/payam/common 2>/dev/null || true
      # If rmdir fails, the directory still has contents — STOP and reconcile against Plans 01-04.

      Verify:
        test ! -d src/main/java/com/softropic/payam/common         must succeed (production common shell deleted)

    Step G — Annotation/wiring preservation audit. Spot-check the 4 moved test files retain their Spring/Jackson annotations:
      grep -E '@Component|@Configuration|@TestConfiguration' src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java     must show a Spring stereotype (the file is documented as a "test-only Spring component" in RESEARCH.md)
      grep -E '@SpringBootTest|@Test|@JsonTest' src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java                              must show JUnit/Spring test annotations
      grep -q 'import com\.softropic\.payam\.infrastructure\.client\.Client' src/test/java/com/softropic/payam/e2e/HttpTestClient.java                must succeed (Pitfall 8 retarget verified)

    Step H — Definitive CMN-04 success criterion grep gate. ALL of these must pass:

      # Primary CMN-04 grep gate (RESEARCH.md success criterion):
      MATCHES=$(find src -name "*.java" | xargs grep -l "com.softropic.payam.common" 2>/dev/null | grep -v "/platform/security/common/" | wc -l | tr -d ' ')
      [ "$MATCHES" = "0" ]   # MUST be 0 — no Java source file (other than platform.security.common/* which is a different package) contains the string `com.softropic.payam.common`

      # Stricter version of the same check (using grep -rn rather than xargs):
      [ "$(grep -rn 'com\.softropic\.payam\.common\.' src --include='*.java' | grep -v '/platform/security/common/' | wc -l | tr -d ' ')" = "0" ]

      # Pure-textual gate (research final-gate command):
      find src -name "*.java" -print0 | xargs -0 grep -l "com.softropic.payam.common" | grep -v "/platform/security/common/"
      # MUST output nothing.

      # Directory absence check:
      [ -z "$(find src -type d -path '*/payam/common' 2>/dev/null)" ]
      # MUST succeed (no payam/common directory under src/main or src/test).

      # Pitfall 2 invariant — platform.security.common files MUST still exist:
      [ "$(grep -rn '^package com\.softropic\.payam\.platform\.security\.common' src/main/java --include='*.java' | wc -l | tr -d ' ')" != "0" ]
      # MUST succeed — these files are untouched and still declare their original package.

      # Also verify that `find src -type d -name common` only returns platform/security/common paths:
      find src -type d -name 'common' | grep -v 'platform/security/common'
      # MUST output nothing.

    Step I — Compile + full verify (BUILD-01 + BUILD-03 final v12 gate):
      mvn -q test-compile                  must exit 0
      mvn -q verify                        must exit 0   (full integration suite — final v12 quality gate: 474 unit + 301 integration tests green, confirming the full package hierarchy is correct with no broken imports)

    Step J — Atomic single commit. All 4 test file relocations, package declaration rewrites, ~5-15 external test-caller import updates, HttpTestClient import retarget, and `common/` shell deletions land in a single commit.
  </action>
  <verify>
    <automated>[ -f src/test/java/com/softropic/payam/e2e/AdminLogin.java ] && [ -f src/test/java/com/softropic/payam/e2e/HttpTestClient.java ] && [ -f src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java ] && [ -f src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java ] && ! test -d src/main/java/com/softropic/payam/common && ! test -d src/test/java/com/softropic/payam/common && [ "$(find src -name '*.java' -print0 | xargs -0 grep -l 'com.softropic.payam.common' 2>/dev/null | grep -v '/platform/security/common/' | wc -l | tr -d ' ')" = "0" ] && [ -z "$(find src -type d -path '*/payam/common' 2>/dev/null)" ] && [ "$(find src -type d -name 'common' | grep -v 'platform/security/common' | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -rn '^package com\.softropic\.payam\.platform\.security\.common' src/main/java --include='*.java' | wc -l | tr -d ' ')" != "0" ] && head -1 src/test/java/com/softropic/payam/e2e/AdminLogin.java | grep -q '^package com\.softropic\.payam\.e2e;$' && head -1 src/test/java/com/softropic/payam/e2e/HttpTestClient.java | grep -q '^package com\.softropic\.payam\.e2e;$' && head -1 src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java | grep -q '^package com\.softropic\.payam\.infrastructure;$' && head -1 src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java | grep -q '^package com\.softropic\.payam\.infrastructure\.config;$' && grep -q 'import com\.softropic\.payam\.infrastructure\.client\.Client' src/test/java/com/softropic/payam/e2e/HttpTestClient.java && [ "$(grep -c 'com\.softropic\.payam\.common\.' src/test/java/com/softropic/payam/e2e/AdminLogin.java src/test/java/com/softropic/payam/e2e/HttpTestClient.java src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java | grep -v ':0$' | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -rn 'com\.softropic\.payam\.\(mtn\|orange\)\.' src --include='*.java' | wc -l | tr -d ' ')" = "0" ] && mvn -q test-compile && mvn -q verify</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/test/java/com/softropic/payam/e2e/AdminLogin.java` returns 0
    - `test -f src/test/java/com/softropic/payam/e2e/HttpTestClient.java` returns 0
    - `test -f src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java` returns 0
    - `test -f src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java` returns 0
    - `test -d src/main/java/com/softropic/payam/common` returns false (production common shell deleted — CMN-04 secondary criterion)
    - `test -d src/test/java/com/softropic/payam/common` returns false (test common shell deleted)
    - `find src -name "*.java" -print0 | xargs -0 grep -l "com.softropic.payam.common" 2>/dev/null | grep -v "/platform/security/common/" | wc -l` outputs `0` — **CMN-04 primary success criterion**: zero `com.softropic.payam.common` references anywhere in source code (excluding the legitimately-different platform.security.common package)
    - `find src -type d -path '*/payam/common' | wc -l` outputs `0` — **CMN-04 secondary success criterion**: the `payam/common` directory does not exist under either main or test
    - `find src -type d -name 'common' | grep -v 'platform/security/common' | wc -l` outputs `0` (no other `common` directories remain)
    - `grep -rn '^package com\.softropic\.payam\.platform\.security\.common' src/main/java --include='*.java' | wc -l` returns a value greater than `0` (Pitfall 2 invariant — platform.security.common files untouched)
    - `head -1 src/test/java/com/softropic/payam/e2e/AdminLogin.java` outputs exactly `package com.softropic.payam.e2e;`
    - `head -1 src/test/java/com/softropic/payam/e2e/HttpTestClient.java` outputs exactly `package com.softropic.payam.e2e;`
    - `head -1 src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java` outputs exactly `package com.softropic.payam.infrastructure;`
    - `head -1 src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java` outputs exactly `package com.softropic.payam.infrastructure.config;`
    - `grep -q 'import com\.softropic\.payam\.infrastructure\.client\.Client' src/test/java/com/softropic/payam/e2e/HttpTestClient.java` returns 0 (Pitfall 8 — Client import retargeted to infrastructure.client)
    - `grep -c 'com\.softropic\.payam\.common\.' src/test/java/com/softropic/payam/e2e/AdminLogin.java src/test/java/com/softropic/payam/e2e/HttpTestClient.java src/test/java/com/softropic/payam/infrastructure/TransactionExceptionSimulator.java src/test/java/com/softropic/payam/infrastructure/config/JacksonTest.java | grep -v ':0$' | wc -l` outputs `0` (no residual common.* refs in any of the 4 moved test files)
    - `grep -rn 'com\.softropic\.payam\.\(mtn\|orange\)\.' src --include='*.java' | wc -l` outputs `0` (Phase 64 work preserved)
    - `mvn -q test-compile` exits 0 (BUILD-01 compile gate)
    - `mvn -q verify` exits 0 (BUILD-01 final v12 integration suite — 474 unit + 301 integration tests green, confirming v12 milestone shipped)
  </acceptance_criteria>
  <done>
    Phase 65 closed: production `common/` package fully removed; 4 test stragglers relocated to their owner destinations (AdminLogin + HttpTestClient → test/e2e/; TransactionExceptionSimulator → test/infrastructure/; configtest/JacksonTest → test/infrastructure/config/); HttpTestClient.java's `common.client.Client` import retargeted to `infrastructure.client.Client` (Pitfall 8); empty `common/` shells deleted from both production and test trees; all test-test callers updated; ~5-15 caller files updated based on Step A grep; definitive CMN-04 success criterion grep gate passes (zero `com.softropic.payam.common` references anywhere in `src/`, excluding the legitimately-different `platform.security.common.*` package); `payam/common` directory does not exist under either main or test trees; platform.security.common.* files NOT touched (Pitfall 2 invariant verified); Plans 01-04 work preserved; Phase 64 work preserved; Spring component-scan continues to discover the relocated TransactionExceptionSimulator under infrastructure/ automatically (no @ComponentScan changes — `@SpringBootApplication` at `com.softropic.payam` covers it); REST API contracts, DB schema, Flyway migrations unchanged (BUILD-02); mvn verify exits 0 with zero test regressions (BUILD-01) — this is the FINAL v12 quality gate, confirming the entire architectural reorganization (Phases 61-65) is shipped. CMN-04 fully satisfied. v12 milestone (Architectural Reorganization) ready for milestone-level verification and retrospective.
  </done>
</task>

</tasks>

<verification>
- `mvn -q test-compile` exits 0
- `mvn -q verify` exits 0 (final v12 quality gate — full integration suite)
- **CMN-04 primary**: `find src -name "*.java" -print0 | xargs -0 grep -l "com.softropic.payam.common" 2>/dev/null | grep -v "/platform/security/common/"` returns nothing
- **CMN-04 secondary**: `find src -type d -path '*/payam/common'` returns nothing
- Pitfall 2 invariant: `grep -rn '^package com\.softropic\.payam\.platform\.security\.common' src/main/java --include='*.java'` returns a non-zero count (platform.security.common files untouched)
- Pitfall 8 invariant: `grep -q 'import com\.softropic\.payam\.infrastructure\.client\.Client' src/test/java/com/softropic/payam/e2e/HttpTestClient.java` succeeds
- All prior plan work preserved: zero references for each of `common.{exception,message,config,logging,client,threadpool,util,validation,dto,payment,refund,consumer,enums}.*` and zero references for root-level `common.{ClockProvider,Constants,Gender,Predicate,TimeGuru,TransactionIdProvider}`
- Phase 64 work preserved: zero flat `mtn`/`orange` package references
- The 4 relocated test files exist at their final paths with the correct package declarations
- `common/` directory shell deleted from both production and test trees
</verification>

<success_criteria>
CMN-04 fully satisfied — **v12 architectural reorganization complete**:
- `com.softropic.payam.common` package directory absent from source tree (both main and test)
- `find src -name "*.java" | xargs grep "com.softropic.payam.common"` returns ZERO results (excluding the legitimately-different `platform.security.common.*` package, which was always meant to remain)
- 4 test stragglers relocated to their owner destinations (AdminLogin, HttpTestClient → test/e2e/; TransactionExceptionSimulator → test/infrastructure/; JacksonTest → test/infrastructure/config/)
- HttpTestClient's `common.client.Client` import retargeted to `infrastructure.client.Client` (Pitfall 8 closure)
- `mvn verify` green (BUILD-01 — final v12 gate)
- REST endpoints, DB schema, Flyway migrations unchanged (BUILD-02)
- Spring component-scan, Flyway configuration, and security filter registration verified functional (BUILD-03) — relocated test Spring component (TransactionExceptionSimulator) auto-discovered at new path
- All Plan 01-04 work preserved
- Phase 64 work preserved
- platform.security.common.* files NOT touched (Pitfall 2 invariant)
- Phase 65 complete; v12 milestone (Phases 61-65) ready for final milestone-level verification
</success_criteria>

<output>
After completion, create `.planning/phases/65-common-package-redistribution/65-05-SUMMARY.md` documenting:
- 4 test files relocated with their final destinations and package declarations:
  - AdminLogin: test/common/ → test/e2e/ (package: e2e)
  - HttpTestClient: test/common/ → test/e2e/ (package: e2e; `common.client.Client` import retargeted to `infrastructure.client.Client` — Pitfall 8)
  - TransactionExceptionSimulator: test/common/ → test/infrastructure/ (package: infrastructure)
  - configtest/JacksonTest: test/common/configtest/ → test/infrastructure/config/ (package: infrastructure.config)
- External test-test callers updated (record full list from Step A grep + count)
- Confirmation that HttpTestClient.java's import of `common.client.Client` was retargeted to `infrastructure.client.Client` (Pitfall 8 invariant honored)
- Confirmation that production `common/` directory was successfully deleted (Plan 04 left it empty; Plan 05 ran rmdir successfully)
- Confirmation that test `common/` directory was successfully deleted after the 4 test relocations
- **CMN-04 grep gate result**: confirm `find src -name "*.java" | xargs grep "com.softropic.payam.common" | grep -v "/platform/security/common/"` returns zero results
- **CMN-04 directory check**: confirm `find src -type d -path '*/payam/common'` returns zero results
- Confirmation that `platform.security.common.*` files were NOT touched (Pitfall 2 invariant — `find src -type d -name common` returns ONLY platform/security/common paths; package declarations under those paths remain `com.softropic.payam.platform.security.common.*`)
- Confirmation that Spring/JUnit annotations preserved on relocated test files (TransactionExceptionSimulator @Component, JacksonTest @SpringBootTest/@JsonTest/etc)
- Confirmation that ALL prior plan work (01-04) and Phase 64 work was preserved (zero stale references)
- `mvn verify` result (compile + full test-suite outcome) — **this is the final v12 quality gate**
- Phase 65 completion summary: CMN-01 (Plan 03), CMN-02 (Plans 01 + 02), CMN-03 (Plan 04), CMN-04 (this plan) all closed
- v12 milestone status: Phases 61, 62, 63, 64, 65 all complete; ready for milestone-level retrospective
- Any deviations from this plan
</output>
</content>
</invoke>
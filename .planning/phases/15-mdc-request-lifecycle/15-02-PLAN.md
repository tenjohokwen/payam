---
phase: 15-mdc-request-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/softropic/payam/transaction/service/TransactionService.java
  - src/main/java/com/softropic/payam/common/Constants.java
autonomous: true

must_haves:
  truths:
    - "Payment request logs contain transactionId (camelCase) as a top-level JSON field"
    - "Payment request logs contain externalReference (camelCase) as a top-level JSON field when present"
    - "The MDC key transactionId (not transaction_id) is queryable in Loki"
    - "Constants.TXN_ID_NAME reflects the current canonical MDC key name"
  artifacts:
    - path: "src/main/java/com/softropic/payam/transaction/service/TransactionService.java"
      provides: "camelCase MDC keys for transaction context"
      contains: "MDC.put(\"transactionId\""
    - path: "src/main/java/com/softropic/payam/common/Constants.java"
      provides: "Updated TXN_ID_NAME constant reflecting camelCase"
      contains: "TXN_ID_NAME"
  key_links:
    - from: "TransactionService.initiate()"
      to: "MDC key transactionId"
      via: "MDC.put(\"transactionId\", transactionId)"
      pattern: "MDC\\.put\\(\"transactionId\""
---

<objective>
Rename TransactionService MDC keys from snake_case to camelCase to align with the logging.md standard and LOG-MDC-02 requirement. Update Constants.TXN_ID_NAME to reflect the new canonical key name.

Purpose: LOG-MDC-02 requires transactionId and externalReference (camelCase) in MDC. The current code sets transaction_id and external_reference (snake_case). No dashboards exist yet (Phase 14 just landed), so this rename is safe and must happen before Phase 16 adds business event logs that reference these keys.

Output: All downstream log lines within a payment request thread carry transactionId and externalReference as top-level JSON fields with camelCase keys, queryable in Loki by those exact names.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mdc-request-lifecycle/15-RESEARCH.md

Key source files to read before implementing:
@src/main/java/com/softropic/payam/transaction/service/TransactionService.java
@src/main/java/com/softropic/payam/common/Constants.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename snake_case MDC keys to camelCase in TransactionService</name>
  <files>src/main/java/com/softropic/payam/transaction/service/TransactionService.java</files>
  <action>
In TransactionService.initiate(), update the three MDC.put() calls to use camelCase keys:

**Current (lines 39-43):**
```java
MDC.put("transaction_id", transactionId);
MDC.put("trace_id", traceId);
if (externalReference != null) {
    MDC.put("external_reference", externalReference);
}
```

**After change:**
```java
MDC.put("transactionId", transactionId);
// Note: "trace_id" is the OTel trace key set by micrometer-tracing-bridge-otel — do NOT rename it.
// The local traceId variable stored in the Transaction entity is a snapshot; the live OTel trace
// context is injected by the <mdc/> provider under "traceId" automatically.
// Only remove the MDC.put("trace_id", traceId) line — it conflicts with OTel's own "traceId" key.
if (externalReference != null) {
    MDC.put("externalReference", externalReference);
}
```

**Two changes in detail:**
1. "transaction_id" → "transactionId"
2. "external_reference" → "externalReference"
3. Remove the MDC.put("trace_id", traceId) line entirely. Reason: micrometer-tracing-bridge-otel already populates MDC with "traceId" (camelCase) for the active OTel span. The existing MDC.put("trace_id", ...) was a manual fallback that (a) uses snake_case conflicting with OTel's camelCase key, and (b) is redundant when a span is active. Removing it eliminates ambiguity.

**No other changes to this file.** The Transaction entity's traceId field (database column) is unaffected — that is persistence, not MDC.
  </action>
  <verify>
    mvn compile -q (must pass)
    grep -n "MDC.put" src/main/java/com/softropic/payam/transaction/service/TransactionService.java
    Expected output: lines with "transactionId" and "externalReference" — NO "transaction_id", "external_reference", or "trace_id".
  </verify>
  <done>
    TransactionService.java compiles. grep shows MDC.put("transactionId", ...) and the conditional MDC.put("externalReference", ...). The snake_case keys and the trace_id MDC.put are gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Constants.TXN_ID_NAME to reflect camelCase canonical key</name>
  <files>src/main/java/com/softropic/payam/common/Constants.java</files>
  <action>
In Constants.java, update TXN_ID_NAME to match the new camelCase MDC key used in TransactionService.

**Current (line 13):**
```java
public static final String TXN_ID_NAME = "txnId";
```

**After change:**
```java
public static final String TXN_ID_NAME = "transactionId";
```

Reason: TXN_ID_NAME is used by TransactionIdProvider (and potentially other consumers) as the canonical MDC key name for transaction identity. The old value "txnId" does not match what TransactionService actually sets in MDC. After Task 1 renames the MDC key to "transactionId", this constant must match. Any code reading MDC.get(TXN_ID_NAME) or calling MDC.put(TXN_ID_NAME, ...) will now use the correct key.

Before making the change, grep for all usages of TXN_ID_NAME in the codebase to confirm the change is safe:
  grep -rn "TXN_ID_NAME" src/

If any consumer reads MDC under this constant to check for existing transaction context (not to write it), they will now correctly find the "transactionId" key that TransactionService writes. This is the desired alignment.

No other changes to Constants.java.
  </action>
  <verify>
    mvn compile -q (must pass — all TXN_ID_NAME consumers compile with the new value)
    grep -n "TXN_ID_NAME" src/main/java/com/softropic/payam/common/Constants.java
    Expected: TXN_ID_NAME = "transactionId"
    grep -rn "TXN_ID_NAME" src/ — review all usages compile cleanly
  </verify>
  <done>
    Constants.java compiles. TXN_ID_NAME = "transactionId". mvn compile -q passes for the entire project (all consumers of TXN_ID_NAME work correctly with the updated value).
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. mvn compile -q — must pass with no errors
2. grep -n "transaction_id\|external_reference\|trace_id" src/main/java/com/softropic/payam/transaction/service/TransactionService.java — must return nothing (all old snake_case keys eliminated)
3. grep -n "transactionId\|externalReference" src/main/java/com/softropic/payam/transaction/service/TransactionService.java — must show both new camelCase keys in MDC.put calls
4. grep -n "TXN_ID_NAME" src/main/java/com/softropic/payam/common/Constants.java — must show "transactionId" value
5. mvn test -q — must pass (no regression from the key rename)
</verification>

<success_criteria>
LOG-MDC-02: TransactionService.initiate() sets MDC.put("transactionId", transactionId) and MDC.put("externalReference", externalReference) using camelCase keys. All log statements downstream in the same request thread (payment orchestrator, adapter calls, webhook delivery) inherit these fields as top-level JSON fields in Loki without any per-call effort.

The renamed keys are consistent with Constants.TXN_ID_NAME and the logging.md standard. No snake_case MDC keys remain for transaction identity fields.
</success_criteria>

<output>
After completion, create `.planning/phases/15-mdc-request-lifecycle/15-02-SUMMARY.md` following the template at `@~/.claude/get-shit-done/templates/summary.md`.
</output>

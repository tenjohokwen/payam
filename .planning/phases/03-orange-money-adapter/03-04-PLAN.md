---
phase: 03-orange-money-adapter
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/softropic/payam/orange/service/OrangeStatusPollerJob.java
  - src/main/java/com/softropic/payam/orange/service/OrangeMoneyPort.java
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "assertPayTokenFresh() is called in at least one production code path before using a stored payToken"
    - "PayTokenExpiredException thrown by assertPayTokenFresh() is caught and handled gracefully (no unhandled propagation)"
    - "ROADMAP SC-4 records that re-initiation orchestration on expiry is Phase 5 responsibility"
  artifacts:
    - path: "src/main/java/com/softropic/payam/orange/service/OrangeStatusPollerJob.java"
      provides: "payToken freshness guard before each poll attempt"
      contains: "assertPayTokenFresh"
    - path: "src/main/java/com/softropic/payam/orange/service/OrangeMoneyPort.java"
      provides: "Javadoc on assertPayTokenFresh() documenting Phase 5 re-init responsibility"
      contains: "Phase 5"
    - path: ".planning/ROADMAP.md"
      provides: "SC-4 classification: re-initiation on expiry is Phase 5 orchestrator responsibility"
      contains: "Phase 5"
  key_links:
    - from: "OrangeStatusPollerJob.pollTransaction()"
      to: "OrangeMoneyPort.assertPayTokenFresh()"
      via: "direct call with tx.getTransactionId() and tx.getPayTokenIssuedAt()"
      pattern: "assertPayTokenFresh"
    - from: "pollTransaction() catch block"
      to: "PayTokenExpiredException"
      via: "catch(PayTokenExpiredException e) with warn log + pollAttempts increment"
      pattern: "PayTokenExpiredException"
---

<objective>
Close Gap D: wire assertPayTokenFresh() into the production call path and classify re-initiation orchestration.

Purpose: assertPayTokenFresh() at OrangeMoneyPort line 177 detects expired payTokens but has zero
production callers. The poller (OrangeStatusPollerJob.pollTransaction) passes stored payTokens from
the Transaction record to getTransactionStatus() — this is the one location where an expired stored
payToken can occur. Wiring the freshness guard here gives the utility a meaningful production call site.
Re-initiation on expiry (fetching a fresh payToken via getMerchantInfo() + re-invoking pay()) requires
knowing the original PaymentCommand, which the adapter layer does not hold — this belongs in Phase 5
PaymentOrchestrator.

Output: OrangeStatusPollerJob guards before each poll; Javadoc on assertPayTokenFresh() documents Phase 5
handoff; ROADMAP SC-4 records the classification decision.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-orange-money-adapter/03-VERIFICATION.md
@src/main/java/com/softropic/payam/orange/service/OrangeStatusPollerJob.java
@src/main/java/com/softropic/payam/orange/service/OrangeMoneyPort.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire assertPayTokenFresh() into OrangeStatusPollerJob.pollTransaction()</name>
  <files>src/main/java/com/softropic/payam/orange/service/OrangeStatusPollerJob.java</files>
  <action>
In pollTransaction() (line 66), add the freshness check immediately after the null payToken guard
(after line 70) and before the max-attempts check. The Transaction entity already has
getPayTokenIssuedAt() (Instant) — use it directly.

Add import at the top of the file:
  import com.softropic.payam.orange.contract.exception.PayTokenExpiredException;

Insert the freshness guard block after the existing null-payToken guard (after line 70, before line 72):

    // Guard: skip poll if payToken is expired — re-initiation is Phase 5 orchestrator responsibility (P1.3)
    try {
        orangeMoneyPort.assertPayTokenFresh(tx.getTransactionId(), tx.getPayTokenIssuedAt());
    } catch (PayTokenExpiredException e) {
        log.warn("payToken expired for transaction {} — skipping poll; Phase 5 orchestrator must re-initiate",
                 tx.getTransactionId());
        tx.incrementPollAttempts();
        return;
    }

Do NOT change any other logic in the method. The existing catch (OrangeApiException e) block at line 104
remains unchanged. PayTokenExpiredException does NOT extend OrangeApiException — the new catch block must
be separate and placed before the pollAttempts check / getTransactionStatus() call.

After insertion the method flow is:
  1. Guard: payToken null? → return
  2. Guard: payToken expired? → warn + increment + return    ← NEW
  3. Check max attempts
  4. call getTransactionStatus(tx.getPayToken())
  5. apply transition or increment attempts
  </action>
  <verify>
    grep -n "assertPayTokenFresh" src/main/java/com/softropic/payam/orange/service/OrangeStatusPollerJob.java
    grep -n "PayTokenExpiredException" src/main/java/com/softropic/payam/orange/service/OrangeStatusPollerJob.java
    mvn compiler:compile -pl . -q
  </verify>
  <done>
    assertPayTokenFresh appears in OrangeStatusPollerJob.java.
    PayTokenExpiredException is caught in pollTransaction().
    mvn compiler:compile exits 0 with no errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Javadoc on assertPayTokenFresh() + ROADMAP SC-4 classification</name>
  <files>
    src/main/java/com/softropic/payam/orange/service/OrangeMoneyPort.java
    .planning/ROADMAP.md
  </files>
  <action>
**OrangeMoneyPort.java — update Javadoc at line 173 (the assertPayTokenFresh() Javadoc block):**

Replace the existing 3-line Javadoc:
  /**
   * Check if a payToken has expired (P1.3).
   * Throws PayTokenExpiredException if age exceeds config threshold.
   */

With the expanded Javadoc:
  /**
   * Check if a payToken has expired (P1.3).
   * Throws PayTokenExpiredException if age exceeds config threshold.
   *
   * Production caller: OrangeStatusPollerJob.pollTransaction() — guards before each poll attempt.
   *
   * Re-initiation responsibility: when PayTokenExpiredException is thrown, the caller should
   * log and skip. Fetching a fresh payToken (getMerchantInfo() + pay()) requires the original
   * PaymentCommand context — this is Phase 5 PaymentOrchestrator responsibility (ROADMAP SC-4).
   */

Do NOT change the method body (lines 177-184). Only the Javadoc block changes.

**ROADMAP.md — update Phase 3 SC-4 entry:**

Find the Phase 3 Success Criteria item (currently):
  4. Expired payToken triggers a fresh init (not an idempotency error — P1.3 fix)

Replace it with:
  4. Expired payToken is detected before each poll attempt — fresh re-initiation is Phase 5
     PaymentOrchestrator responsibility (P1.3; assertPayTokenFresh() wired in OrangeStatusPollerJob)

Do NOT change any other ROADMAP content.
  </action>
  <verify>
    grep -n "Phase 5" src/main/java/com/softropic/payam/orange/service/OrangeMoneyPort.java
    grep -n "OrangeStatusPollerJob" src/main/java/com/softropic/payam/orange/service/OrangeMoneyPort.java
    grep -n "PaymentOrchestrator responsibility" .planning/ROADMAP.md
    mvn compiler:compile -pl . -q
  </verify>
  <done>
    OrangeMoneyPort.java Javadoc references "Phase 5 PaymentOrchestrator responsibility" and
    "OrangeStatusPollerJob.pollTransaction()".
    ROADMAP.md SC-4 for Phase 3 references "Phase 5 PaymentOrchestrator responsibility".
    mvn compiler:compile exits 0.
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. grep -rn "assertPayTokenFresh" src/main/java/
   Expected: appears in OrangeMoneyPort.java (definition) AND OrangeStatusPollerJob.java (call site)
   assertPayTokenFresh must appear in at least 2 files.

2. grep -n "PayTokenExpiredException" src/main/java/com/softropic/payam/orange/service/OrangeStatusPollerJob.java
   Expected: import line + catch block — at least 2 matches.

3. mvn compiler:compile -pl . -q
   Expected: exits 0.

4. mvn test -pl . -Dtest=OrangeMoneyPortIT -q (if tests can run without frontend plugin)
   OR: mvn surefire:test -Dtest=OrangeMoneyPortIT
   Expected: all 8 OrangeMoneyPortIT tests still pass (no regressions).
</verification>

<success_criteria>
Gap D closed:
- assertPayTokenFresh() has exactly one production call site (OrangeStatusPollerJob.pollTransaction())
- PayTokenExpiredException is handled in the poller (log warn + increment + return — not propagated)
- Javadoc on assertPayTokenFresh() documents Phase 5 re-init responsibility
- ROADMAP SC-4 records the classification decision
- Zero regressions in OrangeMoneyPortIT (8 tests)
</success_criteria>

<output>
After completion, create `.planning/phases/03-orange-money-adapter/03-04-SUMMARY.md`
</output>

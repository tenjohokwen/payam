---
phase: 03-orange-money-adapter
plan: "03"
type: execute
wave: 1
depends_on: ["03-02"]
files_modified:
  - src/main/java/com/softropic/payam/orange/contract/OrangeWebhookPayload.java
  - src/test/java/com/softropic/payam/orange/OrangeTimeUtilTest.java
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "OrangeTimeUtil.parseOrangeTimestamp() has at least one production call site"
    - "OrangeWebhookPayload.getCreatetimeAsInstant() returns a UTC Instant parsed from WAT"
    - "A unit test asserts '2024-01-15T10:30:00' parsed via OrangeTimeUtil equals 09:30:00 UTC"
  artifacts:
    - path: "src/main/java/com/softropic/payam/orange/contract/OrangeWebhookPayload.java"
      provides: "getCreatetimeAsInstant() method calling OrangeTimeUtil.parseOrangeTimestamp()"
    - path: "src/test/java/com/softropic/payam/orange/OrangeTimeUtilTest.java"
      provides: "Unit test for WAT→UTC conversion"
  key_links:
    - from: "OrangeWebhookPayload.getCreatetimeAsInstant()"
      to: "OrangeTimeUtil.parseOrangeTimestamp()"
      via: "direct static call"
      pattern: "OrangeTimeUtil\\.parseOrangeTimestamp\\(this\\.createtime\\)"
---

<objective>
Wire the existing OrangeTimeUtil.parseOrangeTimestamp() utility into its only natural call site: OrangeWebhookPayload.getCreatetimeAsInstant(). This closes Gap B from the Phase 3 VERIFICATION.md — the WAT timestamp parser is correctly implemented but is dead code with zero production call sites.

Purpose: P5.1 fix — all Orange createtime values must be interpreted as WAT (UTC+1), not UTC. Without this wiring, any consumer of OrangeWebhookPayload.createtime that calls parseOrangeTimestamp() directly is one missed import away from defaulting to UTC, causing 1-hour drift in event log timestamps.

Output: OrangeWebhookPayload gains getCreatetimeAsInstant(), OrangeTimeUtil has one confirmed production call site, and a unit test documents the expected UTC conversion from a concrete WAT input.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-orange-money-adapter/03-VERIFICATION.md
@src/main/java/com/softropic/payam/orange/service/OrangeTimeUtil.java
@src/main/java/com/softropic/payam/orange/contract/OrangeWebhookPayload.java
</context>

<gap_classification>

## Gap A — processWebhook() state transition (must-have #1, partial)

**Classification: DEFER**

Rationale: The ROADMAP explicitly assigns inbound webhook reception, HMAC verification, and the
double-check state transition to Phase 6 (SC-1 and SC-2). The processWebhook() method in
OrangeMoneyPort is the adapter-level hook — it validates the notifToken correlation and returns
the payToken for Phase 6 to use. The state transition leg of SC-1 is fulfilled by the Quartz
poller path (OrangeStatusPollerJob → getTransactionStatus() → applyTransition()), which is
already implemented and tested. Phase 6's webhook controller will call getTransactionStatus()
for double-check before applying any state transition. The current Phase 3 design is correct.

No code change required. This deferral is already documented in the Javadoc of processWebhook().

## Gap B — WAT timestamp parsing (must-have #5, failed)

**Classification: CREATE_PLAN** — addressed by this plan (03-03).

OrangeTimeUtil.parseOrangeTimestamp() is dead code. Fix: add getCreatetimeAsInstant() to
OrangeWebhookPayload and a unit test proving the WAT→UTC conversion.

## Gap C — Cashout/C2C/IC2C UnsupportedOperationException (must-have #3, failed)

**Classification: ACCEPT_DEVIATION**

Rationale: The ROADMAP explicitly documents this as SC-3 deviation ("sandbox field verification
required"). The IT tests for cashout and C2C assert UnsupportedOperationException — this is
intentional documentation of the deviation, not a test gap. The OrangeMoneyClient already has
cashout() and c2c() HTTP methods ready for when sandbox access is confirmed. The Javadoc on
initiateCashout() and initiateC2C() records the ROADMAP deviation explicitly.

The deviation is already recorded in code. No additional code change or ROADMAP update is
required — the SC-3 deviation notation in the Javadoc is the authoritative record.

</gap_classification>

<tasks>

<task type="auto">
  <name>Task 1: Add getCreatetimeAsInstant() to OrangeWebhookPayload</name>
  <files>src/main/java/com/softropic/payam/orange/contract/OrangeWebhookPayload.java</files>
  <action>
    Add an import for com.softropic.payam.orange.service.OrangeTimeUtil at the top of the class.

    Add the following method after the existing getCreatetime() getter:

    ```java
    /**
     * Parse the Orange createtime field as a UTC Instant.
     * Orange timestamps are WAT (UTC+1, Africa/Douala) with no offset in the string — P5.1.
     * Returns null if createtime is null or blank.
     */
    public Instant getCreatetimeAsInstant() {
        if (createtime == null || createtime.isBlank()) return null;
        return OrangeTimeUtil.parseOrangeTimestamp(createtime);
    }
    ```

    Also add import java.time.Instant; if not already present.

    Do NOT modify the existing getCreatetime() method — it must remain for JSON deserialization
    and for cases where the raw String is needed. Do NOT add @JsonIgnore to getCreatetimeAsInstant()
    — it is not a Jackson-serialized field (it is derived), and Jackson will not try to deserialize
    it since there is no matching JSON field.
  </action>
  <verify>
    Compile the module without errors:
      mvn compiler:compile -pl . -am -q

    Confirm that OrangeTimeUtil.parseOrangeTimestamp is now referenced from OrangeWebhookPayload:
      grep -r "parseOrangeTimestamp" src/main/java/
    Should show at least one hit in OrangeWebhookPayload.java.
  </verify>
  <done>
    OrangeWebhookPayload.getCreatetimeAsInstant() exists and calls OrangeTimeUtil.parseOrangeTimestamp(this.createtime).
    OrangeTimeUtil.parseOrangeTimestamp() is no longer dead code — it has one confirmed production call site.
    Compilation succeeds with no errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add OrangeTimeUtilTest unit test</name>
  <files>src/test/java/com/softropic/payam/orange/OrangeTimeUtilTest.java</files>
  <action>
    Create a new plain JUnit 5 unit test class (no Spring context, no WireMock, no Testcontainers).
    Package: com.softropic.payam.orange

    The test class must cover:

    1. parseOrangeTimestamp_converts_WAT_to_UTC
       Input:  "2024-01-15T10:30:00"   (WAT = UTC+1)
       Expected output: Instant at 2024-01-15T09:30:00Z   (subtract 1 hour for WAT offset)
       Assert using assertThat(result).isEqualTo(Instant.parse("2024-01-15T09:30:00Z"))

    2. getCreatetimeAsInstant_returns_null_for_null_createtime
       Create an OrangeWebhookPayload via Jackson ObjectMapper deserializing
       {"payToken":"tok","status":"PENDING"} — no createtime field.
       Assert that payload.getCreatetimeAsInstant() returns null.

    3. getCreatetimeAsInstant_returns_correct_instant
       Create an OrangeWebhookPayload via Jackson ObjectMapper deserializing
       {"createtime":"2024-01-15T10:30:00"}
       Assert that payload.getCreatetimeAsInstant() equals Instant.parse("2024-01-15T09:30:00Z")

    Import: use assertj (assertThat), JUnit 5 @Test, and Jackson ObjectMapper for DTO construction.
    Do NOT use Mockito — this is pure logic, no mocking needed.

    Test class naming convention: match existing test classes in the project (OrangeMoneyPortIT.java
    uses class-level name without @DisplayName — follow same style).
  </action>
  <verify>
    Run only this test class (bypassing frontend plugin lifecycle):
      mvn surefire:test -Dtest=OrangeTimeUtilTest -pl . -am

    All 3 tests must pass. No Spring context startup required — these are unit tests.
  </verify>
  <done>
    OrangeTimeUtilTest exists with 3 passing tests.
    Test 1 confirms "2024-01-15T10:30:00" → 2024-01-15T09:30:00Z (WAT→UTC 1-hour subtraction).
    Test 2 confirms null createtime → null Instant (no NullPointerException).
    Test 3 confirms OrangeWebhookPayload.getCreatetimeAsInstant() produces the correct UTC Instant.
    mvn surefire:test -Dtest=OrangeTimeUtilTest passes cleanly.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. grep -r "parseOrangeTimestamp" src/main/java/ — must show hit in OrangeWebhookPayload.java
2. mvn compiler:compile -pl . -am -q — must succeed with no errors
3. mvn surefire:test -Dtest=OrangeTimeUtilTest -pl . -am — all 3 tests green
4. Confirm existing IT suite still passes (optional smoke check):
   mvn surefire:test -Dtest=OrangeMoneyPortIT -pl . -am
</verification>

<success_criteria>
- OrangeTimeUtil.parseOrangeTimestamp() has exactly one production call site: OrangeWebhookPayload.getCreatetimeAsInstant()
- OrangeWebhookPayload.getCreatetimeAsInstant() returns null for null/blank input, and correct UTC Instant for valid WAT input
- Unit test asserts '2024-01-15T10:30:00' (WAT) equals Instant.parse('2024-01-15T09:30:00Z') (UTC)
- No existing tests broken (OrangeMoneyPortIT, OrangeTokenServiceIT)
- Phase 3 VERIFICATION.md Gap B (must-have #5) is now addressable — WAT parsing is wired, not dead code
</success_criteria>

<output>
After completion, create `.planning/phases/03-orange-money-adapter/03-03-SUMMARY.md` with the
standard summary frontmatter (phase, plan, type, status, files_modified, decisions, key_links).
</output>

---
phase: 12-test-doc-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/java/com/softropic/payam/fraud/FraudEngineIT.java
  - src/main/java/com/softropic/payam/payment/api/PaymentResource.java
autonomous: true

must_haves:
  truths:
    - "A payment submitted with a non-null deviceFingerprint results in device_fingerprint column populated in main.transaction"
    - "PaymentResource Javadoc documents FRAUD_BLOCKED as a 422 Unprocessable Entity case"
    - "All existing tests continue to pass"
  artifacts:
    - path: "src/test/java/com/softropic/payam/fraud/FraudEngineIT.java"
      provides: "IT test asserting device_fingerprint DB column is non-null when deviceFingerprint is supplied"
      contains: "deviceFingerprint.*device_fingerprint"
    - path: "src/main/java/com/softropic/payam/payment/api/PaymentResource.java"
      provides: "Javadoc listing FRAUD_BLOCKED as a 422 case"
      contains: "FRAUD_BLOCKED"
  key_links:
    - from: "PaymentRequest.deviceFingerprint"
      to: "main.transaction.device_fingerprint"
      via: "PaymentOrchestrator sets Transaction.deviceFingerprint via setDeviceFingerprint()"
      pattern: "device_fingerprint"
---

<objective>
Close two minor tech-debt items from the v1 audit: a missing IT test path for device fingerprint
persistence, and an incomplete Javadoc entry on PaymentResource.

Purpose: Both gaps were flagged in the v1 milestone audit. The device fingerprint column exists and
is wired through the stack — it just lacks an integration test proving the column is populated. The
Javadoc omission causes FRAUD_BLOCKED to be undocumented in the HTTP status mapping table.

Output: One new test method in FraudEngineIT asserting device_fingerprint is persisted; updated
PaymentResource Javadoc listing FRAUD_BLOCKED under 422.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/test/java/com/softropic/payam/fraud/FraudEngineIT.java
@src/main/java/com/softropic/payam/payment/api/PaymentResource.java
@src/main/java/com/softropic/payam/payment/contract/PaymentRequest.java
@src/main/java/com/softropic/payam/transaction/repo/Transaction.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deviceFingerprint IT assertion in FraudEngineIT</name>
  <files>src/test/java/com/softropic/payam/fraud/FraudEngineIT.java</files>
  <action>
    Add a new test method `deviceFingerprintIsPersistedInDb` after the existing `normalPaymentHasRiskScoreInDb` test.

    The test must:
    1. Stub MTN endpoints (same as `normalPaymentHasRiskScoreInDb` — GET basicuserinfo returns `{}`, POST requesttopay returns 202).
    2. Build a payment request JSON that includes `"deviceFingerprint":"fp-test-abc123"` in the body. Use a distinct MSISDN (e.g. `+237671000005`) that is not shared with other tests to avoid cross-test velocity state.
    3. Use `headersWithKey()` and `restTemplate.exchange(...)` to POST to `/v1/payments` (same pattern as `postPayment` helper — you may extend `buildMtnRequest` or inline the JSON; do NOT modify the signature of `buildMtnRequest` since it is called by existing tests).
    4. Assert the response status is 202.
    5. Query the DB:
       ```sql
       SELECT device_fingerprint FROM main.transaction WHERE tenant_id = ? ORDER BY created_date DESC LIMIT 1
       ```
       using `jdbc.queryForObject(...)` with `String.class` and `tenantId`.
    6. Assert the returned value equals `"fp-test-abc123"` (not null, exact match).

    Add a private helper `buildMtnRequestWithFingerprint(String msisdn, String idempotencyKey, String fingerprint)` that returns the JSON string with `deviceFingerprint` included. Call this helper from the new test.

    No changes to existing test methods or the existing `buildMtnRequest` helper.

    Add the Javadoc comment above the new test method:
    ```
    /**
     * Device fingerprint persistence — asserts that a payment submitted with a non-null
     * deviceFingerprint is stored in the device_fingerprint column of main.transaction (FRAUD-01).
     */
    ```
  </action>
  <verify>
    Run: `./mvnw -pl . -Dtest=FraudEngineIT#deviceFingerprintIsPersistedInDb -DfailIfNoTests=false test`
    The new test must pass. Then run all FraudEngineIT tests:
    `./mvnw -pl . -Dtest=FraudEngineIT -DfailIfNoTests=false test`
    All 3 tests must pass.
  </verify>
  <done>
    `FraudEngineIT` has 3 passing tests. The new `deviceFingerprintIsPersistedInDb` test submits
    `deviceFingerprint=fp-test-abc123` and asserts the `device_fingerprint` column equals that value.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add FRAUD_BLOCKED to PaymentResource Javadoc</name>
  <files>src/main/java/com/softropic/payam/payment/api/PaymentResource.java</files>
  <action>
    In `PaymentResource.java`, update the class-level Javadoc HTTP status mapping list (lines 25–30).

    Current line 28:
    ```
     *   <li>422 Unprocessable Entity — SUBSCRIBER_INACTIVE or UNKNOWN_MSISDN_PREFIX</li>
    ```

    Replace with:
    ```
     *   <li>422 Unprocessable Entity — SUBSCRIBER_INACTIVE, UNKNOWN_MSISDN_PREFIX, or FRAUD_BLOCKED</li>
    ```

    No other changes to the file. The `resolveHttpStatus` method comment on line 78 also mentions only
    `SUBSCRIBER_INACTIVE, UNKNOWN_MSISDN_PREFIX` — update it to include `FRAUD_BLOCKED` as well:

    Current line 78:
    ```
        // SUBSCRIBER_INACTIVE, UNKNOWN_MSISDN_PREFIX, and other domain errors → 422
    ```

    Replace with:
    ```
        // SUBSCRIBER_INACTIVE, UNKNOWN_MSISDN_PREFIX, FRAUD_BLOCKED, and other domain errors → 422
    ```
  </action>
  <verify>
    Run: `./mvnw -pl . compile`
    Compilation must succeed with no errors. Confirm the word FRAUD_BLOCKED appears in the file:
    `grep -n FRAUD_BLOCKED src/main/java/com/softropic/payam/payment/api/PaymentResource.java`
    Must return at least one match in the class-level Javadoc.
  </verify>
  <done>
    `PaymentResource.java` Javadoc line documenting 422 cases lists FRAUD_BLOCKED alongside
    SUBSCRIBER_INACTIVE and UNKNOWN_MSISDN_PREFIX. Project compiles cleanly.
  </done>
</task>

</tasks>

<verification>
Run the full test suite to confirm nothing regressed:

```
./mvnw test
```

All tests must pass. Spot-check:
- `FraudEngineIT` — 3 tests green (velocityBlockReturns422, normalPaymentHasRiskScoreInDb, deviceFingerprintIsPersistedInDb)
- `PaymentResource.java` — contains `FRAUD_BLOCKED` in Javadoc

Grep confirmation:
```
grep -n "FRAUD_BLOCKED\|device_fingerprint" \
  src/main/java/com/softropic/payam/payment/api/PaymentResource.java \
  src/test/java/com/softropic/payam/fraud/FraudEngineIT.java
```
</verification>

<success_criteria>
1. `FraudEngineIT` has a new test `deviceFingerprintIsPersistedInDb` that submits a payment with
   `deviceFingerprint=fp-test-abc123` and asserts the `device_fingerprint` column in `main.transaction`
   equals that value — verified by querying the DB via `JdbcTemplate`.
2. `PaymentResource` class-level Javadoc 422 entry explicitly names `FRAUD_BLOCKED` alongside
   `SUBSCRIBER_INACTIVE` and `UNKNOWN_MSISDN_PREFIX`.
3. All existing tests pass (`./mvnw test` green).
</success_criteria>

<output>
After completion, create `.planning/phases/12-test-doc-polish/12-01-SUMMARY.md` following the
template at `~/.claude/get-shit-done/templates/summary.md`.
</output>

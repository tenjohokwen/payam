---
phase: 58-integration-e2e-test-suite
plan: 04
type: execute
wave: 3
depends_on:
  - "58-01"
  - "58-02"
  - "58-03"
files_modified:
  - src/test/java/com/softropic/payam/e2e/disbursement/MtnDisbursementE2EIT.java
  - src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java
  - src/test/java/com/softropic/payam/e2e/disbursement/DisbursementAdminApprovalE2EIT.java
  - src/test/java/com/softropic/payam/e2e/disbursement/DisbursementExpiryE2EIT.java
  - src/test/java/com/softropic/payam/e2e/disbursement/DisbursementFraudBlockE2EIT.java
  - src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java
  - src/test/java/com/softropic/payam/disbursement/api/DisbursementResourceIT.java
autonomous: true
requirements:
  - SC-5
must_haves:
  truths:
    - "`mvn verify -q` exits 0 from a clean repo state — all v11 requirements satisfied (Phase 58 SC-5)"
    - "Any compilation failures introduced by 58-01/02/03 are fixed (e.g. missing imports, unused imports, helper-method ordering)"
    - "Any pre-existing @Disabled annotations on tests that now exercise the v11 claim machinery (and would pass) are evaluated and either removed (with verification) or kept with a documented reason"
    - "The e2e/disbursement package compiles cleanly: `mvn test-compile -q` exits 0"
    - "Full test suite passes: `mvn verify -q` exits 0 with NO test failures, NO compilation errors, NO surefire/failsafe report errors"
    - "If any single test fails after 58-01/02/03, the failure is diagnosed as either (a) a regression caused by the new assertions (fix the assertion) or (b) a pre-existing unrelated flake (document and skip via -Dit.test=!UnrelatedFlake with justification in SUMMARY)"
    - "No production code is modified — Phase 58 is pure test work; only test files may be touched in this plan"
    - "Final `mvn verify` log is captured into the plan SUMMARY for evidence"
  artifacts:
    - path: "src/test/java/com/softropic/payam/e2e/disbursement/MtnDisbursementE2EIT.java"
      provides: "Compiles cleanly post 58-01 + 58-04 cleanup"
      contains: "TRANSACTION_CLAIMED"
    - path: "src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java"
      provides: "Compiles cleanly post 58-02 + 58-04 cleanup"
      contains: "RELEASED"
    - path: "src/test/java/com/softropic/payam/e2e/disbursement/DisbursementAdminApprovalE2EIT.java"
      provides: "Compiles cleanly post 58-03 + 58-04 cleanup"
      contains: "PENDING_ADMIN_APPROVAL"
  key_links:
    - from: "All Phase 58 test classes"
      to: "mvn verify (full suite)"
      via: "Maven Surefire + Failsafe runners"
      pattern: "BUILD SUCCESS"
---

<objective>
Final cleanup and verification plan for Phase 58. After plans 58-01, 58-02, and 58-03 add new test methods and a new test class, this plan runs the full `mvn verify` suite, fixes any compilation issues introduced by the new test code, evaluates whether stale `@Disabled` annotations can be removed (specifically `OrangeDisbursementE2EIT.insufficientBalance_returns422_andOrangeCashoutNotCalled`, which is disabled per SCHEMA-03 wallet retirement and may now be removable), and confirms `mvn verify` exits 0 — Phase 58 SC-5.

Purpose: Plans 58-01/02/03 are scoped to specific test classes and may leave compilation artifacts (unused imports, missing imports, helper-method placement issues) when concatenated. A green `mvn verify` is the explicit Phase 58 success criterion (SC-5: "mvn verify passes cleanly — all v11 requirements satisfied"). This plan is the gate that proves the milestone-level requirement.

Output: A green `mvn verify -q` run, captured in the plan SUMMARY. Any test-file cleanup needed (imports, formatting, stale annotations) is applied in-place. No production code is modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-integration-e2e-test-suite/58-RESEARCH.md
@.planning/phases/58-integration-e2e-test-suite/58-01-PLAN.md
@.planning/phases/58-integration-e2e-test-suite/58-02-PLAN.md
@.planning/phases/58-integration-e2e-test-suite/58-03-PLAN.md

<interfaces>
<!-- This plan does NOT introduce new code interfaces. It exercises the full Maven build. -->

Maven verify command (Surefire + Failsafe + integration tests):
```bash
mvn verify -q
```
Expected exit code: 0
Expected log line: `BUILD SUCCESS`

Targeted single-class run (for diagnosing a specific failure):
```bash
mvn verify -Dit.test={ClassName} -DfailIfNoTests=false -q
```

Compile-only check (faster than full verify when fixing imports):
```bash
mvn test-compile -q
```

Stale @Disabled candidate to evaluate (existing — predates Phase 58):
- src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java
  → `insufficientBalance_returns422_andOrangeCashoutNotCalled` (was disabled per SCHEMA-03 wallet retirement;
    in v11 the equivalent guard is TRANSACTION_CLAIMED — covered by 58-01 Task 2 on the MTN side and
    58-02 Task 2 RELEASED-reuse on the Orange side. Re-enabling this disabled test is OUT OF SCOPE
    because it asserts wallet semantics that no longer exist; KEEP @Disabled with the existing reason.)

Files that may need import cleanup after 58-01/02/03 (only if compilation reports unused/missing imports):
- src/test/java/com/softropic/payam/e2e/disbursement/MtnDisbursementE2EIT.java (added DisbursementOrchestratorError import in 58-01)
- src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java (no new imports if Awaitility + Duration + DisbursementStatus already present — verify)
- src/test/java/com/softropic/payam/e2e/disbursement/DisbursementAdminApprovalE2EIT.java (brand new — full import block per 58-03)

NOTE on scope: This plan ONLY modifies test files if `mvn verify` fails. If `mvn verify` passes on the first run after 58-01/02/03, this plan is a no-op modification-wise (only the SUMMARY is produced).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run mvn verify, diagnose any failures, apply minimal test-file fixes</name>

  <files>
    - src/test/java/com/softropic/payam/e2e/disbursement/MtnDisbursementE2EIT.java (only if needed)
    - src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java (only if needed)
    - src/test/java/com/softropic/payam/e2e/disbursement/DisbursementAdminApprovalE2EIT.java (only if needed)
    - src/test/java/com/softropic/payam/e2e/disbursement/DisbursementExpiryE2EIT.java (only if needed — pre-existing modifications visible in git status)
    - src/test/java/com/softropic/payam/e2e/disbursement/DisbursementFraudBlockE2EIT.java (only if needed — pre-existing modifications visible in git status)
    - src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java (only if needed — pre-existing modifications visible in git status)
    - src/test/java/com/softropic/payam/disbursement/api/DisbursementResourceIT.java (only if needed — pre-existing modifications visible in git status)
  </files>

  <read_first>
    - src/test/java/com/softropic/payam/e2e/disbursement/MtnDisbursementE2EIT.java (post-58-01 state — confirm import block, helper method placement, @Test count)
    - src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java (post-58-02 state — confirm @Test count, @Disabled still present, helper placement)
    - src/test/java/com/softropic/payam/e2e/disbursement/DisbursementAdminApprovalE2EIT.java (post-58-03 — verify class compiles standalone)
    - .planning/phases/58-integration-e2e-test-suite/58-RESEARCH.md (Validation Architecture section: phase gate is `mvn verify`)
    - git status (to enumerate pre-existing modified test files that may need attention as part of the verify gate)
  </read_first>

  <action>
    STEP A — RUN `mvn verify -q` from the project root.

    ```bash
    mvn verify -q 2>&1 | tee /tmp/phase58-verify.log
    echo "EXIT_CODE=$?"
    ```

    STEP B — INTERPRET THE RESULT.

    Case 1: `BUILD SUCCESS` and EXIT_CODE=0 → All tests pass. Skip to STEP D (Documentation). Do NOT edit any files.

    Case 2: COMPILATION FAILURE (e.g. `cannot find symbol`, `package does not exist`) →
      - Read the failing file at the line number reported by javac
      - Identify the missing or unused import
      - Apply the minimal fix:
        - Missing `import com.softropic.payam.disbursement.contract.DisbursementOrchestratorError;` → add to the import block (alphabetical position after other com.softropic.payam.disbursement.* imports)
        - Missing `import java.util.List;` or `import java.time.Duration;` → add (most files already have these)
        - Missing `import static org.awaitility.Awaitility.await;` → add to static imports section
        - Unused import → REMOVE only if javac/SpotBugs/checkstyle warnings are configured to fail the build
      - Re-run `mvn test-compile -q` to confirm the fix; then re-run `mvn verify -q`

    Case 3: TEST FAILURE in a NEWLY-ADDED test (from 58-01, 58-02, or 58-03) →
      - Read the surefire-reports XML or failsafe-reports XML for the failing test
      - Compare actual vs. expected: does the failure indicate the v11 claim contract is broken (real bug) or that the test assertion is wrong (test bug)?
      - If test assertion is wrong: fix the assertion in the test file. Common cases:
        - Race condition: replace `assertClaimStatuses(..., "CLAIMED", N)` with `awaitClaimStatuses(..., "CLAIMED", N)` (Pitfall 3 from 58-RESEARCH)
        - Wrong expected count: align with how many txnIds the test seeded
        - Same idempotency key on second POST: regenerate with UUID.randomUUID() (Pitfall 4)
      - If a real bug in production code surfaces: STOP. Do NOT modify production code in this plan. Document the failure in the SUMMARY and escalate — Phase 58 is test-only.

    Case 4: TEST FAILURE in a PRE-EXISTING test (untouched by 58-01/02/03) →
      - Check git log for the file: `git log --oneline -5 -- src/test/java/com/softropic/payam/e2e/disbursement/{ClassName}.java`
      - Check git status to see if the file appeared in the initial git status block (DisbursementExpiryE2EIT, DisbursementFraudBlockE2EIT, MtnDisbursementE2EIT, OrangeDisbursementE2EIT, StepUpConfirmationE2EIT, DisbursementResourceIT all show modifications). If the modification predates Phase 58 and the failure is unrelated, the plan executor should:
        a. Read the modified file to understand the change
        b. If the change introduced the failure and is unrelated to v11 claim work, REVERT the change (`git checkout HEAD -- {file}`) and re-run mvn verify
        c. If the change IS Phase 58 work in progress that should remain, fix the test
      - If the failure is unrelated and unfixable in this plan, document with a precise reproducer in the SUMMARY and add `-Dit.test=!{ClassName}` to the mvn verify gate command, with explicit justification (e.g., "pre-existing flake X tracked in PROJECT.md, not introduced by Phase 58")

    STEP C — RE-RUN UNTIL GREEN.

    ```bash
    mvn verify -q 2>&1 | tee /tmp/phase58-verify.log
    echo "EXIT_CODE=$?"
    ```

    Repeat STEP B until EXIT_CODE=0 and `BUILD SUCCESS` appears.

    STEP D — EVALUATE THE STALE @Disabled (DOCUMENTATION-ONLY decision).

    Inspect `OrangeDisbursementE2EIT.insufficientBalance_returns422_andOrangeCashoutNotCalled`:
    - The test was disabled per SCHEMA-03 wallet retirement (Phase 54)
    - The v11 equivalent guard is TRANSACTION_CLAIMED (covered by 58-01 Task 2 on the MTN side and the FAILED-callback + reuse path on the Orange side via 58-02 Task 2)
    - DECISION: KEEP @Disabled with the existing reason. The test asserts wallet semantics that no longer exist in v11. Re-enabling it would require rewriting the test body to assert TRANSACTION_CLAIMED, which is already covered by 58-01 Task 2. There is NO net coverage gain from re-enabling.
    - Action: Do NOT remove the @Disabled annotation. Document this decision in the SUMMARY.

    STEP E — VERIFY NO PRODUCTION CODE IS MODIFIED.

    ```bash
    git diff --name-only src/main/java/ | grep . && echo "VIOLATION: production code modified" && exit 1
    git diff --name-only src/main/java/ | wc -l    # MUST be 0
    ```

    STEP F — RECORD THE FINAL STATE.

    Capture the BUILD SUCCESS log tail (last ~30 lines of /tmp/phase58-verify.log) into the plan SUMMARY (created in <output>). Include:
    - mvn verify exit code
    - Surefire/Failsafe test counts (Tests run: N, Failures: 0, Errors: 0, Skipped: K)
    - Total time
    - List of files modified by this plan (likely empty, unless STEP B applied a fix)
    - Decision rationale for the @Disabled (kept; reason carried forward from SCHEMA-03)
  </action>

  <verify>
    <automated>mvn verify -q && git diff --name-only src/main/java/ | wc -l | grep -q '^0$'</automated>
  </verify>

  <acceptance_criteria>
    - `mvn verify -q` exits 0 from the project root (BUILD SUCCESS in the log)
    - `git diff --name-only src/main/java/` returns nothing (0 lines) — NO production code modified by Phase 58 overall
    - The Surefire report shows zero test failures and zero errors (`Tests run: N, Failures: 0, Errors: 0, Skipped: K` in the summary line)
    - The Failsafe report shows zero IT failures and zero errors
    - DisbursementAdminApprovalE2EIT runs both its tests green (`grep -A2 "DisbursementAdminApprovalE2EIT" target/failsafe-reports/*.txt` shows both methods passed)
    - MtnDisbursementE2EIT runs all 4 tests green (3 pre-existing + 1 added in 58-01 Task 2)
    - OrangeDisbursementE2EIT runs 4 @Test methods (3 active + 1 @Disabled stays disabled — `grep -c "@Disabled" src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java` returns 1)
    - The Phase 58 SUMMARY includes the literal mvn verify exit code and the Surefire/Failsafe test totals
    - The Phase 58 SUMMARY documents the explicit decision to keep `insufficientBalance_returns422_andOrangeCashoutNotCalled` @Disabled (with rationale)
    - No new files created in src/main/java/ (`git status src/main/java/` is clean)
  </acceptance_criteria>

  <done>
    `mvn verify -q` exits 0 with all tests green. Phase 58 SC-5 ("mvn verify passes cleanly — all v11 requirements satisfied") is met. No production code is modified by Phase 58 in aggregate. The single pre-existing @Disabled test is documented as intentionally retained.
  </done>
</task>

</tasks>

<verification>
The phase gate is the verify command itself: `mvn verify -q` exits 0. The Surefire and Failsafe report XMLs in target/ contain zero `<failure>` and zero `<error>` elements.
</verification>

<success_criteria>
- `mvn verify -q` exits 0
- All 4 plans of Phase 58 (58-01, 58-02, 58-03, 58-04) committed
- No production code modified by Phase 58 in aggregate (`git diff --stat main..HEAD -- src/main/java/` is empty for the Phase 58 commit range)
- Phase 58 SC-5 satisfied: full v11 milestone is machine-verified green
- The @Disabled test in OrangeDisbursementE2EIT remains disabled with a documented rationale
</success_criteria>

<output>
After completion, create `.planning/phases/58-integration-e2e-test-suite/58-04-SUMMARY.md` containing:
1. The mvn verify exit code and BUILD SUCCESS confirmation
2. The Surefire/Failsafe test totals (Tests run, Failures, Errors, Skipped)
3. List of files modified by this plan (likely empty)
4. Documented rationale for keeping `OrangeDisbursementE2EIT.insufficientBalance_returns422_andOrangeCashoutNotCalled` @Disabled
5. Phase 58 closure statement: SC-5 met; v11 milestone machine-verified
</output>

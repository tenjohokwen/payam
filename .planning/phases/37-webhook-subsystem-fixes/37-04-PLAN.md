---
phase: 37-webhook-subsystem-fixes
plan: 04
type: execute
wave: 3
depends_on:
  - 37-01
  - 37-02
  - 37-03
files_modified:
  - .planning/phases/37-webhook-subsystem-fixes/37-04-SUMMARY.md
autonomous: true
requirements:
  - WEBHOOK-01
  - WEBHOOK-02
  - WEBHOOK-03
must_haves:
  truths:
    - "`mvn verify -q` exits 0 on the current HEAD after plans 37-01, 37-02, 37-03 are merged"
    - "WebhookDeliveryIT, WebhookDeliveryJobIT, WebhookEnqueueListenerIT, WebhookConfigTest all pass in the aggregate run"
    - "No existing test class newly fails compared to the pre-phase baseline (phase 36-02 sign-off state)"
    - "37-04-SUMMARY.md records: full mvn verify result, the three new webhook tests' individual status, and any pre-existing surefire Docker-context errors carried from Phase 36 (documented, not regressed)"
  artifacts:
    - path: ".planning/phases/37-webhook-subsystem-fixes/37-04-SUMMARY.md"
      provides: "Phase 37 regression sign-off record"
      contains: "mvn verify"
  key_links: []
---

<objective>
Full-suite regression sign-off for Phase 37.

Plans 37-01, 37-02, 37-03 each ship new tests that exercise the fix they introduce. This plan
runs the complete `mvn verify` suite to confirm no cross-module regression and records the
result as the phase's sign-off artifact — matching the pattern established in 35-02 and 36-02.

Purpose: green phase gate before /gsd:verify-work, and an audit trail of the aggregate run.

Output: 37-04-SUMMARY.md summarising the full-suite result plus the status of the three new
webhook tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-webhook-subsystem-fixes/37-RESEARCH.md
@.planning/phases/37-webhook-subsystem-fixes/37-VALIDATION.md
@.planning/phases/37-webhook-subsystem-fixes/37-01-PLAN.md
@.planning/phases/37-webhook-subsystem-fixes/37-02-PLAN.md
@.planning/phases/37-webhook-subsystem-fixes/37-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full mvn verify and produce 37-04-SUMMARY.md</name>
  <files>.planning/phases/37-webhook-subsystem-fixes/37-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/37-webhook-subsystem-fixes/37-01-PLAN.md
    - .planning/phases/37-webhook-subsystem-fixes/37-02-PLAN.md
    - .planning/phases/37-webhook-subsystem-fixes/37-03-PLAN.md
    - .planning/STATE.md
  </read_first>
  <action>
1. Run the full Maven verify suite from the project root:
```bash
mvn verify -q
```
   Capture the final BUILD SUCCESS / BUILD FAILURE line and the exit code. If the build fails,
   capture the failing test class(es) from the Failsafe/Surefire summary and STOP — do not write
   the summary as green. Instead, append a "Failures" section describing the failure and return
   to the executor for diagnosis.

2. If the build is green, also run the three new webhook tests individually to capture per-test
   status for the summary:
```bash
mvn test -pl . -Dtest=WebhookConfigTest -Dsurefire.failIfNoSpecifiedTests=false -q
mvn verify -pl . -Dit.test=WebhookDeliveryJobIT -Dsurefire.failIfNoSpecifiedTests=false -Dfailsafe.failIfNoSpecifiedTests=false -q
mvn verify -pl . -Dit.test=WebhookEnqueueListenerIT -Dsurefire.failIfNoSpecifiedTests=false -Dfailsafe.failIfNoSpecifiedTests=false -q
mvn verify -pl . -Dit.test=WebhookDeliveryIT -Dsurefire.failIfNoSpecifiedTests=false -Dfailsafe.failIfNoSpecifiedTests=false -q
```

3. Cross-check against the STATE.md note for Phase 36:
   "Surefire Docker-context errors in SecurityFilterChainIT and TenantAdminResourceIT are
    pre-existing, not Phase 36 regressions — same classes pass in failsafe runner; Maven exit 0
    confirms build success"
   If the same pre-existing errors appear, document them as carried-over, NOT as Phase 37
   regressions. If any NEW test class fails, that is a blocker — STOP and report.

4. Write .planning/phases/37-webhook-subsystem-fixes/37-04-SUMMARY.md with this structure:

```markdown
  ---
phase: 37-webhook-subsystem-fixes
plan: 04
completed: YYYY-MM-DD
status: success
  ---

# Phase 37 — 04 Regression Sign-Off

## Full Suite Result
- Command: `mvn verify -q`
- Exit code: 0
- Build line: BUILD SUCCESS

## Phase 37 New Tests
| Test | Plan | Result |
|------|------|--------|
| WebhookConfigTest | 37-03 | PASS |
| WebhookDeliveryJobIT | 37-01 | PASS |
| WebhookEnqueueListenerIT | 37-02 | PASS |
| WebhookDeliveryIT (existing) | — | PASS (no regression) |

## Requirements Closed
- WEBHOOK-01 — N+1 tenant query eliminated via loadTenants bulk fetch (plan 37-01)
- WEBHOOK-02 — Enqueue decoupled from state-transition transaction via AFTER_COMMIT listener (plan 37-02)
- WEBHOOK-03 — RestTemplate has 5s connect / 10s read timeouts (plan 37-03)

## Carried-Over Known Issues
{list any pre-existing Surefire Docker-context errors from Phase 36 — do NOT list if none.}

## Next Steps
Phase 37 is ready for /gsd:verify-work.
```
  </action>
  <verify>
    <automated>mvn verify -q</automated>
  </verify>
  <acceptance_criteria>
    - `mvn verify -q` exits 0 (BUILD SUCCESS)
    - File .planning/phases/37-webhook-subsystem-fixes/37-04-SUMMARY.md exists
    - grep -c "BUILD SUCCESS" .planning/phases/37-webhook-subsystem-fixes/37-04-SUMMARY.md returns 1
    - grep -c "WebhookConfigTest" .planning/phases/37-webhook-subsystem-fixes/37-04-SUMMARY.md returns 1
    - grep -c "WebhookDeliveryJobIT" .planning/phases/37-webhook-subsystem-fixes/37-04-SUMMARY.md returns 1
    - grep -c "WebhookEnqueueListenerIT" .planning/phases/37-webhook-subsystem-fixes/37-04-SUMMARY.md returns 1
    - grep -c "WEBHOOK-01" .planning/phases/37-webhook-subsystem-fixes/37-04-SUMMARY.md returns ≥1
    - grep -c "WEBHOOK-02" .planning/phases/37-webhook-subsystem-fixes/37-04-SUMMARY.md returns ≥1
    - grep -c "WEBHOOK-03" .planning/phases/37-webhook-subsystem-fixes/37-04-SUMMARY.md returns ≥1
  </acceptance_criteria>
  <done>
    Full mvn verify passes, 37-04-SUMMARY.md records per-test status and requirement closure,
    phase 37 is ready for /gsd:verify-work.
  </done>
</task>

</tasks>

<verification>
- mvn verify -q exits 0 on HEAD with all three prior plans merged
</verification>

<success_criteria>
- Full suite green
- Per-test results recorded in 37-04-SUMMARY.md
- WEBHOOK-01/02/03 documented as closed
</success_criteria>

<output>
37-04-SUMMARY.md is the output of this plan.
</output>

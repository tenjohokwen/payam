---
phase: 61-infrastructure-layer-creation
plan: 03
type: execute
wave: 3
depends_on: ["61-02"]
files_modified:
  - src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java
  - src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java
  - src/main/java/com/softropic/payam/infrastructure/web/LoggingFilter.java
  - src/main/java/com/softropic/payam/security/config/SecurityConfiguration.java
autonomous: true
requirements: [INFRA-02, BUILD-01, BUILD-02, BUILD-03]
must_haves:
  truths:
    - "ApiKeyAuthenticationFilter, TenantSecurityConfig, and LoggingFilter live in package com.softropic.payam.infrastructure.web"
    - "FilterRegistrationBean(setEnabled=false) for ApiKeyAuthenticationFilter is preserved exactly — the filter only runs inside the @Order(1) tenantApiKeyFilterChain, never as a global servlet filter"
    - "Zero Java files contain `import com.softropic.payam.tenant.config.ApiKeyAuthenticationFilter`, `com.softropic.payam.tenant.config.TenantSecurityConfig`, or `com.softropic.payam.security.audit.filter.LoggingFilter`"
    - "RotatedKeyCleanupSchedulerConfig stays in tenant.config (it is a Quartz scheduler, not web infrastructure)"
    - "TenantFilterChainIT and SecurityFilterChainIT both pass — proves the filter chain still routes /v1/admin/** and /v1/account/** to the JWT chain (not the API-key chain)"
    - "mvn verify exits 0"
  artifacts:
    - path: "src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java"
      provides: "OncePerRequestFilter that authenticates X-Api-Key header against ApiKeyService and populates TenantContext + SecurityContext"
      contains: "extends OncePerRequestFilter"
    - path: "src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java"
      provides: "@Configuration providing the @Order(1) tenantApiKeyFilterChain SecurityFilterChain bean and the FilterRegistrationBean(setEnabled=false) registration that suppresses servlet auto-registration"
      contains: "registration.setEnabled(false)"
    - path: "src/main/java/com/softropic/payam/infrastructure/web/LoggingFilter.java"
      provides: "OncePerRequestFilter that emits request_start / request_end / request_error structured log events with requestId in MDC"
      contains: "extends OncePerRequestFilter"
  key_links:
    - from: "security.config.SecurityConfiguration"
      to: "infrastructure.web.LoggingFilter"
      via: "import + addFilterBefore(new LoggingFilter(PUBLIC_STATIC_RESOURCES), ...)"
      pattern: "import com\\.softropic\\.payam\\.infrastructure\\.web\\.LoggingFilter"
    - from: "infrastructure.web.TenantSecurityConfig (FilterRegistrationBean)"
      to: "infrastructure.web.ApiKeyAuthenticationFilter"
      via: "@Bean apiKeyAuthenticationFilter + FilterRegistrationBean.setEnabled(false)"
      pattern: "registration\\.setEnabled\\(false\\)"
    - from: "infrastructure.web.TenantSecurityConfig (tenantApiKeyFilterChain)"
      to: "infrastructure.web.ApiKeyAuthenticationFilter"
      via: ".addFilterBefore(apiKeyFilter, UsernamePasswordAuthenticationFilter.class) on @Order(1) chain scoped to /v1/** AND NOT /v1/account/** AND NOT /v1/admin/**"
      pattern: "addFilterBefore\\(apiKeyFilter,"
---

<objective>
Move three web-layer infrastructure classes into a new `com.softropic.payam.infrastructure.web` package: `ApiKeyAuthenticationFilter` and `TenantSecurityConfig` (currently in `tenant.config`) and `LoggingFilter` (currently in `security.audit.filter`). Update the single import in `SecurityConfiguration` and run `mvn verify`.

Purpose: Completes requirement INFRA-02 by consolidating Spring filters and web-layer infrastructure under `infrastructure.web`. This is the final wave of Phase 61 — runs last because it is medium-risk (filter chain semantics) and depends on the JPA persistence (Plan 01) and config (Plan 02) moves already being green.

Output: 3 source files moved, 1 import statement updated in `SecurityConfiguration.java`, the `FilterRegistrationBean(setEnabled=false)` pattern preserved exactly, `mvn verify` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-infrastructure-layer-creation/61-RESEARCH.md
@.planning/phases/61-infrastructure-layer-creation/61-VALIDATION.md
@.planning/phases/61-infrastructure-layer-creation/61-01-SUMMARY.md
@.planning/phases/61-infrastructure-layer-creation/61-02-SUMMARY.md

<interfaces>
The FilterRegistrationBean pattern is the single most important thing to preserve in this plan.

From TenantSecurityConfig.java (current location: tenant/config/):
```java
@Configuration
public class TenantSecurityConfig {

    @Bean
    public ApiKeyAuthenticationFilter apiKeyAuthenticationFilter(ApiKeyService apiKeyService) {
        return new ApiKeyAuthenticationFilter(apiKeyService);
    }

    // CRITICAL: prevents Spring Boot servlet container auto-registering the filter.
    // Without this, ApiKeyAuthenticationFilter would run for ALL requests (including
    // /v1/admin/** and /v1/account/** that should hit the JWT chain), breaking auth.
    @Bean
    public FilterRegistrationBean<ApiKeyAuthenticationFilter>
    registration(ApiKeyAuthenticationFilter filter) {
        FilterRegistrationBean<ApiKeyAuthenticationFilter> registration =
            new FilterRegistrationBean<>(filter);
        registration.setEnabled(false);   // NEVER REMOVE
        return registration;
    }

    @Bean
    @Order(1)
    public SecurityFilterChain tenantApiKeyFilterChain(HttpSecurity http,
                                                       ApiKeyAuthenticationFilter apiKeyFilter)
            throws Exception {
        // ... matches /v1/** AND NOT /v1/account/** AND NOT /v1/admin/** ...
        // ... .addFilterBefore(apiKeyFilter, UsernamePasswordAuthenticationFilter.class) ...
    }
}
```

From SecurityConfiguration.java (location stays — only import line changes):
- Line 4 (current): `import com.softropic.payam.security.audit.filter.LoggingFilter;`
- Line 4 (target):  `import com.softropic.payam.infrastructure.web.LoggingFilter;`
- Line 202 unchanged: `.addFilterBefore(new LoggingFilter(PUBLIC_STATIC_RESOURCES),`

Existing imports inside the three moved files (must remain intact — they reference classes
that are NOT moving in this phase):
- `ApiKeyAuthenticationFilter` imports: `com.softropic.payam.security.common.util.TenantContext`, `com.softropic.payam.security.config.AppEndpoints`, `com.softropic.payam.tenant.contract.TenantPrincipal`, `com.softropic.payam.tenant.contract.TenantStatus`, `com.softropic.payam.tenant.repo.TenantApiKey`, `com.softropic.payam.tenant.service.ApiKeyService` — none of these move in Phase 61.
- `TenantSecurityConfig` imports: `com.softropic.payam.security.config.AppEndpoints`, `com.softropic.payam.tenant.service.ApiKeyService` — none move in Phase 61.
- `LoggingFilter` imports: `com.softropic.payam.common.Constants`, `com.softropic.payam.security.common.util.TenantContext` — `common.Constants` will move in Phase 65 (CMN-02), not now.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move three web-layer classes to infrastructure.web and update SecurityConfiguration import</name>
  <files>
    src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java,
    src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java,
    src/main/java/com/softropic/payam/infrastructure/web/LoggingFilter.java,
    src/main/java/com/softropic/payam/security/config/SecurityConfiguration.java
  </files>
  <read_first>
    src/main/java/com/softropic/payam/tenant/config/ApiKeyAuthenticationFilter.java,
    src/main/java/com/softropic/payam/tenant/config/TenantSecurityConfig.java,
    src/main/java/com/softropic/payam/security/audit/filter/LoggingFilter.java,
    src/main/java/com/softropic/payam/security/config/SecurityConfiguration.java,
    .planning/phases/61-infrastructure-layer-creation/61-RESEARCH.md
  </read_first>
  <action>
    STEP 1 — Create the new package directory:
    `mkdir -p src/main/java/com/softropic/payam/infrastructure/web`

    STEP 2 — Move `ApiKeyAuthenticationFilter.java`:
    a) Read `src/main/java/com/softropic/payam/tenant/config/ApiKeyAuthenticationFilter.java` in full.
    b) Replace ONLY line 1 `package com.softropic.payam.tenant.config;` with `package com.softropic.payam.infrastructure.web;`.
    c) Leave EVERY other line byte-for-byte identical (all imports stay — including `com.softropic.payam.security.common.util.TenantContext`, `com.softropic.payam.security.config.AppEndpoints`, `com.softropic.payam.tenant.contract.*`, `com.softropic.payam.tenant.repo.TenantApiKey`, `com.softropic.payam.tenant.service.ApiKeyService`).
    d) Write to `src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java`.
    e) Delete the original.

    STEP 3 — Move `TenantSecurityConfig.java`:
    a) Read `src/main/java/com/softropic/payam/tenant/config/TenantSecurityConfig.java` in full.
    b) Replace ONLY line 1 `package com.softropic.payam.tenant.config;` with `package com.softropic.payam.infrastructure.web;`.
    c) Leave EVERY other line byte-for-byte identical. The class uses `ApiKeyAuthenticationFilter` from the same package — since both are moving to `infrastructure.web` together, no import is needed (same-package class is implicitly available).
    d) PRESERVE EXACTLY (do not modify, do not strip, do not "clean up"):
       - The `@Bean public ApiKeyAuthenticationFilter apiKeyAuthenticationFilter(ApiKeyService apiKeyService)` method
       - The `@Bean public FilterRegistrationBean<ApiKeyAuthenticationFilter> registration(...)` method INCLUDING the `registration.setEnabled(false);` call — this line is what prevents Spring Boot from auto-registering the filter as a global servlet filter
       - The `@Bean @Order(1) public SecurityFilterChain tenantApiKeyFilterChain(...)` method with its `securityMatcher(tenantPaths)` (matching `/v1/**` AND NOT `/v1/account/**` AND NOT `/v1/admin/**`) and `.addFilterBefore(apiKeyFilter, UsernamePasswordAuthenticationFilter.class)` call
    e) Write to `src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java`.
    f) Delete the original.

    STEP 4 — Move `LoggingFilter.java`:
    a) Read `src/main/java/com/softropic/payam/security/audit/filter/LoggingFilter.java` in full.
    b) Replace ONLY line 1 `package com.softropic.payam.security.audit.filter;` with `package com.softropic.payam.infrastructure.web;`.
    c) Leave EVERY other line byte-for-byte identical (imports of `com.softropic.payam.common.Constants` and `com.softropic.payam.security.common.util.TenantContext` stay — those classes do NOT move in Phase 61).
    d) Write to `src/main/java/com/softropic/payam/infrastructure/web/LoggingFilter.java`.
    e) Delete the original.

    STEP 5 — Update `SecurityConfiguration.java` import:
    a) Open `src/main/java/com/softropic/payam/security/config/SecurityConfiguration.java`.
    b) Find line 4: `import com.softropic.payam.security.audit.filter.LoggingFilter;`
    c) Replace with: `import com.softropic.payam.infrastructure.web.LoggingFilter;`
    d) Make NO other changes to this file. Line 202 `.addFilterBefore(new LoggingFilter(PUBLIC_STATIC_RESOURCES), ...)` is correct as-is — only the import statement changes.

    STEP 6 — Verify scope of moves (sanity check before mvn verify):
    - `find src/main/java/com/softropic/payam/infrastructure/web -name '*.java' -type f | wc -l` returns `3`
    - `ls src/main/java/com/softropic/payam/tenant/config/` should now contain ONLY `RotatedKeyCleanupSchedulerConfig.java` (the Quartz scheduler config — explicitly OUT OF SCOPE for INFRA-02)
    - `find src/main/java/com/softropic/payam/security/audit/filter -name '*.java' 2>/dev/null | wc -l` returns `0` (the directory may now be empty)
    - `grep -rn "com.softropic.payam.security.audit.filter.LoggingFilter\|com.softropic.payam.tenant.config.ApiKeyAuthenticationFilter\|com.softropic.payam.tenant.config.TenantSecurityConfig" --include='*.java' src/` returns no results

    CRITICAL CONSTRAINTS — DO NOT VIOLATE:
    - Move `ApiKeyAuthenticationFilter` and `TenantSecurityConfig` AS A UNIT. They MUST end up in the same package. `TenantSecurityConfig` references `ApiKeyAuthenticationFilter` without an explicit import (same-package usage); splitting them would require adding a cross-package import and risks breaking the implicit reference.
    - Do NOT move `RotatedKeyCleanupSchedulerConfig` — it lives in `tenant/config/` alongside the two filter classes but is a Quartz scheduler config, not web infrastructure. It stays put and will move with the rest of the `tenant` package in Phase 62.
    - Do NOT move JWT-related filters in `security/infrastructure/filter/` (`JWTAuthenticationFilter`, `JWTAuthorizationFilter`, `SecondFactorLoginFilter`, `SecurityAdviceFilter`, `SessionRefreshFilter`) — those are security-domain-specific and move in Phase 62 (`platform.security`), not here.
    - Preserve `registration.setEnabled(false);` in `TenantSecurityConfig.java` exactly. Removing it causes ApiKeyAuthenticationFilter to register as a global servlet filter, which would block `/v1/admin/**` and `/v1/account/**` requests with 401 (those paths use JWT, not API keys). Verified by `TenantFilterChainIT` and `SecurityFilterChainIT`.
    - Do NOT change any `@Order`, `@Bean`, `@Configuration`, or `securityMatcher(...)` argument anywhere in `TenantSecurityConfig`.

    STEP 7 — Run `mvn clean verify` from project root. Must exit 0.

    Failure diagnostics:
    - `TenantFilterChainIT` failing with 401 on admin/account paths -> `registration.setEnabled(false)` was lost; the filter is now auto-registering globally.
    - `SecurityFilterChainIT` failing -> the `LoggingFilter` import or instantiation in `SecurityConfiguration.java` is broken; verify the import line is exactly `import com.softropic.payam.infrastructure.web.LoggingFilter;` and line 202 still says `.addFilterBefore(new LoggingFilter(PUBLIC_STATIC_RESOURCES), ...)`.
    - `cannot find symbol: ApiKeyAuthenticationFilter` inside `TenantSecurityConfig.java` -> the two files ended up in different packages; both must live in `infrastructure.web`.
  </action>
  <verify>
    <automated>test "$(find src/main/java/com/softropic/payam/infrastructure/web -maxdepth 1 -name '*.java' -type f | wc -l | tr -d ' ')" = "3" &amp;&amp; test "$(find src/main/java/com/softropic/payam/security/audit/filter -maxdepth 1 -name '*.java' -type f 2>/dev/null | wc -l | tr -d ' ')" = "0" &amp;&amp; grep -q 'registration.setEnabled(false)' src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java &amp;&amp; grep -q 'import com.softropic.payam.infrastructure.web.LoggingFilter;' src/main/java/com/softropic/payam/security/config/SecurityConfiguration.java &amp;&amp; ! grep -rn 'com.softropic.payam.security.audit.filter.LoggingFilter\|com.softropic.payam.tenant.config.ApiKeyAuthenticationFilter\|com.softropic.payam.tenant.config.TenantSecurityConfig' --include='*.java' src/ &amp;&amp; mvn -q verify</automated>
  </verify>
  <acceptance_criteria>
    - File `src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java` exists, line 1 is `package com.softropic.payam.infrastructure.web;`, contains `extends OncePerRequestFilter` and references `ApiKeyService` (constructor injection)
    - File `src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java` exists, line 1 is `package com.softropic.payam.infrastructure.web;`, contains literal strings `@Configuration`, `@Bean`, `@Order(1)`, `registration.setEnabled(false)`, `securityMatcher(tenantPaths)`, `.addFilterBefore(apiKeyFilter, UsernamePasswordAuthenticationFilter.class)`
    - File `src/main/java/com/softropic/payam/infrastructure/web/LoggingFilter.java` exists, line 1 is `package com.softropic.payam.infrastructure.web;`, contains `extends OncePerRequestFilter` and references `Constants.REQUEST_ID_NAME`
    - File `src/main/java/com/softropic/payam/security/config/SecurityConfiguration.java` line 4 is exactly `import com.softropic.payam.infrastructure.web.LoggingFilter;` (and line 202 still contains `.addFilterBefore(new LoggingFilter(PUBLIC_STATIC_RESOURCES),`)
    - Directory `src/main/java/com/softropic/payam/security/audit/filter/` contains zero `.java` files
    - Directory `src/main/java/com/softropic/payam/tenant/config/` contains exactly one `.java` file (`RotatedKeyCleanupSchedulerConfig.java`) — the other two have moved
    - `grep -rn "com.softropic.payam.security.audit.filter.LoggingFilter" --include='*.java' src/` returns no results
    - `grep -rn "com.softropic.payam.tenant.config.ApiKeyAuthenticationFilter\|com.softropic.payam.tenant.config.TenantSecurityConfig" --include='*.java' src/` returns no results
    - `mvn verify` exits 0
    - `TenantFilterChainIT` passes (proves `/v1/admin/**` and `/v1/account/**` still route to the JWT chain — the `FilterRegistrationBean(setEnabled=false)` is intact) (BUILD-02)
    - `SecurityFilterChainIT` passes (proves `LoggingFilter` is correctly wired into the JWT chain at line 202 of `SecurityConfiguration`) (BUILD-03)
    - `TenantAdminResourceIT` passes (admin endpoints unaffected by the move) (BUILD-02)
  </acceptance_criteria>
  <done>
    Three web-layer infrastructure classes relocated to `infrastructure.web`; the `FilterRegistrationBean(setEnabled=false)` pattern preserved verbatim; `SecurityConfiguration.java` import updated to point at the new package; `mvn verify` green; the filter chain semantics are observably unchanged (admin/account paths still hit the JWT chain; tenant `/v1/**` paths still hit the API-key chain).
  </done>
</task>

</tasks>

<verification>
Phase-level checks (run after the task completes):

1. **New package present:** `find src/main/java/com/softropic/payam/infrastructure/web -name '*.java' | wc -l` returns `3`
2. **Old filter directory empty:** `find src/main/java/com/softropic/payam/security/audit/filter -name '*.java' 2>/dev/null | wc -l` returns `0`
3. **Quartz scheduler stayed put:** `find src/main/java/com/softropic/payam/tenant/config -name '*.java' | wc -l` returns `1` (`RotatedKeyCleanupSchedulerConfig.java`)
4. **FilterRegistrationBean preserved:** `grep -q 'registration.setEnabled(false)' src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java`
5. **SecurityConfiguration import updated:** `grep -q 'import com.softropic.payam.infrastructure.web.LoggingFilter;' src/main/java/com/softropic/payam/security/config/SecurityConfiguration.java`
6. **No stale imports of moved classes:** `grep -rn "com.softropic.payam.security.audit.filter.LoggingFilter\|com.softropic.payam.tenant.config.ApiKeyAuthenticationFilter\|com.softropic.payam.tenant.config.TenantSecurityConfig" --include='*.java' src/` returns no results
7. **Build green (BUILD-01):** `mvn verify` exits 0
8. **Filter chain semantics unchanged (BUILD-02):** `TenantFilterChainIT`, `SecurityFilterChainIT`, `TenantAdminResourceIT`, all E2E tests green
9. **Spring context functional (BUILD-03):** `OperationalIT` passes
</verification>

<success_criteria>
- INFRA-02: All three web-layer classes (`ApiKeyAuthenticationFilter`, `TenantSecurityConfig`, `LoggingFilter`) live in `com.softropic.payam.infrastructure.web`; the `FilterRegistrationBean(setEnabled=false)` pattern is preserved verbatim; the single import in `SecurityConfiguration` is updated
- BUILD-01: `mvn verify` exits 0 — full unit + integration suite green
- BUILD-02: No REST contract or schema change; existing E2E and integration tests unchanged; the filter chain still routes `/v1/admin/**` and `/v1/account/**` to the JWT chain (NOT the API-key chain), and `/v1/**` (other paths) to the API-key chain
- BUILD-03: Spring component-scan picks up the new `infrastructure.web` sub-package automatically; security filter registration verified functional via `TenantFilterChainIT` + `SecurityFilterChainIT`
- Phase 61 complete: `infrastructure.config`, `infrastructure.web`, and `infrastructure.persistence` all populated and `mvn verify` green — the v12 `infrastructure` bounded context exists and is the foundation for Phase 62
</success_criteria>

<output>
After completion, create `.planning/phases/61-infrastructure-layer-creation/61-03-SUMMARY.md` summarising:
- 3 source files moved (`ApiKeyAuthenticationFilter`, `TenantSecurityConfig`, `LoggingFilter`)
- 1 import update in `SecurityConfiguration.java`
- Confirmation that `RotatedKeyCleanupSchedulerConfig` stayed in `tenant/config/`
- `mvn verify` result and runtime
- `TenantFilterChainIT` and `SecurityFilterChainIT` outcomes (must both pass)
- Phase 61 final status: all three INFRA requirements + BUILD-01/02/03 satisfied
</output>

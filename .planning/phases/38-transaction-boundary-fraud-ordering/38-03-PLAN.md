---
phase: 38-transaction-boundary-fraud-ordering
plan: 03
type: execute
wave: 3
depends_on: ["38-01", "38-02"]
files_modified:
  - .planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md
autonomous: true
requirements:
  - TXN-01
  - OPS-02
must_haves:
  truths:
    - "Full mvn verify completes with exit code 0 after Plans 01 and 02"
    - "Payment orchestration, fraud, idempotency, reconciliation, and webhook E2E test classes are all green"
    - "Phase 38 sign-off summary exists at 38-03-SUMMARY.md with command output evidence"
  artifacts:
    - path: ".planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md"
      provides: "Sign-off summary with test counts and regression confirmation"
      contains: "BUILD SUCCESS"
  key_links:
    - from: "Phase 38 plans (01, 02)"
      to: "mvn verify full regression"
      via: "green suite confirming no cross-test regressions"
      pattern: "BUILD SUCCESS"
---

<objective>
Full regression sign-off for Phase 38. Run `mvn verify` end-to-end, confirm all tests green, and record the outcome in a sign-off summary.

Purpose: Gate phase completion on a clean full-suite run — Plans 01 and 02 touch hot-path code (PaymentOrchestrator, FraudScoringService) used by payment, fraud, webhook, reconciliation, and idempotency flows. Full verify is the only way to prove no collateral damage.
Output: 38-03-SUMMARY.md containing command output, test counts, and sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-transaction-boundary-fraud-ordering/38-RESEARCH.md
@.planning/phases/38-transaction-boundary-fraud-ordering/38-VALIDATION.md
@.planning/phases/38-transaction-boundary-fraud-ordering/38-01-SUMMARY.md
@.planning/phases/38-transaction-boundary-fraud-ordering/38-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full mvn verify and capture output</name>
  <files>.planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/38-transaction-boundary-fraud-ordering/38-01-SUMMARY.md
    - .planning/phases/38-transaction-boundary-fraud-ordering/38-02-SUMMARY.md
    - .planning/phases/38-transaction-boundary-fraud-ordering/38-VALIDATION.md
  </read_first>
  <action>
    Run the full regression suite from the project root:

    ```
    mvn verify 2>&1 | tee /tmp/phase-38-verify.log
    ```

    After the run completes, extract:
    - Final `BUILD SUCCESS` or `BUILD FAILURE` line
    - Surefire totals (Tests run: X, Failures: Y, Errors: Z, Skipped: W)
    - Failsafe totals (Tests run: X, Failures: Y, Errors: Z, Skipped: W)
    - Specifically confirm these test classes are listed and green:
      - PaymentOrchestratorIT (Plan 01 regression + new ordering test)
      - FraudVelocityOrderingIT (Plan 02 Wave 0 — new file)
      - FraudEngineIT (Plan 02 regression — velocityBlockReturns422)
      - FraudScoringServiceIT (Plan 02 regression — evaluate() direct tests)
      - WebhookEnqueueListenerIT (cross-phase regression — Phase 37)
      - IdempotencyServiceIT (cross-phase regression — Phase 35)

    If BUILD FAILURE: read the failing test output, determine whether the failure is:
    (a) caused by Phase 38 changes (must be fixed before sign-off — return to Plan 01 or 02)
    (b) a pre-existing known flake (e.g., SecurityFilterChainIT Docker-context issue from Phase 36 notes) — document but allow

    Write .planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md with this structure:

    ```markdown
    # Phase 38 — Transaction Boundary & Fraud Ordering — Sign-Off

    **Completed:** {date}
    **Status:** SIGNED OFF | FAILED

    ## Scope
    - TXN-01: Fee evaluation hoisted out of SELECT...FOR UPDATE lock (Plan 01)
    - OPS-02: Velocity token consumption deferred until after idempotencyService.store() (Plan 02)

    ## mvn verify result
    ```
    {final BUILD line}
    Surefire: Tests run: X, Failures: Y, Errors: Z, Skipped: W
    Failsafe: Tests run: X, Failures: Y, Errors: Z, Skipped: W
    ```

    ## Critical tests — GREEN confirmation
    - [x] PaymentOrchestratorIT — N tests, all passing (incl. new feeEvaluationHappensBeforeLock)
    - [x] FraudVelocityOrderingIT — 1 test, passing (new OPS-02 proof)
    - [x] FraudEngineIT — N tests, all passing (velocityBlockReturns422 regression)
    - [x] FraudScoringServiceIT — N tests, all passing
    - [x] WebhookEnqueueListenerIT — N tests, all passing (Phase 37 regression)
    - [x] IdempotencyServiceIT — N tests, all passing (Phase 35 regression)

    ## Pre-existing issues (not Phase 38 regressions)
    {list any known flakes documented in STATE.md such as SecurityFilterChainIT Docker-context from Phase 36}

    ## Sign-off
    TXN-01 and OPS-02 both verified green. Phase 38 closed.
    ```

    Constraints:
    - Do NOT edit any source files from this task.
    - Do NOT mark status SIGNED OFF if BUILD FAILURE is caused by Phase 38 changes.
    - The summary MUST contain the literal string `BUILD SUCCESS` (from the mvn output) if sign-off is granted.
  </action>
  <verify>
    <automated>test -f .planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md && grep -q "BUILD SUCCESS" .planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md && grep -q "SIGNED OFF" .planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `mvn verify` exits 0 (BUILD SUCCESS).
    - `.planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md` exists.
    - `grep "BUILD SUCCESS" .planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md` returns at least 1 match.
    - `grep "SIGNED OFF" .planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md` returns at least 1 match.
    - `grep "PaymentOrchestratorIT" .planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md` returns at least 1 match.
    - `grep "FraudVelocityOrderingIT" .planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md` returns at least 1 match.
    - `grep "TXN-01" .planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md` returns at least 1 match.
    - `grep "OPS-02" .planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md` returns at least 1 match.
  </acceptance_criteria>
  <done>mvn verify green; sign-off summary written with evidence of both requirements satisfied and no cross-phase regressions.</done>
</task>

</tasks>

<verification>
- Full `mvn verify` passes
- Sign-off summary exists with BUILD SUCCESS and both requirement IDs referenced
</verification>

<success_criteria>
- Phase 38 sign-off complete
- TXN-01 and OPS-02 both proven green in full-suite context
</success_criteria>

<output>
Phase 38 sign-off summary at `.planning/phases/38-transaction-boundary-fraud-ordering/38-03-SUMMARY.md`
</output>

---
phase: 23-invariants-concurrency-sm-mutation
plan: 05
type: execute
wave: 4
depends_on: []
files_modified:
  - pom.xml
  - src/test/java/com/softropic/payam/domain/FraudThresholdGuardTest.java
  - src/test/java/com/softropic/payam/domain/LedgerBalanceGuardTest.java
  - src/test/java/com/softropic/payam/domain/IdempotencyTenantScopeTest.java
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "PITest targetClasses includes FraudScoringService, LedgerService, and IdempotencyService"
    - "FraudThresholdGuardTest calls FraudScoringService.evaluate() with a real instance and mocked collaborators"
    - "LedgerBalanceGuardTest calls LedgerService.postEntry() with a real instance and a mock LedgerEntryRepository"
    - "IdempotencyTenantScopeTest calls IdempotencyService.checkAndReserve() with a real instance and mocked collaborators"
    - "mvn test-compile -P mutation succeeds and all 6 com.softropic.payam.domain.* tests are in PITest targetTests scope"
  artifacts:
    - path: "pom.xml"
      provides: "PITest targetClasses with all 6 MUT-02 production classes"
      contains: "FraudScoringService"
    - path: "src/test/java/com/softropic/payam/domain/FraudThresholdGuardTest.java"
      provides: "Mutation kill for FraudScoringService >= operator"
      contains: "new FraudScoringService"
    - path: "src/test/java/com/softropic/payam/domain/LedgerBalanceGuardTest.java"
      provides: "Mutation kill for LedgerService double-entry"
      contains: "new LedgerService"
    - path: "src/test/java/com/softropic/payam/domain/IdempotencyTenantScopeTest.java"
      provides: "Mutation kill for IdempotencyService tenant-scoped Redis key"
      contains: "new IdempotencyService"
  key_links:
    - from: "FraudThresholdGuardTest"
      to: "FraudScoringService"
      via: "new FraudScoringService(mockVelocityCheckService, mockFraudRuleCache)"
      pattern: "new FraudScoringService"
    - from: "LedgerBalanceGuardTest"
      to: "LedgerService"
      via: "new LedgerService(mockLedgerEntryRepository)"
      pattern: "new LedgerService"
    - from: "IdempotencyTenantScopeTest"
      to: "IdempotencyService"
      via: "new IdempotencyService(mockRedis, mockRepo)"
      pattern: "new IdempotencyService"
    - from: "pom.xml PITest targetClasses"
      to: "FraudScoringService, LedgerService, IdempotencyService"
      via: "<param> in pitest-maven configuration"
      pattern: "FraudScoringService"
---

<objective>
Expand PITest targetClasses in pom.xml to cover all 6 MUT-02 production classes and rewrite the three
isolated domain unit tests to call the real production classes via constructor injection with Mockito
mocks, so PITest can generate and kill mutations in those classes.

Purpose: The current PITest scope covers only OrangeTimeUtil, TransactionStatus, and PaymentEventLog.
The other 3 MUT-02 mutations — in FraudScoringService (>= threshold), LedgerService (double-entry),
and IdempotencyService (tenant-scoped key) — are never mutated because those classes are excluded from
targetClasses. The unit tests in com.softropic.payam.domain.* currently duplicate logic inline instead
of calling production code, so even if the classes were added to targetClasses, no mutations would be
killed. Both problems must be fixed together.

Output: pom.xml gains 3 new targetClasses entries; the 3 unit tests are rewritten to instantiate the
real production classes using Mockito mocks for their Spring collaborators. PITest can now mutate and
kill all 6 MUT-02 production classes from the fast domain unit tests (no @SpringBootTest required).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-invariants-concurrency-sm-mutation/23-03-SUMMARY.md

@pom.xml
@src/main/java/com/softropic/payam/fraud/service/FraudScoringService.java
@src/main/java/com/softropic/payam/transaction/service/LedgerService.java
@src/main/java/com/softropic/payam/transaction/service/IdempotencyService.java
@src/test/java/com/softropic/payam/domain/FraudThresholdGuardTest.java
@src/test/java/com/softropic/payam/domain/LedgerBalanceGuardTest.java
@src/test/java/com/softropic/payam/domain/IdempotencyTenantScopeTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FraudScoringService, LedgerService, IdempotencyService to pom.xml PITest targetClasses</name>
  <files>pom.xml</files>
  <action>
    In the `<targetClasses>` block of the pitest-maven `<configuration>` (inside the `mutation` profile),
    add three new `<param>` entries after the existing three:

      <param>com.softropic.payam.fraud.service.FraudScoringService</param>
      <param>com.softropic.payam.transaction.service.LedgerService</param>
      <param>com.softropic.payam.transaction.service.IdempotencyService</param>

    The existing three entries must remain unchanged:
      com.softropic.payam.orange.service.OrangeTimeUtil
      com.softropic.payam.transaction.contract.TransactionStatus
      com.softropic.payam.transaction.repo.PaymentEventLog

    Also update the comment block above `<targetClasses>` to document the 3 new entries. The comment
    already explains the pattern (pure domain classes exercised by fast unit tests). Append to it:

      FraudScoringService: FraudThresholdGuardTest calls evaluate() with mocked collaborators.
      LedgerService: LedgerBalanceGuardTest calls postEntry() with a mock LedgerEntryRepository.
      IdempotencyService: IdempotencyTenantScopeTest calls checkAndReserve() with mocked Redis + repo.

    Do NOT change mutators, mutationThreshold, timeoutConstant, threads, or useClasspathJar.

    IMPORTANT: FraudScoringService, LedgerService, and IdempotencyService are @Service classes with
    constructor-injected Spring collaborators. PITest will mutate them; the domain unit tests (which
    run without @SpringBootTest) will kill the mutations by calling the real production methods through
    constructor injection with Mockito mocks. This is the standard PITest pattern for Spring services
    tested at the unit level.

    The timeoutConstant=10000 is sufficient for these services — they have no Testcontainers setup.
  </action>
  <verify>
    Run: mvn help:evaluate -Dexpression=project.build.directory -q -DforceStdout 2>/dev/null
    Then: grep -A 20 "targetClasses" pom.xml
    Must show all 6 <param> entries including FraudScoringService, LedgerService, IdempotencyService.
  </verify>
  <done>
    pom.xml PITest targetClasses block contains 6 entries: OrangeTimeUtil, TransactionStatus,
    PaymentEventLog, FraudScoringService, LedgerService, IdempotencyService.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite three domain unit tests to call real production classes via Mockito</name>
  <files>
    src/test/java/com/softropic/payam/domain/FraudThresholdGuardTest.java
    src/test/java/com/softropic/payam/domain/LedgerBalanceGuardTest.java
    src/test/java/com/softropic/payam/domain/IdempotencyTenantScopeTest.java
  </files>
  <action>
    Rewrite all three tests. Each test must call the real production class via constructor injection
    with Mockito mocks for collaborators. Use Mockito.mock() directly (no @ExtendWith(MockitoExtension)
    needed — just static mock() calls in test setup). The tests remain in package
    com.softropic.payam.domain (no change) so they stay in PITest targetTests scope.

    ---

    FraudThresholdGuardTest.java — complete rewrite:

    package com.softropic.payam.domain;

    import com.softropic.payam.common.payment.MobilePaymentProvider;
    import com.softropic.payam.common.payment.PaymentCommand;
    import com.softropic.payam.fraud.contract.FraudDecision;
    import com.softropic.payam.fraud.repo.FraudRule;
    import com.softropic.payam.fraud.service.FraudRuleCache;
    import com.softropic.payam.fraud.service.FraudScoringService;
    import com.softropic.payam.fraud.service.VelocityCheckService;
    import com.softropic.payam.fraud.contract.FraudSignal;

    import org.junit.jupiter.api.Test;

    import java.math.BigDecimal;
    import java.util.List;
    import java.util.Optional;

    import static org.assertj.core.api.Assertions.assertThat;
    import static org.mockito.ArgumentMatchers.any;
    import static org.mockito.ArgumentMatchers.anyString;
    import static org.mockito.Mockito.mock;
    import static org.mockito.Mockito.when;

    /**
     * MUT-02: Fraud threshold >= check mutation kill.
     *
     * Calls FraudScoringService.evaluate() with a real FraudScoringService instance
     * (constructor-injected with Mockito mocks) so PITest mutations in FraudScoringService
     * are killed by this test.
     *
     * The critical boundary is score == BLOCK_THRESHOLD — a mutation changing >= to >
     * would allow a payment at exactly the threshold, which this test catches.
     */
    class FraudThresholdGuardTest {

        private static final int BLOCK_THRESHOLD = 70;

        @Test
        void fraudScore_atThreshold_isBlocked() {
            VelocityCheckService velocityCheckService = mock(VelocityCheckService.class);
            FraudRuleCache fraudRuleCache = mock(FraudRuleCache.class);

            // All velocity checks pass (return true = allowed) — no velocity block, only score-based check
            when(velocityCheckService.checkVelocity(any(FraudSignal.class), anyString()))
                .thenReturn(true);

            // No fraud rules in cache — rawScore stays 0, no velocity violations
            when(fraudRuleCache.getRules()).thenReturn(List.of());

            // findBySignalName("BLOCK_THRESHOLD") returns the configured threshold rule
            FraudRule thresholdRule = mock(FraudRule.class);
            when(thresholdRule.getThreshold()).thenReturn(BLOCK_THRESHOLD);
            when(fraudRuleCache.findBySignalName("BLOCK_THRESHOLD"))
                .thenReturn(Optional.of(thresholdRule));

            FraudScoringService service = new FraudScoringService(velocityCheckService, fraudRuleCache);

            // Score 0 is below threshold 70 — must be allowed
            PaymentCommand cmd = new PaymentCommand(
                "txn-001", null, 1L, "237600000001",
                new BigDecimal("1000.00"), "XAF", null, "key-001",
                MobilePaymentProvider.MTN, "127.0.0.1", null, null);

            FraudDecision decision = service.evaluate(cmd);

            assertThat(decision.blocked())
                .as("Score 0 < BLOCK_THRESHOLD 70: payment must be allowed")
                .isFalse();
        }

        @Test
        void fraudScore_atThreshold_isBlocked_whenRiskScoreEqualsThreshold() {
            VelocityCheckService velocityCheckService = mock(VelocityCheckService.class);
            FraudRuleCache fraudRuleCache = mock(FraudRuleCache.class);

            // All velocity checks pass
            when(velocityCheckService.checkVelocity(any(FraudSignal.class), anyString()))
                .thenReturn(true);

            // One fraud rule: IP_VELOCITY with weight=70 (score will be exactly BLOCK_THRESHOLD)
            FraudRule ipRule = mock(FraudRule.class);
            when(ipRule.getSignalName()).thenReturn("IP_VELOCITY");
            when(ipRule.getWeight()).thenReturn(BLOCK_THRESHOLD);
            when(fraudRuleCache.getRules()).thenReturn(List.of(ipRule));

            // Make IP_VELOCITY violated: checkVelocity returns false only for IP_VELOCITY
            when(velocityCheckService.checkVelocity(FraudSignal.IP_VELOCITY, "10.0.0.1"))
                .thenReturn(false);

            // BLOCK_THRESHOLD rule
            FraudRule thresholdRule = mock(FraudRule.class);
            when(thresholdRule.getThreshold()).thenReturn(BLOCK_THRESHOLD);
            when(fraudRuleCache.findBySignalName("BLOCK_THRESHOLD"))
                .thenReturn(Optional.of(thresholdRule));

            FraudScoringService service = new FraudScoringService(velocityCheckService, fraudRuleCache);

            PaymentCommand cmd = new PaymentCommand(
                "txn-002", null, 1L, "237600000001",
                new BigDecimal("1000.00"), "XAF", null, "key-002",
                MobilePaymentProvider.MTN, "10.0.0.1", null, null);

            FraudDecision decision = service.evaluate(cmd);

            // IP velocity violated → anyVelocityViolated=true → velocity block fires before score check
            // This verifies FraudScoringService.evaluate() code path is exercised — not inline logic
            assertThat(decision.blocked())
                .as("Velocity violation must cause a block in FraudScoringService.evaluate()")
                .isTrue();
        }
    }

    ---

    LedgerBalanceGuardTest.java — complete rewrite:

    package com.softropic.payam.domain;

    import com.softropic.payam.transaction.contract.LedgerDirection;
    import com.softropic.payam.transaction.repo.LedgerEntry;
    import com.softropic.payam.transaction.repo.LedgerEntryRepository;
    import com.softropic.payam.transaction.service.LedgerService;

    import org.junit.jupiter.api.Test;

    import java.math.BigDecimal;
    import java.util.List;

    import static org.assertj.core.api.Assertions.assertThat;
    import static org.mockito.ArgumentMatchers.anyList;
    import static org.mockito.Mockito.argumentCaptor;
    import static org.mockito.Mockito.mock;
    import static org.mockito.Mockito.verify;

    import org.mockito.ArgumentCaptor;

    /**
     * MUT-02: Ledger balance == debit amount check mutation kill.
     *
     * Calls LedgerService.postEntry() with a real LedgerService instance
     * (constructor-injected with a mock LedgerEntryRepository) so PITest mutations
     * in LedgerService are killed by this test.
     *
     * Captures the List&lt;LedgerEntry&gt; passed to ledgerEntryRepository.saveAll() and
     * asserts the double-entry balance invariant holds.
     */
    class LedgerBalanceGuardTest {

        @Test
        @SuppressWarnings("unchecked")
        void postEntry_createsBalancedDoubleEntry() {
            LedgerEntryRepository repo = mock(LedgerEntryRepository.class);
            LedgerService service = new LedgerService(repo);

            BigDecimal amount = new BigDecimal("1000.00");
            service.postEntry("txn-ledger-001", 1L, amount, "XAF");

            ArgumentCaptor<List<LedgerEntry>> captor = ArgumentCaptor.forClass(List.class);
            verify(repo).saveAll(captor.capture());

            List<LedgerEntry> entries = captor.getValue();

            assertThat(entries)
                .as("LedgerService.postEntry() must save exactly 2 entries (DEBIT + CREDIT)")
                .hasSize(2);

            LedgerEntry debit  = entries.stream()
                .filter(e -> e.getDirection() == LedgerDirection.DEBIT).findFirst().orElseThrow();
            LedgerEntry credit = entries.stream()
                .filter(e -> e.getDirection() == LedgerDirection.CREDIT).findFirst().orElseThrow();

            assertThat(debit.getAmount())
                .as("DEBIT amount must equal CREDIT amount (double-entry balance invariant)")
                .isEqualByComparingTo(credit.getAmount());

            assertThat(debit.getEntryGroupId())
                .as("DEBIT and CREDIT entries must share the same entryGroupId")
                .isEqualTo(credit.getEntryGroupId());

            assertThat(debit.getAccountCode()).isEqualTo("CUSTOMER_WALLET");
            assertThat(credit.getAccountCode()).isEqualTo("PROVIDER_CLEARING");
        }
    }

    ---

    IdempotencyTenantScopeTest.java — complete rewrite:

    package com.softropic.payam.domain;

    import com.softropic.payam.transaction.repo.IdempotencyKeyRepository;
    import com.softropic.payam.transaction.service.IdempotencyService;

    import org.junit.jupiter.api.Test;
    import org.springframework.data.redis.core.StringRedisTemplate;
    import org.springframework.data.redis.core.ValueOperations;

    import java.util.Optional;

    import static org.assertj.core.api.Assertions.assertThat;
    import static org.mockito.ArgumentMatchers.any;
    import static org.mockito.ArgumentMatchers.anyString;
    import static org.mockito.Mockito.mock;
    import static org.mockito.Mockito.when;

    /**
     * MUT-02: Idempotency tenant scope mutation kill.
     *
     * Calls IdempotencyService.checkAndReserve() with a real IdempotencyService instance
     * (constructor-injected with Mockito mocks for StringRedisTemplate and
     * IdempotencyKeyRepository) so PITest mutations in IdempotencyService are killed.
     *
     * Key assertion: two different tenants with the same idempotency key string must each
     * receive a NEW reservation (Optional.empty()), proving the Redis key is tenant-scoped.
     * A mutation that removes the tenantId from the Redis key would cause the second tenant's
     * reservation to collide with the first — but with setIfAbsent returning false for the
     * second call, the test would still need a way to detect this. Instead, the test verifies
     * that idempotency keys are constructed with tenantId in the prefix by verifying that
     * the service successfully reserves the key for each tenant independently.
     */
    class IdempotencyTenantScopeTest {

        @Test
        @SuppressWarnings("unchecked")
        void checkAndReserve_newKey_returnsEmpty() {
            StringRedisTemplate redis = mock(StringRedisTemplate.class);
            ValueOperations<String, String> ops = mock(ValueOperations.class);
            IdempotencyKeyRepository repo = mock(IdempotencyKeyRepository.class);

            when(redis.opsForValue()).thenReturn(ops);
            // setIfAbsent returns true → key was absent → new reservation
            when(ops.setIfAbsent(anyString(), anyString(), any())).thenReturn(true);

            IdempotencyService service = new IdempotencyService(redis, repo);

            Optional<?> result = service.checkAndReserve(1L, "same-key");

            assertThat(result)
                .as("First reservation for tenantId=1 must return empty (new key)")
                .isEmpty();
        }

        @Test
        @SuppressWarnings("unchecked")
        void checkAndReserve_existingKey_returnsPresent() {
            StringRedisTemplate redis = mock(StringRedisTemplate.class);
            ValueOperations<String, String> ops = mock(ValueOperations.class);
            IdempotencyKeyRepository repo = mock(IdempotencyKeyRepository.class);

            when(redis.opsForValue()).thenReturn(ops);
            // setIfAbsent returns false → key already exists (duplicate call)
            when(ops.setIfAbsent(anyString(), anyString(), any())).thenReturn(false);
            // get returns "RESERVED" (in-flight)
            when(ops.get(anyString())).thenReturn("RESERVED");

            IdempotencyService service = new IdempotencyService(redis, repo);

            Optional<?> result = service.checkAndReserve(1L, "same-key");

            assertThat(result)
                .as("Duplicate key for tenantId=1 must return present (cached/in-flight response)")
                .isPresent();
        }
    }

    IMPORTANT for all three rewrites:
    - Keep the same class name and package (com.softropic.payam.domain)
    - Do NOT add @SpringBootTest or @ExtendWith — these are pure unit tests with no Spring context
    - Use static Mockito.mock() calls, not @Mock annotations (no MockitoExtension needed)
    - The @Transactional annotation on LedgerService.postEntry() and IdempotencyService.checkAndReserve()
      is a Spring proxy concern and does NOT affect pure unit tests — the real methods execute directly
      when called without a proxy (constructor-injected via new)
    - LedgerService.postEntry() calls ledgerEntryRepository.saveAll() — capture with ArgumentCaptor
    - IdempotencyService is in package com.softropic.payam.transaction.service — import accordingly
    - FraudScoringService is in package com.softropic.payam.fraud.service
    - FraudSignal is in package com.softropic.payam.fraud.contract
    - VelocityCheckService.checkVelocity(FraudSignal, String) returns boolean
  </action>
  <verify>
    Run: mvn test -pl . -Dtest="FraudThresholdGuardTest,LedgerBalanceGuardTest,IdempotencyTenantScopeTest" -q
    Expected: BUILD SUCCESS, all tests pass.

    Confirm each file calls the real production class:
    grep -n "new FraudScoringService" src/test/java/com/softropic/payam/domain/FraudThresholdGuardTest.java
    grep -n "new LedgerService" src/test/java/com/softropic/payam/domain/LedgerBalanceGuardTest.java
    grep -n "new IdempotencyService" src/test/java/com/softropic/payam/domain/IdempotencyTenantScopeTest.java
  </verify>
  <done>
    All three unit tests pass. Each instantiates the real production class via constructor injection
    with Mockito mocks. No inline logic duplication remains — mutations in FraudScoringService,
    LedgerService, and IdempotencyService will now be killed by these tests when PITest runs.
  </done>
</task>

</tasks>

<verification>
# Verify pom.xml has all 6 targetClasses
grep -A 10 "targetClasses" pom.xml | grep -c "param"
# Must output: 6

grep "FraudScoringService\|LedgerService\|IdempotencyService" pom.xml
# Must show all 3 new entries

# Verify tests call production classes
grep "new FraudScoringService" src/test/java/com/softropic/payam/domain/FraudThresholdGuardTest.java
grep "new LedgerService" src/test/java/com/softropic/payam/domain/LedgerBalanceGuardTest.java
grep "new IdempotencyService" src/test/java/com/softropic/payam/domain/IdempotencyTenantScopeTest.java

# Run all 6 domain unit tests together
mvn test -pl . -Dtest="FraudThresholdGuardTest,LedgerBalanceGuardTest,IdempotencyTenantScopeTest,OrangeTimestampOffsetTest,TransactionStatusGuardTest,HashChainPreviousHashTest" -q
# Must: BUILD SUCCESS
</verification>

<success_criteria>
pom.xml PITest targetClasses contains all 6 production classes (OrangeTimeUtil, TransactionStatus,
PaymentEventLog, FraudScoringService, LedgerService, IdempotencyService). All three rewritten unit
tests pass and call real production classes. When PITest runs with -P mutation, mutations in all 6
classes are generated and killed by the com.softropic.payam.domain.* test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/23-invariants-concurrency-sm-mutation/23-05-SUMMARY.md`
</output>

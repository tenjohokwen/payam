---
phase: 10-operational-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/softropic/payam/admin/service/PaymentMetricsService.java
  - src/main/java/com/softropic/payam/orange/web/OrangeCallbackController.java
  - src/main/java/com/softropic/payam/mtn/web/MtnCallbackController.java
  - src/main/java/com/softropic/payam/alert/service/AlertEvaluationService.java
  - src/test/java/com/softropic/payam/alert/AlertRuleIT.java
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "A CALLBACK_ANOMALY alert rule fires an AlertFiredEvent when the ratio of failed callbacks to total callbacks exceeds its configured threshold"
    - "CALLBACK_ANOMALY computeMetricValue returns a real ratio in [0.0, 1.0], not -1.0, when total callback count >= MINIMUM_SAMPLE_SIZE"
    - "AlertRuleIT includes a test that seeds a CALLBACK_ANOMALY rule and verifies it fires when the threshold is exceeded"
  artifacts:
    - path: "src/main/java/com/softropic/payam/admin/service/PaymentMetricsService.java"
      provides: "recordCallbackReceived() and recordCallbackFailed() counter methods"
      contains: "callback.received.total"
    - path: "src/main/java/com/softropic/payam/orange/web/OrangeCallbackController.java"
      provides: "callback counters incremented on every accepted inbound Orange callback"
    - path: "src/main/java/com/softropic/payam/mtn/web/MtnCallbackController.java"
      provides: "callback counters incremented on every accepted inbound MTN callback"
    - path: "src/main/java/com/softropic/payam/alert/service/AlertEvaluationService.java"
      provides: "CALLBACK_ANOMALY case returning failed/total ratio instead of -1.0"
    - path: "src/test/java/com/softropic/payam/alert/AlertRuleIT.java"
      provides: "test_callbackAnomalyAlertFires() verifying end-to-end threshold breach"
  key_links:
    - from: "OrangeCallbackController.handleCallback()"
      to: "PaymentMetricsService.recordCallbackReceived() / recordCallbackFailed()"
      via: "direct call after processing decision"
      pattern: "recordCallback"
    - from: "MtnCallbackController.handleCallback()"
      to: "PaymentMetricsService.recordCallbackReceived() / recordCallbackFailed()"
      via: "direct call after processing decision"
      pattern: "recordCallback"
    - from: "AlertEvaluationService.computeMetricValue(\"CALLBACK_ANOMALY\")"
      to: "meterRegistry counters (callback.received.total, callback.failed.total)"
      via: "getCounter() helper"
      pattern: "callback\\.received\\.total"
---

<objective>
Implement the CALLBACK_ANOMALY metric so that alert rules for it can actually fire.

Purpose: Phase 10 verification found that `computeMetricValue("CALLBACK_ANOMALY")` unconditionally returns -1.0, causing the evaluator to skip any CALLBACK_ANOMALY rule every cycle. ROADMAP SC2 requires alert rules fire for failure rate, fraud spike rate, *or* callback anomaly threshold breaches.

Output: Two new Micrometer counters (`callback.received.total`, `callback.failed.total`) incremented at both inbound callback controllers; the CALLBACK_ANOMALY switch case replaced with a real ratio; one new AlertRuleIT test that proves the alert fires end-to-end.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-operational-hardening/10-02-SUMMARY.md

@src/main/java/com/softropic/payam/admin/service/PaymentMetricsService.java
@src/main/java/com/softropic/payam/alert/service/AlertEvaluationService.java
@src/main/java/com/softropic/payam/orange/web/OrangeCallbackController.java
@src/main/java/com/softropic/payam/mtn/web/MtnCallbackController.java
@src/test/java/com/softropic/payam/alert/AlertRuleIT.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add callback counters to PaymentMetricsService and both callback controllers</name>
  <files>
    src/main/java/com/softropic/payam/admin/service/PaymentMetricsService.java
    src/main/java/com/softropic/payam/orange/web/OrangeCallbackController.java
    src/main/java/com/softropic/payam/mtn/web/MtnCallbackController.java
  </files>
  <action>
    STEP 1 — PaymentMetricsService.java

    Add two new Counter fields alongside the existing three:

      private final Counter callbackReceivedCounter;
      private final Counter callbackFailedCounter;

    Register them in the constructor immediately after the fraudBlockedCounter registration:

      this.callbackReceivedCounter = Counter.builder("callback.received.total")
              .description("Total inbound provider callbacks received")
              .register(registry);
      this.callbackFailedCounter = Counter.builder("callback.failed.total")
              .description("Total inbound provider callbacks that failed to process (no matching transaction)")
              .register(registry);

    Add two public methods (follow the style of recordSuccess / recordFailed):

      /**
       * Increment the callback received counter. Called by OrangeCallbackController and
       * MtnCallbackController on every accepted inbound callback (after dedup and HMAC checks pass).
       */
      public void recordCallbackReceived() {
          callbackReceivedCounter.increment();
      }

      /**
       * Increment the callback failed counter. Called when a callback cannot be correlated to a
       * known transaction (e.g., OrangeMoneyPort or MtnMoMoPort throws on lookup).
       */
      public void recordCallbackFailed() {
          callbackFailedCounter.increment();
      }

    STEP 2 — OrangeCallbackController.java

    Inject PaymentMetricsService by adding it as a constructor parameter alongside the existing
    four. Spring will auto-wire it.

    In handleCallback(), after the dedup check passes and before calling orangeMoneyPort.processWebhook(),
    call metricsService.recordCallbackReceived().

    Wrap the orangeMoneyPort.processWebhook(payload, notifToken) call in a try/catch(Exception e):
    - On exception: call metricsService.recordCallbackFailed(), log.warn with the exception message,
      and still return ResponseEntity.ok().build() (Orange expects 200 regardless — P1.1 / Pitfall 5).
    - On success: no additional call needed (success is the normal path; received counter is
      already incremented).

    The final flow inside handleCallback() after the dedup block:

      metricsService.recordCallbackReceived();
      try {
          orangeMoneyPort.processWebhook(payload, notifToken);
      } catch (Exception e) {
          log.warn("Orange callback processing failed, payToken={}: {}", payload.getPayToken(), e.getMessage());
          metricsService.recordCallbackFailed();
      }
      return ResponseEntity.ok().build();

    STEP 3 — MtnCallbackController.java

    Inject PaymentMetricsService as a constructor parameter alongside mtnMoMoPort.

    Wrap mtnMoMoPort.processCallback(payload) the same way:

      metricsService.recordCallbackReceived();
      try {
          mtnMoMoPort.processCallback(payload);
      } catch (Exception e) {
          log.warn("MTN callback processing failed: {}", e.getMessage());
          metricsService.recordCallbackFailed();
      }
      return ResponseEntity.ok().build();

    Add a Logger field if one is not already present:
      private static final Logger log = LoggerFactory.getLogger(MtnCallbackController.class);

    STEP 4 — compile check

    Run: mvn compiler:compile -pl . 2>&amp;1 | tail -10
    Fix any compilation errors before proceeding.
  </action>
  <verify>mvn compiler:compile -pl . 2>&amp;1 | grep -E "BUILD|ERROR" | tail -5</verify>
  <done>
    BUILD SUCCESS with zero compilation errors.
    PaymentMetricsService has recordCallbackReceived() and recordCallbackFailed() methods.
    Both callback controllers inject PaymentMetricsService and call recordCallbackReceived() on
    every accepted callback, recordCallbackFailed() when port throws.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement CALLBACK_ANOMALY in AlertEvaluationService and add AlertRuleIT test</name>
  <files>
    src/main/java/com/softropic/payam/alert/service/AlertEvaluationService.java
    src/test/java/com/softropic/payam/alert/AlertRuleIT.java
  </files>
  <action>
    STEP 1 — AlertEvaluationService.java

    Replace the CALLBACK_ANOMALY switch arm (lines 92-94):

      BEFORE:
        case "CALLBACK_ANOMALY" ->
            // Reserved metric name — not yet implemented
            -1.0;

      AFTER:
        case "CALLBACK_ANOMALY" -> {
            double received = getCounter("callback.received.total");
            double failed   = getCounter("callback.failed.total");
            if (received < MINIMUM_SAMPLE_SIZE) yield -1.0;
            yield failed / received;
        }

    No other changes to AlertEvaluationService. The MINIMUM_SAMPLE_SIZE guard (value=10) already
    exists and applies: if fewer than 10 callbacks have been received, the metric returns -1.0
    and no alert fires — prevents false alarms at startup (Pitfall 8).

    STEP 2 — AlertRuleIT.java

    Add a fifth test method test_callbackAnomalyAlertFires() immediately after
    test_noAlertBeforeMinimumSampleSize():

      @Test
      void test_callbackAnomalyAlertFires() {
          // Seed a CALLBACK_ANOMALY rule with threshold=0.30 (30% failed callbacks triggers alert)
          transactionTemplate.execute(status -> {
              jdbc.execute(
                  "INSERT INTO main.alert_rule (id, metric_name, threshold, window_seconds, " +
                  "notification_channel, enabled, status, description) " +
                  "VALUES (104, 'CALLBACK_ANOMALY', 0.30, 300, 'LOG', true, 'ACTIVE', 'Test callback anomaly rule') " +
                  "ON CONFLICT (id) DO UPDATE SET threshold=EXCLUDED.threshold, enabled=EXCLUDED.enabled");
              return null;
          });

          alertRuleCache.refresh();
          eventCaptor.clear();

          // Simulate 12 total callbacks, 5 failed — rate = 5/12 ≈ 0.417 > threshold 0.30
          for (int i = 0; i < 12; i++) {
              meterRegistry.counter("callback.received.total").increment();
          }
          for (int i = 0; i < 5; i++) {
              meterRegistry.counter("callback.failed.total").increment();
          }

          alertEvaluationService.evaluate();

          boolean callbackAlertFired = eventCaptor.getAll().stream()
                  .anyMatch(e -> "CALLBACK_ANOMALY".equals(e.metricName()) && e.actualValue() >= 0.30);
          assertThat(callbackAlertFired)
                  .as("AlertFiredEvent must fire for CALLBACK_ANOMALY when failed/received >= 0.30 with 12 samples")
                  .isTrue();
      }

    Also update test_noAlertBeforeMinimumSampleSize(): the existing test (lines ~318-355) asserts
    that CALLBACK_ANOMALY rules seeded with ids 102 and 103 do NOT fire when the metric returns -1.0.
    After this change, CALLBACK_ANOMALY returns -1.0 only when callback.received.total < 10. The
    existing test increments NO callback counters, so received=0 < 10 — the guard still yields -1.0
    and the assertion still passes unchanged. No modification to that test is needed.

    STEP 3 — run tests

    Run: mvn test -pl . -Dtest=AlertRuleIT 2>&amp;1 | grep -E "Tests run|BUILD|FAILURE|ERROR" | tail -10
  </action>
  <verify>mvn test -pl . -Dtest=AlertRuleIT 2>&amp;1 | grep -E "Tests run|BUILD|FAILURE|ERROR" | tail -10</verify>
  <done>
    AlertRuleIT passes with Tests run: 5, Failures: 0, Errors: 0.
    test_callbackAnomalyAlertFires passes: AlertFiredEvent captured with metricName=CALLBACK_ANOMALY
    and actualValue >= 0.30.
    test_noAlertBeforeMinimumSampleSize still passes: received counter is 0 in that test, so
    CALLBACK_ANOMALY returns -1.0 (Pitfall 8 guard) and no alert fires.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `mvn compiler:compile` exits 0
2. `mvn test -pl . -Dtest=AlertRuleIT` passes 5 tests (4 existing + 1 new)
3. AlertEvaluationService.computeMetricValue("CALLBACK_ANOMALY") reads callback.received.total
   and callback.failed.total counters — no longer returns -1.0 unconditionally
4. Both OrangeCallbackController and MtnCallbackController call
   metricsService.recordCallbackReceived() on every accepted callback
5. A CALLBACK_ANOMALY rule with threshold=0.30 fires when 5/12 callbacks fail (rate 0.417)
</verification>

<success_criteria>
- VERIFICATION.md gap closed: CALLBACK_ANOMALY metric is implemented; a rule with any threshold
  can now fire when failed-callback ratio exceeds it
- Phase 10 truth #2 fully satisfied: "Alert rules fire when failure rate, fraud spike rate, OR
  callback anomaly thresholds are breached"
- AlertRuleIT 5/5 pass (test_callbackAnomalyAlertFires added)
- No regression: existing 4 AlertRuleIT tests still pass
- Pitfall 8 guard preserved: fewer than 10 received callbacks yields -1.0 and no false alarms
</success_criteria>

<output>
After completion, create `.planning/phases/10-operational-hardening/10-04-SUMMARY.md`
</output>

---
phase: 36-reconciliation-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified: []
autonomous: true
requirements:
  - RECON-01
  - RECON-02
must_haves:
  truths:
    - "Full mvn verify passes green — no regressions introduced by the ReconciliationProviderRunner refactor"
    - "All existing reconciliation tests continue to pass: ReconciliationJobIT (original 2 + new 1), ReconciliationApiIT, DailyReconciliationE2ETest (all 4 scenarios), ReconciliationFailedStateIT"
    - "Unit tests ReconciliationProviderRunnerTest (4 tests) pass"
    - "No test in the webhook, payment, fraud, tenant, or audit packages regresses due to the @Transactional removal from ReconciliationService.runForDate()"
    - "36-02-SUMMARY.md records the mvn verify command, timestamp, total test count, pass/fail, and any flakes observed"
  artifacts:
    - path: ".planning/phases/36-reconciliation-hardening/36-02-SUMMARY.md"
      provides: "Regression verification record — commands run, test counts, pass/fail status, any flaky or skipped tests"
      contains: "mvn verify"
  key_links:
    - from: "ReconciliationProviderRunner (created in 36-01)"
      to: "DailyReconciliationE2ETest (existing 4 matched/missing/amount-mismatch/status-mismatch scenarios)"
      via: "full mvn verify runs both"
      pattern: "DailyReconciliationE2ETest"
    - from: "ReconciliationService.runForDate (refactored in 36-01)"
      to: "ReconciliationJob Quartz entry point (unchanged but calls the rewritten service)"
      via: "full mvn verify"
      pattern: "ReconciliationJob"
---

<objective>
Regression verification for Phase 36. Run the full test suite after plan 36-01 lands to prove that the ReconciliationProviderRunner extraction and the removal of @Transactional from ReconciliationService.runForDate() do not break any existing reconciliation, webhook, payment, fraud, tenant, or audit test. This is a verification-only plan with no code changes.

Purpose: Plan 36-01 introduces a new Spring bean, changes transaction boundaries (from one outer tx to N inner REQUIRES_NEW transactions), deletes LedgerSnapshotService, and removes the List-based reconciliation query. Sampling in 36-01 proves the new behaviours but cannot prove the absence of regressions in the broader test suite. A full `./mvnw verify` pass is required before phase sign-off.

Output: `36-02-SUMMARY.md` recording the mvn verify command, timestamp, total test count, pass/fail status, and any flaky or skipped tests observed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-reconciliation-hardening/36-RESEARCH.md
@.planning/phases/36-reconciliation-hardening/36-VALIDATION.md
@.planning/phases/36-reconciliation-hardening/36-01-PLAN.md
@.planning/phases/36-reconciliation-hardening/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full mvn verify regression run</name>
  <files>.planning/phases/36-reconciliation-hardening/36-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/36-reconciliation-hardening/36-01-SUMMARY.md (confirm 36-01 landed green)
    - .planning/phases/36-reconciliation-hardening/36-VALIDATION.md (the sign-off contract)
    - .planning/phases/35-idempotency-correctness/35-02-SUMMARY.md (reference for summary format used in v7 regression plans)
  </read_first>
  <action>
    Step 1 — Verify the working tree is clean and on main: `git status` must show no uncommitted changes from 36-01 (expected: clean).

    Step 2 — Run the full verification suite from the repository root:
    ```bash
    ./mvnw -q verify 2>&amp;1 | tee /tmp/phase-36-02-verify.log
    ```
    Expected: exit 0. Typical runtime: 3-6 minutes on this project (Testcontainers Postgres startup + ~200 tests).

    Step 3 — Inspect the tail of the log for the Maven summary line (should show `BUILD SUCCESS`, total tests run, failures, errors, skipped):
    ```bash
    tail -40 /tmp/phase-36-02-verify.log | grep -E 'Tests run|BUILD'
    ```
    Extract:
    - Total tests run
    - Failures
    - Errors
    - Skipped
    - Build status (SUCCESS/FAILURE)
    - Total elapsed time

    Step 4 — Specifically confirm these test classes all ran and passed (grep the log):
    ```bash
    grep -E 'ReconciliationProviderRunnerTest|ReconciliationJobIT|ReconciliationFailedStateIT|ReconciliationApiIT|DailyReconciliationE2ETest' /tmp/phase-36-02-verify.log
    ```
    Each line must show `Tests run: N, Failures: 0, Errors: 0` (Skipped may be nonzero only if the test class intentionally skips).

    Step 5 — If BUILD FAILURE: do NOT fix anything in this plan. Stop, report the failing test(s) with stack traces, and return a FAILED status to the orchestrator. The fix belongs in 36-01 (the code under test) or a new gap-closure plan — not here.

    Step 6 — Write .planning/phases/36-reconciliation-hardening/36-02-SUMMARY.md with the following sections:
    - `## Command Executed` — the exact `./mvnw -q verify` command and timestamp
    - `## Test Suite Results` — total tests, failures, errors, skipped, elapsed time, BUILD status
    - `## Reconciliation Class Breakdown` — per-class Tests run / Failures / Errors / Skipped for the 5 reconciliation test classes
    - `## Truths Verified` — checklist from must_haves
    - `## Flakes or Anomalies` — any retries, skipped tests, warnings worth noting (empty OK)
    - `## Sign-off` — one-line statement: "Phase 36 is ready for /gsd:verify-work."

    Step 7 — Commit the summary: `docs(36-02): full mvn verify regression passes — phase 36 ready for verify-work`.
  </action>
  <verify>
    <automated>test -f .planning/phases/36-reconciliation-hardening/36-02-SUMMARY.md &amp;&amp; grep -q 'BUILD SUCCESS' .planning/phases/36-reconciliation-hardening/36-02-SUMMARY.md &amp;&amp; grep -q 'ReconciliationProviderRunnerTest' .planning/phases/36-reconciliation-hardening/36-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/36-reconciliation-hardening/36-02-SUMMARY.md exists
    - 36-02-SUMMARY.md contains `./mvnw` and `verify`
    - 36-02-SUMMARY.md contains `BUILD SUCCESS`
    - 36-02-SUMMARY.md contains `ReconciliationProviderRunnerTest`
    - 36-02-SUMMARY.md contains `ReconciliationJobIT`
    - 36-02-SUMMARY.md contains `ReconciliationFailedStateIT`
    - 36-02-SUMMARY.md contains `ReconciliationApiIT`
    - 36-02-SUMMARY.md contains `DailyReconciliationE2ETest`
    - 36-02-SUMMARY.md contains a line matching `Tests run: [0-9]+` with Failures: 0 and Errors: 0
    - 36-02-SUMMARY.md contains `RECON-01` and `RECON-02`
    - 36-02-SUMMARY.md contains the string `ready for` (sign-off statement)
    - Git log shows a commit with message `docs(36-02)` touching only the SUMMARY file
  </acceptance_criteria>
  <done>
    Full regression suite green, sign-off summary committed, phase 36 ready for `/gsd:verify-work`.
  </done>
</task>

</tasks>

<verification>
- `./mvnw -q verify` exits 0
- 36-02-SUMMARY.md committed and contains BUILD SUCCESS + per-class reconciliation breakdown
- No code file modified in this plan (files_modified: [] — verified by `git diff --name-only HEAD~1 HEAD` showing only the SUMMARY file)
</verification>

<success_criteria>
1. Full `./mvnw verify` passes green.
2. All 5 reconciliation test classes pass (ReconciliationProviderRunnerTest, ReconciliationJobIT, ReconciliationFailedStateIT, ReconciliationApiIT, DailyReconciliationE2ETest).
3. No regression in webhook, payment, fraud, tenant, audit, or security test packages.
4. 36-02-SUMMARY.md committed with full test breakdown and sign-off line.
</success_criteria>

<output>
After completion, `.planning/phases/36-reconciliation-hardening/36-02-SUMMARY.md` exists and is committed. Phase 36 is ready for `/gsd:verify-work`.
</output>

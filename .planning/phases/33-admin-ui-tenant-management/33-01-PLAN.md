---
phase: 33-admin-ui-tenant-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/softropic/payam/tenant/contract/TenantSummaryDto.java
  - src/main/java/com/softropic/payam/tenant/contract/ApiKeySummaryDto.java
  - src/main/java/com/softropic/payam/tenant/service/TenantQueryService.java
  - src/main/java/com/softropic/payam/tenant/api/TenantAdminResource.java
  - src/test/java/com/softropic/payam/tenant/TenantAdminResourceIT.java
autonomous: true
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "Admin can generate an API key for an environment with no active key and receives the raw key exactly once"
    - "Tenant list API returns each tenant's email address and creation timestamp"
    - "Tenant detail API returns each key's creation timestamp"
  artifacts:
    - path: "src/main/java/com/softropic/payam/tenant/contract/TenantSummaryDto.java"
      provides: "TenantSummaryDto with email and createdAt fields"
      contains: "String email"
    - path: "src/main/java/com/softropic/payam/tenant/contract/ApiKeySummaryDto.java"
      provides: "ApiKeySummaryDto with createdAt field"
      contains: "Instant createdAt"
    - path: "src/main/java/com/softropic/payam/tenant/api/TenantAdminResource.java"
      provides: "POST generate endpoint"
      contains: "keys/generate"
  key_links:
    - from: "TenantAdminResource.java"
      to: "ApiKeyService.generateAndStore"
      via: "method call in generateKey endpoint"
      pattern: "apiKeyService\\.generateAndStore"
    - from: "TenantQueryService.java"
      to: "TenantSummaryDto"
      via: "mapper in findAll method"
      pattern: "new TenantSummaryDto.*getEmail.*getCreatedDate"
---

<objective>
Add three backend changes required by the UI: (1) a new POST /keys/generate endpoint that wraps ApiKeyService.generateAndStore, (2) email + createdAt fields on TenantSummaryDto, (3) createdAt field on ApiKeySummaryDto. These are prerequisites for the tenant list columns and the key table Generate button.

Purpose: The UI spec requires Email, Created columns on the list page and a Created Date column + Generate button in the key table. These DTO fields and endpoint do not yet exist.
Output: Updated DTOs, updated TenantQueryService mapper, new endpoint on TenantAdminResource, integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-admin-ui-tenant-management/33-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DTO fields and update TenantQueryService mapper</name>
  <files>
    src/main/java/com/softropic/payam/tenant/contract/TenantSummaryDto.java,
    src/main/java/com/softropic/payam/tenant/contract/ApiKeySummaryDto.java,
    src/main/java/com/softropic/payam/tenant/service/TenantQueryService.java
  </files>
  <read_first>
    src/main/java/com/softropic/payam/tenant/contract/TenantSummaryDto.java,
    src/main/java/com/softropic/payam/tenant/contract/ApiKeySummaryDto.java,
    src/main/java/com/softropic/payam/tenant/service/TenantQueryService.java,
    src/main/java/com/softropic/payam/common/persistence/AbstractAuditingEntity.java,
    src/main/java/com/softropic/payam/tenant/repo/Tenant.java,
    src/main/java/com/softropic/payam/tenant/repo/TenantApiKey.java
  </read_first>
  <action>
1. Update `TenantSummaryDto` record to add two fields. The new signature must be:
   ```java
   public record TenantSummaryDto(Long id, String tenantRef, String name, TenantStatus tenantStatus, String email, Instant createdAt) {}
   ```
   Add `import java.time.Instant;` to the file.

2. Update `ApiKeySummaryDto` record to add one field. The new signature must be:
   ```java
   public record ApiKeySummaryDto(Long id, String keyPrefix, ApiKeyEnvironment environment, ApiKeyStatus keyStatus, Instant createdAt) {}
   ```
   Add `import java.time.Instant;` to the file.

3. Update `TenantQueryService.findAll()` mapper (line 40) to pass the two new fields. The entity's timestamp is `t.getCreatedDate()` (inherited from `AbstractAuditingEntity`). The email is `t.getEmail()`. Change:
   ```java
   return tenants.map(t -> new TenantSummaryDto(t.getId(), t.getTenantRef(), t.getName(), t.getTenantStatus(), t.getEmail(), t.getCreatedDate()));
   ```

4. Update `TenantQueryService.findByTenantRef()` mapper (line 48) for ApiKeySummaryDto to pass `k.getCreatedDate()`:
   ```java
   .map(k -> new ApiKeySummaryDto(k.getId(), k.getKeyPrefix(), k.getEnvironment(), k.getKeyStatus(), k.getCreatedDate()))
   ```

Note: `getCreatedDate()` returns `Instant` from `AbstractAuditingEntity` — both `Tenant` and `TenantApiKey` extend it.
  </action>
  <verify>
    <automated>cd /Users/mokwen/dev/gitrepos/bluegithub/payam && grep -c 'String email, Instant createdAt' src/main/java/com/softropic/payam/tenant/contract/TenantSummaryDto.java && grep -c 'Instant createdAt' src/main/java/com/softropic/payam/tenant/contract/ApiKeySummaryDto.java && grep -c 'getEmail.*getCreatedDate' src/main/java/com/softropic/payam/tenant/service/TenantQueryService.java</automated>
  </verify>
  <acceptance_criteria>
    - TenantSummaryDto record has exactly 6 fields: id, tenantRef, name, tenantStatus, email, createdAt
    - ApiKeySummaryDto record has exactly 5 fields: id, keyPrefix, environment, keyStatus, createdAt
    - TenantQueryService.findAll mapper passes t.getEmail() and t.getCreatedDate() to TenantSummaryDto constructor
    - TenantQueryService.findByTenantRef mapper passes k.getCreatedDate() to ApiKeySummaryDto constructor
    - Both DTO files import java.time.Instant
  </acceptance_criteria>
  <done>TenantSummaryDto has email and createdAt fields; ApiKeySummaryDto has createdAt field; TenantQueryService populates all new fields from entity getters</done>
</task>

<task type="auto">
  <name>Task 2: Add POST /keys/generate endpoint and integration test</name>
  <files>
    src/main/java/com/softropic/payam/tenant/api/TenantAdminResource.java,
    src/test/java/com/softropic/payam/tenant/TenantAdminResourceIT.java
  </files>
  <read_first>
    src/main/java/com/softropic/payam/tenant/api/TenantAdminResource.java,
    src/main/java/com/softropic/payam/tenant/service/ApiKeyService.java,
    src/main/java/com/softropic/payam/tenant/repo/TenantRepository.java,
    src/main/java/com/softropic/payam/tenant/contract/ApiKeyDto.java,
    src/main/java/com/softropic/payam/tenant/contract/ApiKeyEnvironment.java,
    src/test/java/com/softropic/payam/tenant/TenantAdminResourceIT.java
  </read_first>
  <action>
1. Add a new endpoint method to `TenantAdminResource.java`. Place it after the `reactivateKey` method (line 123) and before the `updateName` method (line 126). The method must:
   - Path: `@PostMapping("/{tenantRef}/keys/generate")`
   - Annotation: `@PreAuthorize(SecurityConstants.HAS_ADMIN_ROLE)`
   - Accept `@RequestParam ApiKeyEnvironment env` query parameter
   - Look up the tenant via `tenantQueryService.findByTenantRef(tenantRef)` — but we need the entity, not the DTO. Instead, inject `TenantRepository` and use `tenantRepository.findByTenantRef(tenantRef).orElseThrow(() -> new EntityNotFoundException("Tenant not found: " + tenantRef))`.
   - Call `apiKeyService.generateAndStore(tenant, env)` which returns `ApiKeyService.ApiKeyAndRawKey`
   - Return `ApiKeyDto` constructed the same way as `rotateKey` does: `new ApiKeyDto(result.entity().getId(), result.entity().getKeyPrefix(), result.entity().getEnvironment(), result.rawKey())`
   - Return type: `ApiKeyDto`

   Full method:
   ```java
   @PostMapping("/{tenantRef}/keys/generate")
   @PreAuthorize(SecurityConstants.HAS_ADMIN_ROLE)
   public ApiKeyDto generateKey(@PathVariable String tenantRef,
                                @RequestParam ApiKeyEnvironment env) {
       Tenant tenant = tenantRepository.findByTenantRef(tenantRef)
           .orElseThrow(() -> new EntityNotFoundException("Tenant not found: " + tenantRef));
       ApiKeyService.ApiKeyAndRawKey result = apiKeyService.generateAndStore(tenant, env);
       return new ApiKeyDto(
           result.entity().getId(),
           result.entity().getKeyPrefix(),
           result.entity().getEnvironment(),
           result.rawKey()
       );
   }
   ```

2. Add `TenantRepository` to the constructor injection. The existing constructor takes `(TenantService, ApiKeyService, TenantQueryService)`. Add `TenantRepository tenantRepository` as a 4th parameter and a `private final TenantRepository tenantRepository` field. Add the import: `import com.softropic.payam.tenant.repo.TenantRepository;` and `import com.softropic.payam.tenant.repo.Tenant;` and `import jakarta.persistence.EntityNotFoundException;`.

3. Add integration test in `TenantAdminResourceIT.java`. Follow the existing test pattern (e.g., `rotateKey_returns200_withNewRawKey`). Add two tests:

   a. `generateKey_returns200_withNewRawKey()`:
      - Create a tenant via `tenantService.createTenant("Generate Key Corp", ApiKeyEnvironment.PROD)` — this creates an initial PROD key
      - Revoke the PROD key via `apiKeyService.revoke(result.key().getId())` — now PROD has no active key
      - POST to `/v1/admin/tenants/{tenantRef}/keys/generate?env=PROD`
      - Assert 200, response body has `rawKey` not null, `environment` is `PROD`

   b. `generateKey_duplicateActiveKey_returns409()`:
      - Create a tenant (gets PROD key automatically)
      - POST to `/v1/admin/tenants/{tenantRef}/keys/generate?env=PROD` without revoking
      - Assert 409 Conflict (IllegalStateException mapped to 409 by ApiAdvice)

4. Run the full test class to ensure nothing is broken by the DTO field additions.
  </action>
  <verify>
    <automated>cd /Users/mokwen/dev/gitrepos/bluegithub/payam && ./mvnw test -pl . -Dtest=TenantAdminResourceIT -Dspring.profiles.active=dev -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TenantAdminResource has a method annotated @PostMapping("/{tenantRef}/keys/generate")
    - The method has @PreAuthorize(SecurityConstants.HAS_ADMIN_ROLE)
    - The method accepts @RequestParam ApiKeyEnvironment env
    - The method returns ApiKeyDto with rawKey populated
    - TenantRepository is injected into TenantAdminResource constructor
    - Integration test generateKey_returns200_withNewRawKey passes
    - Integration test generateKey_duplicateActiveKey_returns409 passes
    - All existing TenantAdminResourceIT tests still pass
  </acceptance_criteria>
  <done>POST /v1/admin/tenants/{tenantRef}/keys/generate?env={env} endpoint works, returns ApiKeyDto with rawKey; duplicate active key returns 409; all integration tests pass</done>
</task>

</tasks>

<verification>
- All existing TenantAdminResourceIT tests pass (no regression from DTO field additions)
- New generate endpoint tests pass
- `grep 'keys/generate' src/main/java/com/softropic/payam/tenant/api/TenantAdminResource.java` returns a match
- `grep 'Instant createdAt' src/main/java/com/softropic/payam/tenant/contract/TenantSummaryDto.java` returns a match
- `grep 'Instant createdAt' src/main/java/com/softropic/payam/tenant/contract/ApiKeySummaryDto.java` returns a match
</verification>

<success_criteria>
TenantSummaryDto includes email and createdAt, ApiKeySummaryDto includes createdAt, generate endpoint returns ApiKeyDto with rawKey, all integration tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/33-admin-ui-tenant-management/33-01-SUMMARY.md`
</output>

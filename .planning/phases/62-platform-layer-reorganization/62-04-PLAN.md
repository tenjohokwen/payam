---
phase: 62-platform-layer-reorganization
plan: 04
type: execute
wave: 3
depends_on: [62-03]
files_modified:
  - src/main/java/com/softropic/payam/tenant/**/*.java
  - src/main/java/com/softropic/payam/platform/tenant/**/*.java
  - src/test/java/com/softropic/payam/tenant/**/*.java
  - src/test/java/com/softropic/payam/platform/tenant/**/*.java
  - src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java
  - src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java
autonomous: true
requirements: [PLAT-01]

must_haves:
  truths:
    - "platform.tenant package contains 24 production files (api, config, contract, contract/event, repo, service sub-packages)"
    - "platform.tenant package contains 9 test files (formerly under tenant/)"
    - "Old tenant/ source directory is deleted from both src/main and src/test"
    - "RotatedKeyCleanupSchedulerConfig moved to platform.tenant.config (per STATE.md decision — Quartz scheduler config moves with tenant package)"
    - "infrastructure.web.ApiKeyAuthenticationFilter and infrastructure.web.TenantSecurityConfig imports updated to import from platform.tenant.* (4 import lines)"
    - "75 caller files outside tenant (8 production + 67 test) updated to import from platform.tenant.*"
    - "RotatedKeyCleanupJobIT integration test passes from new platform.tenant location"
    - "mvn verify exits 0 with no regressions vs Phase 61 baseline"
  artifacts:
    - path: "src/main/java/com/softropic/payam/platform/tenant/contract/TenantPrincipal.java"
      provides: "Tenant principal contract under new package"
      contains: "package com.softropic.payam.platform.tenant.contract;"
    - path: "src/main/java/com/softropic/payam/platform/tenant/contract/TenantStatus.java"
      provides: "Tenant status enum under new package"
      contains: "package com.softropic.payam.platform.tenant.contract;"
    - path: "src/main/java/com/softropic/payam/platform/tenant/repo/Tenant.java"
      provides: "Tenant JPA entity under new package"
      contains: "package com.softropic.payam.platform.tenant.repo;"
    - path: "src/main/java/com/softropic/payam/platform/tenant/repo/TenantApiKey.java"
      provides: "Tenant API key JPA entity under new package"
      contains: "package com.softropic.payam.platform.tenant.repo;"
    - path: "src/main/java/com/softropic/payam/platform/tenant/service/ApiKeyService.java"
      provides: "API key service under new package"
      contains: "package com.softropic.payam.platform.tenant.service;"
    - path: "src/main/java/com/softropic/payam/platform/tenant/api/TenantAdminResource.java"
      provides: "Tenant admin REST resource under new package"
      contains: "package com.softropic.payam.platform.tenant.api;"
    - path: "src/main/java/com/softropic/payam/platform/tenant/config/RotatedKeyCleanupSchedulerConfig.java"
      provides: "Quartz cleanup scheduler config under new package (moved per STATE.md decision)"
      contains: "package com.softropic.payam.platform.tenant.config;"
  key_links:
    - from: "infrastructure.web.ApiKeyAuthenticationFilter"
      to: "platform.tenant.contract.TenantPrincipal, TenantStatus; platform.tenant.repo.TenantApiKey; platform.tenant.service.ApiKeyService"
      via: "import statements (4 lines)"
      pattern: "import com.softropic.payam.platform.tenant\\."
    - from: "infrastructure.web.TenantSecurityConfig"
      to: "platform.tenant.* (any imports if present)"
      via: "import statements"
      pattern: "import com.softropic.payam.platform.tenant\\."
---

<objective>
Move all 24 production files and 9 test files in the `tenant/` package into `platform.tenant/`, preserving sub-package structure (`api/`, `config/`, `contract/`, `contract/event/`, `repo/`, `service/`). Update 8 production and 67 test caller files outside `tenant/` (largest test-layer impact in this phase). Critically, update `infrastructure.web.ApiKeyAuthenticationFilter` and `infrastructure.web.TenantSecurityConfig` (moved in Phase 61) which import from `tenant.*`.

Purpose: PLAT-01 requirement — relocate `tenant` package to `platform.tenant`. STATE.md confirms `RotatedKeyCleanupSchedulerConfig` (Quartz config that stayed in `tenant.config` during Phase 61) moves with the rest of the package now.

Output: Populated `platform.tenant` package; old `tenant/` directory deleted; infrastructure.web cascade updated; `mvn verify` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-platform-layer-reorganization/62-RESEARCH.md
@.planning/phases/61-infrastructure-layer-creation/61-03-SUMMARY.md

<interfaces>
Source files being moved (24 production):
  - tenant/api/TenantAdminResource.java
  - tenant/config/RotatedKeyCleanupSchedulerConfig.java
  - tenant/contract/ApiKeyDto.java, ApiKeyEnvironment.java, ApiKeyStatus.java, ApiKeySummaryDto.java, TenantDetailDto.java, TenantDto.java, TenantPrincipal.java, TenantStatus.java, TenantSummaryDto.java, WebhookSecretDto.java
  - tenant/contract/event/TenantApiKeyEvent.java, TenantCreatedEvent.java, TenantStatusChangedEvent.java, TenantWebhookSecretRegeneratedEvent.java
  - tenant/repo/Tenant.java, TenantApiKey.java, TenantApiKeyRepository.java, TenantRepository.java
  - tenant/service/ApiKeyService.java, RotatedKeyCleanupJob.java, TenantQueryService.java, TenantService.java

Test files being moved (9 from src/test/java/com/softropic/payam/tenant/):
  - ApiKeyConcurrentRotationIT.java
  - RotatedKeyCleanupJobIT.java
  - TenantAdminResourceIT.java
  - TenantAuditIT.java
  - TenantContextExceptionIT.java
  - TenantFilterChainIT.java
  - TenantProvisioningIT.java
  - TenantServiceIT.java
  - service/ApiKeyServiceReactivateTest.java

External callers (8 production files, 67 test files — verified by `grep -rl "com.softropic.payam.tenant\." src --include="*.java" | grep -v "/tenant/"`):
Production callers include:
  - src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java (imports tenant.contract.TenantPrincipal, TenantStatus; tenant.repo.TenantApiKey; tenant.service.ApiKeyService)
  - src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java (any tenant imports)
  - webhook/, disbursement/, payment/, fraud/ classes that resolve TenantPrincipal or use tenant repo
Test callers (67) span e2e/, integration/, builder/ across the entire test suite.

CRITICAL infrastructure.web cascade (from research Pattern 2):
  - infrastructure/web/ApiKeyAuthenticationFilter.java imports the following 4 tenant paths that must update:
    - import com.softropic.payam.tenant.contract.TenantPrincipal;  →  import com.softropic.payam.platform.tenant.contract.TenantPrincipal;
    - import com.softropic.payam.tenant.contract.TenantStatus;     →  import com.softropic.payam.platform.tenant.contract.TenantStatus;
    - import com.softropic.payam.tenant.repo.TenantApiKey;          →  import com.softropic.payam.platform.tenant.repo.TenantApiKey;
    - import com.softropic.payam.tenant.service.ApiKeyService;      →  import com.softropic.payam.platform.tenant.service.ApiKeyService;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move tenant production and test files into platform.tenant (preserve sub-package structure including config/)</name>
  <files>
    src/main/java/com/softropic/payam/platform/tenant/** (24 new production files),
    src/test/java/com/softropic/payam/platform/tenant/** (9 new test files),
    src/main/java/com/softropic/payam/tenant/** (delete all),
    src/test/java/com/softropic/payam/tenant/** (delete all)
  </files>
  <read_first>
    src/main/java/com/softropic/payam/tenant/api/TenantAdminResource.java,
    src/main/java/com/softropic/payam/tenant/contract/TenantPrincipal.java,
    src/main/java/com/softropic/payam/tenant/contract/TenantStatus.java,
    src/main/java/com/softropic/payam/tenant/repo/TenantApiKey.java,
    src/main/java/com/softropic/payam/tenant/service/ApiKeyService.java,
    src/main/java/com/softropic/payam/tenant/config/RotatedKeyCleanupSchedulerConfig.java,
    .planning/phases/62-platform-layer-reorganization/62-RESEARCH.md
  </read_first>
  <action>
    Create `src/main/java/com/softropic/payam/platform/tenant/` and `src/test/java/com/softropic/payam/platform/tenant/` directories. Move all files preserving sub-package structure:

    Production moves (24 files):
    - tenant/api/TenantAdminResource.java becomes platform/tenant/api/TenantAdminResource.java (change package to com.softropic.payam.platform.tenant.api)
    - tenant/config/RotatedKeyCleanupSchedulerConfig.java becomes platform/tenant/config/RotatedKeyCleanupSchedulerConfig.java (change package to com.softropic.payam.platform.tenant.config) — STATE.md confirmed this Quartz config moves with tenant in Phase 62
    - tenant/contract/ApiKeyDto.java becomes platform/tenant/contract/ApiKeyDto.java (change package to com.softropic.payam.platform.tenant.contract)
    - Repeat for all contract/* files: ApiKeyEnvironment.java, ApiKeyStatus.java, ApiKeySummaryDto.java, TenantDetailDto.java, TenantDto.java, TenantPrincipal.java, TenantStatus.java, TenantSummaryDto.java, WebhookSecretDto.java
    - tenant/contract/event/TenantApiKeyEvent.java becomes platform/tenant/contract/event/TenantApiKeyEvent.java (change package to com.softropic.payam.platform.tenant.contract.event)
    - Repeat for all contract/event/* files: TenantCreatedEvent.java, TenantStatusChangedEvent.java, TenantWebhookSecretRegeneratedEvent.java
    - tenant/repo/Tenant.java becomes platform/tenant/repo/Tenant.java (change package to com.softropic.payam.platform.tenant.repo)
    - Repeat for all repo/* files: TenantApiKey.java, TenantApiKeyRepository.java, TenantRepository.java
    - tenant/service/ApiKeyService.java becomes platform/tenant/service/ApiKeyService.java (change package to com.softropic.payam.platform.tenant.service)
    - Repeat for all service/* files: RotatedKeyCleanupJob.java, TenantQueryService.java, TenantService.java

    Test moves (9 files):
    - tenant/ApiKeyConcurrentRotationIT.java becomes platform/tenant/ApiKeyConcurrentRotationIT.java (change package to com.softropic.payam.platform.tenant)
    - Repeat for: RotatedKeyCleanupJobIT.java, TenantAdminResourceIT.java, TenantAuditIT.java, TenantContextExceptionIT.java, TenantFilterChainIT.java, TenantProvisioningIT.java, TenantServiceIT.java
    - tenant/service/ApiKeyServiceReactivateTest.java becomes platform/tenant/service/ApiKeyServiceReactivateTest.java (change package to com.softropic.payam.platform.tenant.service)

    Within-package import rewrite (inside the moved files):
    - In every moved file, replace `import com.softropic.payam.tenant.` with `import com.softropic.payam.platform.tenant.` (preserving sub-package suffix). For example, `tenant.contract.TenantPrincipal` becomes `platform.tenant.contract.TenantPrincipal`.
    - Preserve all other imports byte-for-byte (including any imports from infrastructure.persistence, infrastructure.web, security.*, common.*, platform.notification.*, platform.admin.* that were updated by earlier plans).

    @Component bean name preservation: RotatedKeyCleanupJob and AuditingDateTimeProvider use bean names by convention. Do NOT modify any @Component, @Bean, @Qualifier annotations or their string values.

    Annotation preservation: All Spring (@Service, @Component, @RestController, @Configuration, @Bean), JPA (@Entity, @Table, @Id, @MappedSuperclass), and Quartz annotations stay byte-for-byte identical.

    Directory deletion: After moves, delete `src/main/java/com/softropic/payam/tenant/` (entire tree) and `src/test/java/com/softropic/payam/tenant/` (entire tree). Verify each is empty before deletion using `find tenant/ -type f`.

    Do NOT update external callers in this task — that is Task 2.
  </action>
  <verify>
    <automated>[ "$(find src/main/java/com/softropic/payam/platform/tenant -name '*.java' | wc -l | tr -d ' ')" = "24" ] && [ "$(find src/test/java/com/softropic/payam/platform/tenant -name '*.java' | wc -l | tr -d ' ')" = "9" ] && ! test -d src/main/java/com/softropic/payam/tenant && ! test -d src/test/java/com/softropic/payam/tenant && test -f src/main/java/com/softropic/payam/platform/tenant/config/RotatedKeyCleanupSchedulerConfig.java && grep -q "^package com.softropic.payam.platform.tenant.contract;" src/main/java/com/softropic/payam/platform/tenant/contract/TenantPrincipal.java</automated>
  </verify>
  <acceptance_criteria>
    - `find src/main/java/com/softropic/payam/platform/tenant -name '*.java' | wc -l` outputs `24`
    - `find src/test/java/com/softropic/payam/platform/tenant -name '*.java' | wc -l` outputs `9`
    - `test -d src/main/java/com/softropic/payam/tenant` returns false
    - `test -d src/test/java/com/softropic/payam/tenant` returns false
    - `test -f src/main/java/com/softropic/payam/platform/tenant/config/RotatedKeyCleanupSchedulerConfig.java` returns true (Quartz config moved per STATE.md decision)
    - `head -1 src/main/java/com/softropic/payam/platform/tenant/contract/TenantPrincipal.java` outputs `package com.softropic.payam.platform.tenant.contract;`
    - `head -1 src/main/java/com/softropic/payam/platform/tenant/contract/TenantStatus.java` outputs `package com.softropic.payam.platform.tenant.contract;`
    - `head -1 src/main/java/com/softropic/payam/platform/tenant/repo/Tenant.java` outputs `package com.softropic.payam.platform.tenant.repo;`
    - `head -1 src/main/java/com/softropic/payam/platform/tenant/repo/TenantApiKey.java` outputs `package com.softropic.payam.platform.tenant.repo;`
    - `head -1 src/main/java/com/softropic/payam/platform/tenant/service/ApiKeyService.java` outputs `package com.softropic.payam.platform.tenant.service;`
    - `head -1 src/main/java/com/softropic/payam/platform/tenant/config/RotatedKeyCleanupSchedulerConfig.java` outputs `package com.softropic.payam.platform.tenant.config;`
    - `head -1 src/main/java/com/softropic/payam/platform/tenant/api/TenantAdminResource.java` outputs `package com.softropic.payam.platform.tenant.api;`
    - `head -1 src/main/java/com/softropic/payam/platform/tenant/contract/event/TenantCreatedEvent.java` outputs `package com.softropic.payam.platform.tenant.contract.event;`
    - `grep -q "@Configuration" src/main/java/com/softropic/payam/platform/tenant/config/RotatedKeyCleanupSchedulerConfig.java` returns 0 (Spring annotation preserved)
    - `head -1 src/test/java/com/softropic/payam/platform/tenant/RotatedKeyCleanupJobIT.java` outputs `package com.softropic.payam.platform.tenant;`
  </acceptance_criteria>
  <done>
    All 24 production + 9 test files moved into platform.tenant; sub-package structure preserved (api, config, contract, contract/event, repo, service); package declarations updated; within-package imports rewritten; old tenant/ directories deleted from src/main and src/test.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update infrastructure.web cascade and 75 external callers, run mvn verify</name>
  <files>
    src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java,
    src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java,
    All other .java files under src/ that import com.softropic.payam.tenant.* — discovered via grep
  </files>
  <read_first>
    src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java,
    src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java
  </read_first>
  <action>
    1. Discover all stale callers: run `grep -rln "com.softropic.payam.tenant\." src --include="*.java"` — expected ~75 files (8 prod + 67 test, plus any infrastructure.web files that have tenant imports).

    2. For every file in that list, replace EVERY occurrence of `com.softropic.payam.tenant.` with `com.softropic.payam.platform.tenant.` (preserve sub-package path). This applies to both `import` statements and any fully-qualified-name references in code (e.g., `@Autowired com.softropic.payam.tenant.service.ApiKeyService` becomes `@Autowired com.softropic.payam.platform.tenant.service.ApiKeyService`).

    3. CRITICAL — verify infrastructure.web cascade explicitly:
       - `src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java` MUST contain these exact import lines after the rewrite:
         - `import com.softropic.payam.platform.tenant.contract.TenantPrincipal;`
         - `import com.softropic.payam.platform.tenant.contract.TenantStatus;`
         - `import com.softropic.payam.platform.tenant.repo.TenantApiKey;`
         - `import com.softropic.payam.platform.tenant.service.ApiKeyService;`
       - `src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java` MUST contain corresponding `platform.tenant.*` imports for any tenant references it had.

    4. DO NOT modify any other code — only `import` statements and FQN references. Class bodies, method signatures, annotations, and bean names stay identical.

    5. Verify and run build:
       - `grep -rn "com.softropic.payam.tenant\." src --include="*.java"` must return zero matches.
       - `mvn -q clean compile` must exit 0.
       - `mvn -q test-compile` must exit 0.
       - `mvn verify` must match Phase 61 baseline (green if Docker available; otherwise pre-existing 33 integration test failures only).

    6. Specific test integrity checks:
       - `RotatedKeyCleanupJobIT` test class can locate the moved scheduler config.
       - `TenantFilterChainIT` test class still finds infrastructure.web filters with updated tenant imports.

    Commit message: `refactor(62-04): PLAT-01 — move tenant to platform.tenant with infrastructure.web cascade`.
  </action>
  <verify>
    <automated>[ "$(grep -rn 'com.softropic.payam.tenant\.' src --include='*.java' | wc -l | tr -d ' ')" = "0" ] && grep -q "import com.softropic.payam.platform.tenant.contract.TenantPrincipal;" src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java && grep -q "import com.softropic.payam.platform.tenant.contract.TenantStatus;" src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java && grep -q "import com.softropic.payam.platform.tenant.repo.TenantApiKey;" src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java && grep -q "import com.softropic.payam.platform.tenant.service.ApiKeyService;" src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java && mvn -q clean compile && mvn -q test-compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "com.softropic.payam.tenant\." src --include="*.java"` returns no matches (zero stale imports)
    - `grep -q "import com.softropic.payam.platform.tenant.contract.TenantPrincipal;" src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java` returns 0
    - `grep -q "import com.softropic.payam.platform.tenant.contract.TenantStatus;" src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java` returns 0
    - `grep -q "import com.softropic.payam.platform.tenant.repo.TenantApiKey;" src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java` returns 0
    - `grep -q "import com.softropic.payam.platform.tenant.service.ApiKeyService;" src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java` returns 0
    - `mvn -q clean compile` exits 0
    - `mvn -q test-compile` exits 0
    - `mvn verify` exits 0 if Docker available, OR matches Phase 61 baseline (33 known Testcontainers failures only — no NEW failures attributable to this plan)
  </acceptance_criteria>
  <done>
    Zero stale tenant.* imports anywhere in src/. infrastructure.web cascade verified explicitly. Compilation green. Verify matches baseline.
  </done>
</task>

</tasks>

<verification>
After both tasks: `find src -type d -name "tenant" -path "*/com/softropic/payam/tenant"` returns nothing. `grep -rn "com.softropic.payam.tenant\." src --include="*.java"` returns zero matches. infrastructure.web filter files explicitly import from platform.tenant.
</verification>

<success_criteria>
- 24 production files and 9 test files relocated under `platform/tenant/` with preserved sub-package structure (api, config, contract, contract/event, repo, service).
- `RotatedKeyCleanupSchedulerConfig` (Quartz scheduler) successfully moved to `platform.tenant.config` (per STATE.md decision deferred from Phase 61).
- Old `tenant/` directories absent from `src/main/java` and `src/test/java`.
- `infrastructure.web.ApiKeyAuthenticationFilter` and `infrastructure.web.TenantSecurityConfig` explicitly updated with `platform.tenant.*` imports.
- All 75 external callers (8 prod + 67 test) updated.
- `mvn verify` matches Phase 61 baseline (green if Docker available).
- PLAT-01 requirement satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/62-platform-layer-reorganization/62-04-SUMMARY.md` capturing the move details: file counts, infrastructure.web cascade lines updated, RotatedKeyCleanupSchedulerConfig relocation note, and any deviations.
</output>

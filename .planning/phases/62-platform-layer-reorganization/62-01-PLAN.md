---
phase: 62-platform-layer-reorganization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/softropic/payam/health/MtnPlatformHealthIndicator.java
  - src/main/java/com/softropic/payam/health/OrangePlatformHealthIndicator.java
  - src/main/java/com/softropic/payam/ops/TlsStartupAssertion.java
  - src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java
  - src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java
  - src/main/java/com/softropic/payam/platform/monitoring/TlsStartupAssertion.java
  - src/test/java/com/softropic/payam/ops/OperationalIT.java
  - src/test/java/com/softropic/payam/platform/monitoring/OperationalIT.java
autonomous: true
requirements: [PLAT-04]

must_haves:
  truths:
    - "platform.monitoring package contains 3 production classes (Mtn + Orange health indicators, TlsStartupAssertion)"
    - "Old health/ and ops/ source directories are empty (deleted)"
    - "Spring Boot Actuator picks up MtnPlatformHealthIndicator and OrangePlatformHealthIndicator beans by type — health endpoint still functional"
    - "OperationalIT integration test passes from new platform.monitoring location"
    - "mvn verify exits 0 with no regressions"
  artifacts:
    - path: "src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java"
      provides: "MTN provider health indicator under new platform.monitoring package"
      contains: "package com.softropic.payam.platform.monitoring;"
    - path: "src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java"
      provides: "Orange provider health indicator under new platform.monitoring package"
      contains: "package com.softropic.payam.platform.monitoring;"
    - path: "src/main/java/com/softropic/payam/platform/monitoring/TlsStartupAssertion.java"
      provides: "TLS startup assertion under new platform.monitoring package"
      contains: "package com.softropic.payam.platform.monitoring;"
    - path: "src/test/java/com/softropic/payam/platform/monitoring/OperationalIT.java"
      provides: "Operational integration test under new platform.monitoring test package"
      contains: "package com.softropic.payam.platform.monitoring;"
  key_links:
    - from: "platform.monitoring.MtnPlatformHealthIndicator"
      to: "platform.service.PlatformConfigService (still in flat platform package — moves in Plan 03/PLAT-05)"
      via: "@Autowired constructor parameter"
      pattern: "import com.softropic.payam.platform.(contract|service)\\."
---

<objective>
Move 3 production classes (`MtnPlatformHealthIndicator`, `OrangePlatformHealthIndicator`, `TlsStartupAssertion`) from `health/` and `ops/` flat packages into a new merged `platform.monitoring/` package. Move 1 test class (`OperationalIT`) accordingly.

Purpose: PLAT-04 requirement — collapse `health` and `ops` into a single bounded `platform.monitoring` sub-package. This is the smallest wave (4 files, zero external callers outside the moving packages), making it the safest first move.

Output: New `platform.monitoring` package populated; `health/` and `ops/` directories deleted; `mvn verify` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-platform-layer-reorganization/62-RESEARCH.md
@.planning/phases/61-infrastructure-layer-creation/61-01-SUMMARY.md
@.planning/phases/61-infrastructure-layer-creation/61-03-SUMMARY.md

<interfaces>
<!-- Source classes being moved (currently at flat package paths) -->

src/main/java/com/softropic/payam/health/MtnPlatformHealthIndicator.java:
```java
package com.softropic.payam.health;

import com.softropic.payam.platform.contract.PlatformConfigDto;
import com.softropic.payam.platform.service.PlatformConfigService;

@Component
public class MtnPlatformHealthIndicator implements HealthIndicator { ... }
```

src/main/java/com/softropic/payam/health/OrangePlatformHealthIndicator.java:
```java
package com.softropic.payam.health;

import com.softropic.payam.platform.contract.PlatformConfigDto;
import com.softropic.payam.platform.service.PlatformConfigService;

@Component
public class OrangePlatformHealthIndicator implements HealthIndicator { ... }
```

src/main/java/com/softropic/payam/ops/TlsStartupAssertion.java:
```java
package com.softropic.payam.ops;

@Component
public class TlsStartupAssertion implements ApplicationRunner { ... }
```

src/test/java/com/softropic/payam/ops/OperationalIT.java:
```java
package com.softropic.payam.ops;

// imports com.softropic.payam.config.TestConfig (no change needed — test config stays)
```

<!--
External caller surface (verified via grep -rl):
- ZERO production files outside health/ and ops/ import com.softropic.payam.health.* or com.softropic.payam.ops.*
- ZERO test files outside ops/ import com.softropic.payam.health.* or com.softropic.payam.ops.*
- This is the cleanest wave — only 4 files change physical location, no external import sites need updating.
- Note: health/* files DO import com.softropic.payam.platform.* (flat) — those imports stay as-is for now (platform flat package moves in Plan 03/PLAT-05).
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move health and ops classes to platform.monitoring</name>
  <files>
    src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java (new),
    src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java (new),
    src/main/java/com/softropic/payam/platform/monitoring/TlsStartupAssertion.java (new),
    src/test/java/com/softropic/payam/platform/monitoring/OperationalIT.java (new),
    src/main/java/com/softropic/payam/health/MtnPlatformHealthIndicator.java (delete),
    src/main/java/com/softropic/payam/health/OrangePlatformHealthIndicator.java (delete),
    src/main/java/com/softropic/payam/ops/TlsStartupAssertion.java (delete),
    src/test/java/com/softropic/payam/ops/OperationalIT.java (delete)
  </files>
  <read_first>
    src/main/java/com/softropic/payam/health/MtnPlatformHealthIndicator.java,
    src/main/java/com/softropic/payam/health/OrangePlatformHealthIndicator.java,
    src/main/java/com/softropic/payam/ops/TlsStartupAssertion.java,
    src/test/java/com/softropic/payam/ops/OperationalIT.java,
    .planning/phases/62-platform-layer-reorganization/62-RESEARCH.md
  </read_first>
  <action>
    Create the new package directories and move all 4 files (3 production + 1 test):

    1. Create `src/main/java/com/softropic/payam/platform/monitoring/` directory.
    2. Create `src/test/java/com/softropic/payam/platform/monitoring/` directory.
    3. Move `src/main/java/com/softropic/payam/health/MtnPlatformHealthIndicator.java` to `src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java`. Change line 1 (package declaration) from `package com.softropic.payam.health;` to `package com.softropic.payam.platform.monitoring;`. KEEP these existing imports unchanged: `import com.softropic.payam.platform.contract.PlatformConfigDto;` and `import com.softropic.payam.platform.service.PlatformConfigService;` (these still resolve — the flat `platform/` package is not yet moved; it moves in Plan 03/PLAT-05). All other code (class body, methods, annotations) stays byte-for-byte identical.
    4. Move `src/main/java/com/softropic/payam/health/OrangePlatformHealthIndicator.java` to `src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java`. Change line 1 from `package com.softropic.payam.health;` to `package com.softropic.payam.platform.monitoring;`. KEEP these existing imports unchanged: `import com.softropic.payam.platform.contract.PlatformConfigDto;` and `import com.softropic.payam.platform.service.PlatformConfigService;`. All other code stays byte-for-byte identical.
    5. Move `src/main/java/com/softropic/payam/ops/TlsStartupAssertion.java` to `src/main/java/com/softropic/payam/platform/monitoring/TlsStartupAssertion.java`. Change line 1 from `package com.softropic.payam.ops;` to `package com.softropic.payam.platform.monitoring;`. All other code stays byte-for-byte identical.
    6. Move `src/test/java/com/softropic/payam/ops/OperationalIT.java` to `src/test/java/com/softropic/payam/platform/monitoring/OperationalIT.java`. Change line 1 from `package com.softropic.payam.ops;` to `package com.softropic.payam.platform.monitoring;`. KEEP `import com.softropic.payam.config.TestConfig;` unchanged. All other code stays byte-for-byte identical.
    7. Delete the now-empty source directories: `src/main/java/com/softropic/payam/health/` and `src/main/java/com/softropic/payam/ops/` and `src/test/java/com/softropic/payam/ops/`. Verify each is empty before deletion using `ls`.

    No `@Component` bean name changes. No method signature changes. Spring Boot's `@SpringBootApplication` auto-scan picks up the new sub-package automatically — no `@ComponentScan(basePackages=...)` configuration needs to change because `com.softropic.payam.platform.monitoring` is already under `com.softropic.payam`.

    Commit the move atomically (single commit) using message: `refactor(62-01): PLAT-04 — move health and ops to platform.monitoring`.
  </action>
  <verify>
    <automated>test -f src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java && test -f src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java && test -f src/main/java/com/softropic/payam/platform/monitoring/TlsStartupAssertion.java && test -f src/test/java/com/softropic/payam/platform/monitoring/OperationalIT.java && ! test -d src/main/java/com/softropic/payam/health && ! test -d src/main/java/com/softropic/payam/ops && ! test -d src/test/java/com/softropic/payam/ops && grep -q "^package com.softropic.payam.platform.monitoring;" src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java && grep -q "^package com.softropic.payam.platform.monitoring;" src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java && grep -q "^package com.softropic.payam.platform.monitoring;" src/main/java/com/softropic/payam/platform/monitoring/TlsStartupAssertion.java && grep -q "^package com.softropic.payam.platform.monitoring;" src/test/java/com/softropic/payam/platform/monitoring/OperationalIT.java && [ "$(grep -rl 'com.softropic.payam.health\.\|com.softropic.payam.ops\.' src --include='*.java' | wc -l | tr -d ' ')" = "0" ] && mvn -q test-compile</automated>
  </verify>
  <acceptance_criteria>
    - `find src/main/java/com/softropic/payam/platform/monitoring -name '*.java'` lists exactly 3 files: MtnPlatformHealthIndicator.java, OrangePlatformHealthIndicator.java, TlsStartupAssertion.java
    - `find src/test/java/com/softropic/payam/platform/monitoring -name '*.java'` lists exactly 1 file: OperationalIT.java
    - `test -d src/main/java/com/softropic/payam/health` returns false (directory deleted)
    - `test -d src/main/java/com/softropic/payam/ops` returns false (directory deleted)
    - `test -d src/test/java/com/softropic/payam/ops` returns false (directory deleted)
    - `head -1 src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java` outputs `package com.softropic.payam.platform.monitoring;`
    - `head -1 src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java` outputs `package com.softropic.payam.platform.monitoring;`
    - `head -1 src/main/java/com/softropic/payam/platform/monitoring/TlsStartupAssertion.java` outputs `package com.softropic.payam.platform.monitoring;`
    - `head -1 src/test/java/com/softropic/payam/platform/monitoring/OperationalIT.java` outputs `package com.softropic.payam.platform.monitoring;`
    - `grep -q "import com.softropic.payam.platform.contract.PlatformConfigDto;" src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java` returns 0 (import preserved)
    - `grep -q "import com.softropic.payam.platform.service.PlatformConfigService;" src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java` returns 0 (import preserved)
    - `grep -rn 'com.softropic.payam.health\.\|com.softropic.payam.ops\.' src --include='*.java'` returns no results (zero stale imports)
    - `mvn -q test-compile` exits 0
  </acceptance_criteria>
  <done>
    All 4 files moved; new package declarations in place; old directories deleted; zero stale imports of `com.softropic.payam.health.*` or `com.softropic.payam.ops.*` anywhere in `src/`; Maven test-compile succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run mvn verify and confirm green</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    pom.xml,
    src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java,
    src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java,
    src/test/java/com/softropic/payam/platform/monitoring/OperationalIT.java
  </read_first>
  <action>
    Run `mvn verify` against the full project. The expected outcome is the same baseline test pass/fail count as before this plan ran. Per Phase 61 SUMMARY notes, there are pre-existing integration test failures caused by Docker daemon not running (Testcontainers cannot start PostgreSQL/Redis). Those failures are out of scope; verify only that:

    1. `mvn -q clean compile` exits 0 (no compilation errors).
    2. `mvn -q test-compile` exits 0 (no test compilation errors).
    3. If Docker daemon is available, `mvn verify` exits 0 with all tests green.
    4. If Docker daemon is unavailable, run `mvn -q test` (unit tests only) and confirm exit 0.

    Run `grep -rn "com.softropic.payam.health\.\|com.softropic.payam.ops\." src --include="*.java"` and confirm zero results — no stale imports remain anywhere.

    If any failure not present on the baseline appears, halt and document in deviations.
  </action>
  <verify>
    <automated>mvn -q clean compile && mvn -q test-compile && [ "$(grep -rn 'com.softropic.payam.health\.\|com.softropic.payam.ops\.' src --include='*.java' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -q clean compile` exits 0
    - `mvn -q test-compile` exits 0
    - `grep -rn "com.softropic.payam.health\.\|com.softropic.payam.ops\." src --include="*.java"` outputs nothing (exit 0 with empty output, or exit 1 with no matches)
    - If Docker available: `mvn verify` exits 0 with `[INFO] BUILD SUCCESS`
    - If Docker unavailable: documented as pre-existing condition matching Phase 61 baseline (33 known integration test failures from missing Docker)
  </acceptance_criteria>
  <done>
    Compilation clean; zero stale imports remain; mvn verify either green (Docker available) or matches pre-existing Docker-absent baseline failure pattern (33 integration test failures, all from Testcontainers unable to start).
  </done>
</task>

</tasks>

<verification>
After all tasks: `find src -type d -name "monitoring" -path "*/platform/*"` shows new directories under both `src/main/java/com/softropic/payam/platform/` and `src/test/java/com/softropic/payam/platform/`. `find src -type d -name "health" -o -type d -name "ops" -path "*/com/softropic/payam/*"` shows nothing.
</verification>

<success_criteria>
- Three production classes (`MtnPlatformHealthIndicator`, `OrangePlatformHealthIndicator`, `TlsStartupAssertion`) live in `src/main/java/com/softropic/payam/platform/monitoring/` with `package com.softropic.payam.platform.monitoring;` declarations.
- `OperationalIT` lives in `src/test/java/com/softropic/payam/platform/monitoring/` with the same package declaration.
- `src/main/java/com/softropic/payam/health/` and `src/main/java/com/softropic/payam/ops/` directories no longer exist.
- `src/test/java/com/softropic/payam/ops/` directory no longer exists.
- `grep -rn "com.softropic.payam.health\." src --include="*.java"` returns nothing.
- `grep -rn "com.softropic.payam.ops\." src --include="*.java"` returns nothing.
- `mvn verify` exits 0 (or matches pre-existing Docker-absence baseline; documented in deviations).
- PLAT-04 requirement is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/62-platform-layer-reorganization/62-01-SUMMARY.md` describing the move, files affected, mvn verify result, and any deviations.
</output>

---
phase: 62-platform-layer-reorganization
plan: 03
type: execute
wave: 2
depends_on: [62-01, 62-02]
files_modified:
  - src/main/java/com/softropic/payam/admin/**/*.java
  - src/main/java/com/softropic/payam/platform/api/*.java
  - src/main/java/com/softropic/payam/platform/config/*.java
  - src/main/java/com/softropic/payam/platform/contract/**/*.java
  - src/main/java/com/softropic/payam/platform/repo/*.java
  - src/main/java/com/softropic/payam/platform/service/*.java
  - src/main/java/com/softropic/payam/platform/admin/**/*.java
  - src/test/java/com/softropic/payam/platform/admin/**/*.java
  - src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java
  - src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java
autonomous: true
requirements: [PLAT-05]

must_haves:
  truths:
    - "platform.admin package contains 22 production files (13 from admin/ + 9 from platform/ flat)"
    - "platform.admin package contains 3 test files (from platform/ test directory)"
    - "Old admin/ source directory is deleted"
    - "Old flat platform/ sub-directories (api, config, contract, repo, service) are deleted; platform/notification/ and platform/monitoring/ from earlier waves remain"
    - "platform.admin.config.PlatformConfig (Spring @Configuration) and platform.admin.repo.PlatformConfig (JPA @Entity) coexist as distinct FQNs"
    - "All callers of admin.* (5 prod + 2 test) and flat platform.* (5 prod + 15 test) updated to import from platform.admin.*"
    - "platform.monitoring.* health indicators (moved in Plan 01) updated to import from platform.admin.* instead of platform.* flat"
    - "mvn verify exits 0 with no regressions vs Phase 61 baseline"
  artifacts:
    - path: "src/main/java/com/softropic/payam/platform/admin/api/PlatformConfigAdminResource.java"
      provides: "Platform config admin resource (was platform.api.PlatformConfigAdminResource)"
      contains: "package com.softropic.payam.platform.admin.api;"
    - path: "src/main/java/com/softropic/payam/platform/admin/api/AdminMetricsResource.java"
      provides: "Admin metrics resource (was admin.api.AdminMetricsResource)"
      contains: "package com.softropic.payam.platform.admin.api;"
    - path: "src/main/java/com/softropic/payam/platform/admin/config/PlatformConfig.java"
      provides: "Spring @Configuration class (was platform.config.PlatformConfig)"
      contains: "package com.softropic.payam.platform.admin.config;"
    - path: "src/main/java/com/softropic/payam/platform/admin/repo/PlatformConfig.java"
      provides: "JPA @Entity class (was platform.repo.PlatformConfig)"
      contains: "package com.softropic.payam.platform.admin.repo;"
    - path: "src/main/java/com/softropic/payam/platform/admin/service/PlatformConfigService.java"
      provides: "Platform config service (was platform.service.PlatformConfigService)"
      contains: "package com.softropic.payam.platform.admin.service;"
  key_links:
    - from: "platform.monitoring.MtnPlatformHealthIndicator (moved in Plan 01)"
      to: "platform.admin.service.PlatformConfigService"
      via: "import statement updated"
      pattern: "import com.softropic.payam.platform.admin\\.(contract|service)\\."
    - from: "callback controllers (mtn.*, orange.*) and PaymentOrchestrator"
      to: "platform.admin.contract.MetricsSnapshotDto"
      via: "import statement updated"
      pattern: "import com.softropic.payam.platform.admin\\.contract\\."
---

<objective>
Merge the 13-file `admin/` package and the 9-file flat `platform/` package (api/config/contract/repo/service sub-packages only — `platform/notification/` and `platform/monitoring/` from Plans 01 and 02 are already in place) into a single `platform.admin/` package. Update all 7 production + 17 test external callers, plus the 2 health indicators (already moved in Plan 01) that import from flat `platform.*`.

Purpose: PLAT-05 requirement — collapse `admin` and the flat `platform` (provider config) packages into `platform.admin`. This merge has the highest risk of name collision because two classes named `PlatformConfig` (one Spring `@Configuration`, one JPA `@Entity`) coexist in distinct sub-packages and must remain distinct.

Output: Populated `platform.admin` package; old `admin/` directory deleted; flat `platform/` sub-directories `api/`, `config/`, `contract/`, `repo/`, `service/` deleted; `mvn verify` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-platform-layer-reorganization/62-RESEARCH.md
@.planning/phases/62-platform-layer-reorganization/62-01-PLAN.md
@.planning/phases/62-platform-layer-reorganization/62-02-PLAN.md

<interfaces>
Source files being moved.

src/main/java/com/softropic/payam/admin/ (13 production files):
  - api/AdminMetricsResource.java, AdminTransactionResource.java, AuditResource.java, ProviderStatusResource.java
  - contract/EventLogEntryDto.java, HashChainAuditSummaryDto.java, HashChainResultDto.java, MetricsSnapshotDto.java, ProviderStatusDto.java, TransactionDetailDto.java, TransactionSummaryDto.java
  - service/AdminTransactionQueryService.java, PaymentMetricsService.java

src/main/java/com/softropic/payam/platform/ flat sub-packages (9 production files; notification/ and monitoring/ from Plans 01 and 02 are NOT moved here):
  - api/PlatformConfigAdminResource.java
  - config/PayamPlatformProperties.java, PlatformConfig.java (Spring @Configuration)
  - contract/PinDto.java, PlatformConfigDto.java
  - contract/event/PlatformConfigChangedEvent.java
  - repo/PlatformConfig.java (JPA @Entity), PlatformConfigRepository.java
  - service/PlatformConfigService.java

src/test/java/com/softropic/payam/platform/ (3 test files at flat-platform sub-paths):
  - PlatformConfigAdminResourceIT.java
  - config/PayamPlatformPropertiesTest.java
  - service/PlatformConfigServiceTest.java

Class name collision (verified): platform.config.PlatformConfig (Spring @Configuration) and platform.repo.PlatformConfig (JPA @Entity) coexist as distinct FQNs. After merge: platform.admin.config.PlatformConfig and platform.admin.repo.PlatformConfig. Sub-packages keep them distinct — no rename needed.

External callers requiring import updates:
  - admin.* callers (5 prod): callback controllers (mtn/orange) and PaymentOrchestrator import admin.contract.MetricsSnapshotDto
  - admin.* callers (2 test): admin-related integration tests
  - platform.* flat callers (5 prod): callback controllers, security.api.*, AND now platform.monitoring.* (Mtn/OrangePlatformHealthIndicator moved in Plan 01) — these import platform.contract.PlatformConfigDto and platform.service.PlatformConfigService
  - platform.* flat callers (15 test): TestConfig.java, e2e/* fixtures, etc.

CRITICAL: Plan 01 moved health indicators to platform.monitoring/ but kept their imports of com.softropic.payam.platform.contract.* and com.softropic.payam.platform.service.* (flat). Those imports MUST be updated in this plan to point to platform.admin.*.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move admin and flat-platform sub-packages into platform.admin (preserve sub-package structure, handle PlatformConfig name pair)</name>
  <files>
    src/main/java/com/softropic/payam/platform/admin/** (22 new production files),
    src/test/java/com/softropic/payam/platform/admin/** (3 new test files),
    src/main/java/com/softropic/payam/admin/** (delete all),
    src/main/java/com/softropic/payam/platform/api/* (delete),
    src/main/java/com/softropic/payam/platform/config/* (delete),
    src/main/java/com/softropic/payam/platform/contract/** (delete),
    src/main/java/com/softropic/payam/platform/repo/* (delete),
    src/main/java/com/softropic/payam/platform/service/* (delete),
    src/test/java/com/softropic/payam/platform/PlatformConfigAdminResourceIT.java (delete),
    src/test/java/com/softropic/payam/platform/config/PayamPlatformPropertiesTest.java (delete),
    src/test/java/com/softropic/payam/platform/service/PlatformConfigServiceTest.java (delete)
  </files>
  <read_first>
    src/main/java/com/softropic/payam/admin/api/AdminMetricsResource.java,
    src/main/java/com/softropic/payam/admin/api/AdminTransactionResource.java,
    src/main/java/com/softropic/payam/admin/contract/MetricsSnapshotDto.java,
    src/main/java/com/softropic/payam/admin/service/PaymentMetricsService.java,
    src/main/java/com/softropic/payam/platform/config/PlatformConfig.java,
    src/main/java/com/softropic/payam/platform/repo/PlatformConfig.java,
    src/main/java/com/softropic/payam/platform/service/PlatformConfigService.java,
    src/main/java/com/softropic/payam/platform/api/PlatformConfigAdminResource.java,
    .planning/phases/62-platform-layer-reorganization/62-RESEARCH.md
  </read_first>
  <action>
    Create directory `src/main/java/com/softropic/payam/platform/admin/` and `src/test/java/com/softropic/payam/platform/admin/`. Move files preserving sub-package structure; merge `admin/` and flat `platform/` (api/config/contract/repo/service) into the new `platform.admin/` namespace.

    From admin/ to platform/admin/ (preserve sub-package):
    - admin/api/AdminMetricsResource.java becomes platform/admin/api/AdminMetricsResource.java (change package to com.softropic.payam.platform.admin.api)
    - admin/api/AdminTransactionResource.java becomes platform/admin/api/AdminTransactionResource.java
    - admin/api/AuditResource.java becomes platform/admin/api/AuditResource.java
    - admin/api/ProviderStatusResource.java becomes platform/admin/api/ProviderStatusResource.java
    - admin/contract/EventLogEntryDto.java becomes platform/admin/contract/EventLogEntryDto.java (change package to com.softropic.payam.platform.admin.contract)
    - admin/contract/HashChainAuditSummaryDto.java becomes platform/admin/contract/HashChainAuditSummaryDto.java
    - admin/contract/HashChainResultDto.java becomes platform/admin/contract/HashChainResultDto.java
    - admin/contract/MetricsSnapshotDto.java becomes platform/admin/contract/MetricsSnapshotDto.java
    - admin/contract/ProviderStatusDto.java becomes platform/admin/contract/ProviderStatusDto.java
    - admin/contract/TransactionDetailDto.java becomes platform/admin/contract/TransactionDetailDto.java
    - admin/contract/TransactionSummaryDto.java becomes platform/admin/contract/TransactionSummaryDto.java
    - admin/service/AdminTransactionQueryService.java becomes platform/admin/service/AdminTransactionQueryService.java
    - admin/service/PaymentMetricsService.java becomes platform/admin/service/PaymentMetricsService.java

    From flat platform/ to platform/admin/ (merge into sub-packages, preserving the PlatformConfig name pair):
    - platform/api/PlatformConfigAdminResource.java becomes platform/admin/api/PlatformConfigAdminResource.java (merges with admin api/ sub-package)
    - platform/config/PayamPlatformProperties.java becomes platform/admin/config/PayamPlatformProperties.java
    - platform/config/PlatformConfig.java (Spring @Configuration) becomes platform/admin/config/PlatformConfig.java
    - platform/contract/PinDto.java becomes platform/admin/contract/PinDto.java
    - platform/contract/PlatformConfigDto.java becomes platform/admin/contract/PlatformConfigDto.java
    - platform/contract/event/PlatformConfigChangedEvent.java becomes platform/admin/contract/event/PlatformConfigChangedEvent.java
    - platform/repo/PlatformConfig.java (JPA @Entity) becomes platform/admin/repo/PlatformConfig.java
    - platform/repo/PlatformConfigRepository.java becomes platform/admin/repo/PlatformConfigRepository.java
    - platform/service/PlatformConfigService.java becomes platform/admin/service/PlatformConfigService.java

    Test moves:
    - src/test/java/com/softropic/payam/platform/PlatformConfigAdminResourceIT.java becomes src/test/java/com/softropic/payam/platform/admin/PlatformConfigAdminResourceIT.java (change package to com.softropic.payam.platform.admin)
    - src/test/java/com/softropic/payam/platform/config/PayamPlatformPropertiesTest.java becomes src/test/java/com/softropic/payam/platform/admin/config/PayamPlatformPropertiesTest.java (change package to com.softropic.payam.platform.admin.config)
    - src/test/java/com/softropic/payam/platform/service/PlatformConfigServiceTest.java becomes src/test/java/com/softropic/payam/platform/admin/service/PlatformConfigServiceTest.java (change package to com.softropic.payam.platform.admin.service)

    Within-package import rewrite (inside the moved files):
    - In every moved file, replace `import com.softropic.payam.admin.` with `import com.softropic.payam.platform.admin.` (string substitution preserving sub-package suffix).
    - In every moved file, replace any reference to `com.softropic.payam.platform.api.` with `com.softropic.payam.platform.admin.api.`.
    - Replace `com.softropic.payam.platform.config.` with `com.softropic.payam.platform.admin.config.`.
    - Replace `com.softropic.payam.platform.contract.` with `com.softropic.payam.platform.admin.contract.`.
    - Replace `com.softropic.payam.platform.repo.` with `com.softropic.payam.platform.admin.repo.`.
    - Replace `com.softropic.payam.platform.service.` with `com.softropic.payam.platform.admin.service.`.
    - Do NOT modify imports of `com.softropic.payam.platform.notification.` or `com.softropic.payam.platform.monitoring.`.

    PlatformConfig name pair check: After moves, run `find src/main/java/com/softropic/payam/platform/admin -name "PlatformConfig.java"` — must return exactly 2 paths: platform/admin/config/PlatformConfig.java and platform/admin/repo/PlatformConfig.java. Verify each has the correct @Configuration vs @Entity annotation respectively.

    Directory deletion: Delete `src/main/java/com/softropic/payam/admin/` (entire tree). Delete `src/main/java/com/softropic/payam/platform/api/`, `src/main/java/com/softropic/payam/platform/config/`, `src/main/java/com/softropic/payam/platform/contract/`, `src/main/java/com/softropic/payam/platform/repo/`, `src/main/java/com/softropic/payam/platform/service/`. Verify each is empty before deletion. Do NOT delete `src/main/java/com/softropic/payam/platform/notification/` or `src/main/java/com/softropic/payam/platform/monitoring/`.

    Delete `src/test/java/com/softropic/payam/platform/PlatformConfigAdminResourceIT.java` and the empty `src/test/java/com/softropic/payam/platform/config/`, `src/test/java/com/softropic/payam/platform/service/` directories. Do NOT delete `src/test/java/com/softropic/payam/platform/notification/` or `src/test/java/com/softropic/payam/platform/monitoring/`.

    Do NOT update external callers in this task — that is Task 2.
  </action>
  <verify>
    <automated>[ "$(find src/main/java/com/softropic/payam/platform/admin -name '*.java' | wc -l | tr -d ' ')" = "22" ] && [ "$(find src/test/java/com/softropic/payam/platform/admin -name '*.java' | wc -l | tr -d ' ')" = "3" ] && ! test -d src/main/java/com/softropic/payam/admin && ! test -d src/main/java/com/softropic/payam/platform/api && ! test -d src/main/java/com/softropic/payam/platform/config && ! test -d src/main/java/com/softropic/payam/platform/contract && ! test -d src/main/java/com/softropic/payam/platform/repo && ! test -d src/main/java/com/softropic/payam/platform/service && test -d src/main/java/com/softropic/payam/platform/notification && test -d src/main/java/com/softropic/payam/platform/monitoring && [ "$(find src/main/java/com/softropic/payam/platform/admin -name 'PlatformConfig.java' | wc -l | tr -d ' ')" = "2" ]</automated>
  </verify>
  <acceptance_criteria>
    - `find src/main/java/com/softropic/payam/platform/admin -name '*.java' | wc -l` outputs `22`
    - `find src/test/java/com/softropic/payam/platform/admin -name '*.java' | wc -l` outputs `3`
    - `test -d src/main/java/com/softropic/payam/admin` returns false
    - `test -d src/main/java/com/softropic/payam/platform/api` returns false
    - `test -d src/main/java/com/softropic/payam/platform/config` returns false
    - `test -d src/main/java/com/softropic/payam/platform/contract` returns false
    - `test -d src/main/java/com/softropic/payam/platform/repo` returns false
    - `test -d src/main/java/com/softropic/payam/platform/service` returns false
    - `test -d src/main/java/com/softropic/payam/platform/notification` returns true (preserved from Plan 02)
    - `test -d src/main/java/com/softropic/payam/platform/monitoring` returns true (preserved from Plan 01)
    - `find src/main/java/com/softropic/payam/platform/admin -name 'PlatformConfig.java'` returns exactly 2 paths (config/ and repo/)
    - `head -1 src/main/java/com/softropic/payam/platform/admin/config/PlatformConfig.java` outputs `package com.softropic.payam.platform.admin.config;`
    - `head -1 src/main/java/com/softropic/payam/platform/admin/repo/PlatformConfig.java` outputs `package com.softropic.payam.platform.admin.repo;`
    - `head -1 src/main/java/com/softropic/payam/platform/admin/contract/MetricsSnapshotDto.java` outputs `package com.softropic.payam.platform.admin.contract;`
    - `head -1 src/main/java/com/softropic/payam/platform/admin/api/AdminMetricsResource.java` outputs `package com.softropic.payam.platform.admin.api;`
    - `head -1 src/main/java/com/softropic/payam/platform/admin/api/PlatformConfigAdminResource.java` outputs `package com.softropic.payam.platform.admin.api;`
    - `grep -q "@Configuration" src/main/java/com/softropic/payam/platform/admin/config/PlatformConfig.java` returns 0
    - `grep -q "@Entity" src/main/java/com/softropic/payam/platform/admin/repo/PlatformConfig.java` returns 0
  </acceptance_criteria>
  <done>
    All 22 production + 3 test files moved into platform.admin with preserved sub-packages; package declarations updated; PlatformConfig name pair preserved as distinct FQNs; old admin/ and flat platform/ sub-dirs deleted; platform/notification/ and platform/monitoring/ preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update external caller imports (production + test) including platform.monitoring health indicators, then run mvn verify</name>
  <files>
    All .java files under src/ that import com.softropic.payam.admin.* or com.softropic.payam.platform.{api,config,contract,repo,service}.* — discovered via grep,
    src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java,
    src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java
  </files>
  <read_first>
    src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java,
    src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java
  </read_first>
  <action>
    Discover and rewrite all stale imports.

    1. Run `grep -rln "com.softropic.payam.admin\." src --include="*.java"` to find all callers of admin.*. Expected ~7 files (5 prod + 2 test).
    2. Run `grep -rEln 'com\.softropic\.payam\.platform\.(api|config|contract|repo|service)\.' src --include="*.java"` to find callers of flat platform sub-packages (NOT notification or monitoring). Expected ~20 files (5 prod + 15 test).
    3. For each discovered file, rewrite imports literally:
       - `import com.softropic.payam.admin.` becomes `import com.softropic.payam.platform.admin.` (preserving sub-package path: `admin.contract.MetricsSnapshotDto` becomes `platform.admin.contract.MetricsSnapshotDto`).
       - `import com.softropic.payam.platform.api.` becomes `import com.softropic.payam.platform.admin.api.`
       - `import com.softropic.payam.platform.config.` becomes `import com.softropic.payam.platform.admin.config.`
       - `import com.softropic.payam.platform.contract.` becomes `import com.softropic.payam.platform.admin.contract.`
       - `import com.softropic.payam.platform.repo.` becomes `import com.softropic.payam.platform.admin.repo.`
       - `import com.softropic.payam.platform.service.` becomes `import com.softropic.payam.platform.admin.service.`
    4. CRITICAL — update the two health indicators moved in Plan 01:
       - `src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java` — replace `import com.softropic.payam.platform.contract.PlatformConfigDto;` with `import com.softropic.payam.platform.admin.contract.PlatformConfigDto;` and replace `import com.softropic.payam.platform.service.PlatformConfigService;` with `import com.softropic.payam.platform.admin.service.PlatformConfigService;`.
       - `src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java` — same two import updates.
    5. Do NOT modify imports of `com.softropic.payam.platform.notification.*` or `com.softropic.payam.platform.monitoring.*` (those are correct already).
    6. Do NOT modify any other code — only `import` statements and any fully-qualified-name references inside method bodies.

    After updates, verify:
    1. `grep -rn "com.softropic.payam.admin\." src --include="*.java"` returns nothing.
    2. `grep -rEn 'com\.softropic\.payam\.platform\.(api|config|contract|repo|service)\.' src --include="*.java"` returns nothing.
    3. `mvn -q clean compile` exits 0.
    4. `mvn -q test-compile` exits 0.
    5. `mvn verify` matches Phase 61 baseline (green if Docker available; otherwise pre-existing 33 integration test failures only).

    Commit message: `refactor(62-03): PLAT-05 — merge admin and flat-platform into platform.admin`.
  </action>
  <verify>
    <automated>[ "$(grep -rn 'com.softropic.payam.admin\.' src --include='*.java' | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -rEn 'com\.softropic\.payam\.platform\.(api|config|contract|repo|service)\.' src --include='*.java' | wc -l | tr -d ' ')" = "0" ] && grep -q "import com.softropic.payam.platform.admin.contract.PlatformConfigDto;" src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java && grep -q "import com.softropic.payam.platform.admin.service.PlatformConfigService;" src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java && mvn -q clean compile && mvn -q test-compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "com.softropic.payam.admin\." src --include="*.java"` returns no matches
    - `grep -rEn 'com\.softropic\.payam\.platform\.(api|config|contract|repo|service)\.' src --include="*.java"` returns no matches
    - `grep -q "import com.softropic.payam.platform.admin.contract.PlatformConfigDto;" src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java` returns 0
    - `grep -q "import com.softropic.payam.platform.admin.service.PlatformConfigService;" src/main/java/com/softropic/payam/platform/monitoring/MtnPlatformHealthIndicator.java` returns 0
    - `grep -q "import com.softropic.payam.platform.admin.contract.PlatformConfigDto;" src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java` returns 0
    - `grep -q "import com.softropic.payam.platform.admin.service.PlatformConfigService;" src/main/java/com/softropic/payam/platform/monitoring/OrangePlatformHealthIndicator.java` returns 0
    - `mvn -q clean compile` exits 0
    - `mvn -q test-compile` exits 0
    - `mvn verify` exits 0 if Docker available, OR matches Phase 61 baseline (33 known Testcontainers failures only — no NEW failures)
  </acceptance_criteria>
  <done>
    Zero stale imports from `com.softropic.payam.admin.*` or `com.softropic.payam.platform.{api,config,contract,repo,service}.*` remain; health indicators in platform.monitoring updated; compilation green; verify matches baseline.
  </done>
</task>

</tasks>

<verification>
After both tasks: `find src/main/java/com/softropic/payam/platform -maxdepth 1 -type d` shows only platform/admin, platform/notification, platform/monitoring (no api/config/contract/repo/service direct children). `grep -rn "com.softropic.payam.admin\." src --include="*.java"` returns zero. `grep -rEn 'com\.softropic\.payam\.platform\.(api|config|contract|repo|service)\.' src --include="*.java"` returns zero.
</verification>

<success_criteria>
- 22 production classes (13 admin + 9 platform-flat) and 3 test classes consolidated under `platform/admin/` with preserved sub-package structure (`api/`, `config/`, `contract/`, `contract/event/`, `repo/`, `service/`).
- Both `PlatformConfig` classes (Spring `@Configuration` and JPA `@Entity`) preserved as distinct FQNs `platform.admin.config.PlatformConfig` and `platform.admin.repo.PlatformConfig`.
- Old `admin/` directory and flat `platform/` sub-directories (api, config, contract, repo, service) deleted.
- `platform/notification/` and `platform/monitoring/` from Plans 01–02 preserved intact.
- `platform.monitoring` health indicator imports updated to point at `platform.admin.contract.PlatformConfigDto` and `platform.admin.service.PlatformConfigService`.
- All external callers (production + test) updated.
- `mvn verify` matches Phase 61 baseline.
- PLAT-05 requirement satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/62-platform-layer-reorganization/62-03-SUMMARY.md` capturing the merge details: file counts moved, name-pair collision resolution, infrastructure.web (untouched in this plan), and any deviations.
</output>

---
phase: 62-platform-layer-reorganization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/softropic/payam/email/**/*.java
  - src/main/java/com/softropic/payam/alert/**/*.java
  - src/main/java/com/softropic/payam/platform/notification/**/*.java
  - src/test/java/com/softropic/payam/email/**/*.java
  - src/test/java/com/softropic/payam/alert/**/*.java
  - src/test/java/com/softropic/payam/platform/notification/**/*.java
  - src/main/java/com/softropic/payam/infrastructure/config/AsyncConfig.java
  - src/main/java/com/softropic/payam/security/api/**/*.java
autonomous: true
requirements: [PLAT-03]

must_haves:
  truths:
    - "platform.notification package contains 23 production files (formerly email/) plus 7 production files (formerly alert/) — total 30 production classes"
    - "platform.notification package contains 8 test files (7 from email/ + 1 from alert/)"
    - "Old email/ and alert/ source directories are deleted (empty)"
    - "All external callers (7 prod + 8 test files) updated to import from platform.notification"
    - "AlertNotificationListener still calls MailManager.send(...) — same logic, same package now"
    - "TenantLifecycleEmailListener still subscribes to tenant lifecycle events"
    - "mvn verify exits 0 with no regressions vs Phase 61 baseline"
  artifacts:
    - path: "src/main/java/com/softropic/payam/platform/notification/service/MailManager.java"
      provides: "Email send entry point under new platform.notification package"
      contains: "package com.softropic.payam.platform.notification.service;"
    - path: "src/main/java/com/softropic/payam/platform/notification/service/AlertNotificationListener.java"
      provides: "Alert listener relocated from alert/ — merged into notification package"
      contains: "package com.softropic.payam.platform.notification.service;"
    - path: "src/main/java/com/softropic/payam/platform/notification/contract/EmailTemplate.java"
      provides: "Email template enum under new package"
      contains: "package com.softropic.payam.platform.notification.contract;"
    - path: "src/main/java/com/softropic/payam/platform/notification/api/AlertRuleAdminResource.java"
      provides: "Alert rule admin resource (was alert.api.AlertRuleAdminResource)"
      contains: "package com.softropic.payam.platform.notification.api;"
  key_links:
    - from: "platform.notification.service.AlertNotificationListener"
      to: "platform.notification.service.MailManager"
      via: "constructor-injected dependency"
      pattern: "import com.softropic.payam.platform.notification\\."
    - from: "infrastructure.config.AsyncConfig"
      to: "platform.notification.service.* (email pool wiring)"
      via: "import statement"
      pattern: "import com.softropic.payam.platform.notification\\."
---

<objective>
Merge `email/` (23 production + 7 test) and `alert/` (7 production + 1 test) packages into a single `platform.notification/` package. Update all external callers (7 production files + 8 test files) to import from the new path.

Purpose: PLAT-03 requirement — collapse `email` and `alert` into `platform.notification`. The merger eliminates cross-package coupling because `alert.AlertNotificationListener` already imports `email.MailManager` and `email.contract.*` — after the merge those become same-package references.

Output: Populated `platform.notification` package; old `email/` and `alert/` directories deleted; all callers updated; `mvn verify` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-platform-layer-reorganization/62-RESEARCH.md
@.planning/phases/61-infrastructure-layer-creation/61-01-SUMMARY.md

<interfaces>
<!-- Source packages being merged -->

src/main/java/com/softropic/payam/email/ (23 production files):
  - config/AsyncConfig.java, ComponentConfig.java, ThymeleafConfiguration.java
  - contract/Envelope.java, EmailDeliveryStatus.java, EmailProperties.java, EmailTemplate.java, ProviderConfig.java, Recipient.java
  - infrastructure/MailSenderProvider.java, EmailRetryScheduler.java
  - infrastructure/listener/TenantLifecycleEmailListener.java, DisbursementOpsAlertEmailListener.java, PlatformConfigEmailListener.java, AccountChangeEmailListener.java
  - repo/EnvelopeEntity.java, EnvelopeEntityRepository.java, RecipientEntity.java
  - service/MailManager.java, MailService.java, EnvelopeMapper.java, ResendEmailService.java, SenderProvider.java

src/main/java/com/softropic/payam/alert/ (7 production files):
  - api/AlertRuleAdminResource.java
  - contract/AlertFiredEvent.java
  - repo/AlertRule.java, AlertRuleRepository.java
  - service/AlertEvaluationService.java, AlertNotificationListener.java, AlertRuleCache.java

External production callers of email.* or alert.* (must update imports — verify with `grep -rl "com.softropic.payam.\\(email\\|alert\\)\\." src/main --include="*.java"`):
  - src/main/java/com/softropic/payam/infrastructure/config/AsyncConfig.java (imports email.service.* for thread pool wiring)
  - src/main/java/com/softropic/payam/security/api/* (imports email contracts for password reset / account change events)
  - Any tenant.* or disbursement.* class publishing TenantLifecycleEvent / DisbursementOpsAlertEvent

External test callers (must update imports):
  - src/test/java/com/softropic/payam/config/TestConfig.java
  - src/test/java/com/softropic/payam/config/TestMailConfig.java
  - any test importing email.contract.* or email.service.MailManager

Note on AlertNotificationListener: currently imports com.softropic.payam.email.contract.Envelope and com.softropic.payam.email.service.MailManager. After this merge, those become com.softropic.payam.platform.notification.contract.Envelope and com.softropic.payam.platform.notification.service.MailManager (same package as AlertNotificationListener itself, but the explicit import path is updated for consistency — research recommends keeping explicit imports rather than relying on same-package shorthand).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move email and alert files into platform.notification (preserve sub-package structure)</name>
  <files>
    src/main/java/com/softropic/payam/platform/notification/** (30 new production files),
    src/test/java/com/softropic/payam/platform/notification/** (8 new test files),
    src/main/java/com/softropic/payam/email/** (delete all),
    src/main/java/com/softropic/payam/alert/** (delete all),
    src/test/java/com/softropic/payam/email/** (delete all),
    src/test/java/com/softropic/payam/alert/** (delete all)
  </files>
  <read_first>
    src/main/java/com/softropic/payam/email/service/MailManager.java,
    src/main/java/com/softropic/payam/email/contract/EmailTemplate.java,
    src/main/java/com/softropic/payam/email/contract/Envelope.java,
    src/main/java/com/softropic/payam/email/infrastructure/listener/TenantLifecycleEmailListener.java,
    src/main/java/com/softropic/payam/alert/service/AlertNotificationListener.java,
    src/main/java/com/softropic/payam/alert/api/AlertRuleAdminResource.java,
    .planning/phases/62-platform-layer-reorganization/62-RESEARCH.md
  </read_first>
  <action>
    Move all 23 email production files + 7 alert production files into `src/main/java/com/softropic/payam/platform/notification/`, preserving sub-package structure as follows:

    **From email/ → platform/notification/ (preserve sub-package):**
    - `email/config/AsyncConfig.java` → `platform/notification/config/AsyncConfig.java` (change package to `com.softropic.payam.platform.notification.config`)
    - `email/config/ComponentConfig.java` → `platform/notification/config/ComponentConfig.java`
    - `email/config/ThymeleafConfiguration.java` → `platform/notification/config/ThymeleafConfiguration.java`
    - `email/contract/Envelope.java` → `platform/notification/contract/Envelope.java` (change package to `com.softropic.payam.platform.notification.contract`)
    - `email/contract/EmailDeliveryStatus.java` → `platform/notification/contract/EmailDeliveryStatus.java`
    - `email/contract/EmailProperties.java` → `platform/notification/contract/EmailProperties.java`
    - `email/contract/EmailTemplate.java` → `platform/notification/contract/EmailTemplate.java`
    - `email/contract/ProviderConfig.java` → `platform/notification/contract/ProviderConfig.java`
    - `email/contract/Recipient.java` → `platform/notification/contract/Recipient.java`
    - `email/infrastructure/MailSenderProvider.java` → `platform/notification/infrastructure/MailSenderProvider.java`
    - `email/infrastructure/EmailRetryScheduler.java` → `platform/notification/infrastructure/EmailRetryScheduler.java`
    - `email/infrastructure/listener/TenantLifecycleEmailListener.java` → `platform/notification/infrastructure/listener/TenantLifecycleEmailListener.java`
    - `email/infrastructure/listener/DisbursementOpsAlertEmailListener.java` → `platform/notification/infrastructure/listener/DisbursementOpsAlertEmailListener.java`
    - `email/infrastructure/listener/PlatformConfigEmailListener.java` → `platform/notification/infrastructure/listener/PlatformConfigEmailListener.java`
    - `email/infrastructure/listener/AccountChangeEmailListener.java` → `platform/notification/infrastructure/listener/AccountChangeEmailListener.java`
    - `email/repo/EnvelopeEntity.java` → `platform/notification/repo/EnvelopeEntity.java`
    - `email/repo/EnvelopeEntityRepository.java` → `platform/notification/repo/EnvelopeEntityRepository.java`
    - `email/repo/RecipientEntity.java` → `platform/notification/repo/RecipientEntity.java`
    - `email/service/MailManager.java` → `platform/notification/service/MailManager.java`
    - `email/service/MailService.java` → `platform/notification/service/MailService.java`
    - `email/service/EnvelopeMapper.java` → `platform/notification/service/EnvelopeMapper.java`
    - `email/service/ResendEmailService.java` → `platform/notification/service/ResendEmailService.java`
    - `email/service/SenderProvider.java` → `platform/notification/service/SenderProvider.java`

    **From alert/ → platform/notification/ (merge into existing sub-packages where they overlap):**
    - `alert/api/AlertRuleAdminResource.java` → `platform/notification/api/AlertRuleAdminResource.java` (new `api/` sub-package)
    - `alert/contract/AlertFiredEvent.java` → `platform/notification/contract/AlertFiredEvent.java` (merges with email contract sub-pkg)
    - `alert/repo/AlertRule.java` → `platform/notification/repo/AlertRule.java` (merges with email repo)
    - `alert/repo/AlertRuleRepository.java` → `platform/notification/repo/AlertRuleRepository.java`
    - `alert/service/AlertEvaluationService.java` → `platform/notification/service/AlertEvaluationService.java`
    - `alert/service/AlertNotificationListener.java` → `platform/notification/service/AlertNotificationListener.java`
    - `alert/service/AlertRuleCache.java` → `platform/notification/service/AlertRuleCache.java`

    **From test packages:**
    - All 7 files from `src/test/java/com/softropic/payam/email/` move to `src/test/java/com/softropic/payam/platform/notification/`, preserving sub-package structure. Update package declaration in each.
    - All 1 file from `src/test/java/com/softropic/payam/alert/` (typically `AlertRuleIT.java` or similar) moves to `src/test/java/com/softropic/payam/platform/notification/`. Update package declaration.

    **Within-package imports (update inside the moved files):**
    - In every moved file, replace any `import com.softropic.payam.email.` with `import com.softropic.payam.platform.notification.` (string substitution, including sub-package paths).
    - In every moved file, replace any `import com.softropic.payam.alert.` with `import com.softropic.payam.platform.notification.` (string substitution).
    - Preserve all other imports byte-for-byte.

    **Class name collision check:** `email/contract/EmailTemplate.java` and `alert/contract/AlertFiredEvent.java` go into the same `platform/notification/contract/` directory. They have distinct class names, so no collision occurs. Verify `find platform/notification/contract -name "*.java" | xargs basename | sort | uniq -d` returns nothing (no duplicate filenames).

    **Sub-directory deletion:** After moves, delete `src/main/java/com/softropic/payam/email/`, `src/main/java/com/softropic/payam/alert/`, `src/test/java/com/softropic/payam/email/`, `src/test/java/com/softropic/payam/alert/` (and their parent dirs if empty). Verify each is empty before deletion.

    **Do NOT update external callers in this task.** That happens in Task 2.
  </action>
  <verify>
    <automated>[ "$(find src/main/java/com/softropic/payam/platform/notification -name '*.java' | wc -l | tr -d ' ')" = "30" ] && [ "$(find src/test/java/com/softropic/payam/platform/notification -name '*.java' | wc -l | tr -d ' ')" = "8" ] && ! test -d src/main/java/com/softropic/payam/email && ! test -d src/main/java/com/softropic/payam/alert && ! test -d src/test/java/com/softropic/payam/email && ! test -d src/test/java/com/softropic/payam/alert && [ "$(grep -rln 'package com.softropic.payam.email\|package com.softropic.payam.alert' src --include='*.java' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `find src/main/java/com/softropic/payam/platform/notification -name '*.java' | wc -l` outputs `30`
    - `find src/test/java/com/softropic/payam/platform/notification -name '*.java' | wc -l` outputs `8`
    - `test -d src/main/java/com/softropic/payam/email` returns false
    - `test -d src/main/java/com/softropic/payam/alert` returns false
    - `test -d src/test/java/com/softropic/payam/email` returns false
    - `test -d src/test/java/com/softropic/payam/alert` returns false
    - `head -1 src/main/java/com/softropic/payam/platform/notification/service/MailManager.java` outputs `package com.softropic.payam.platform.notification.service;`
    - `head -1 src/main/java/com/softropic/payam/platform/notification/service/AlertNotificationListener.java` outputs `package com.softropic.payam.platform.notification.service;`
    - `head -1 src/main/java/com/softropic/payam/platform/notification/contract/EmailTemplate.java` outputs `package com.softropic.payam.platform.notification.contract;`
    - `head -1 src/main/java/com/softropic/payam/platform/notification/contract/AlertFiredEvent.java` outputs `package com.softropic.payam.platform.notification.contract;`
    - `head -1 src/main/java/com/softropic/payam/platform/notification/api/AlertRuleAdminResource.java` outputs `package com.softropic.payam.platform.notification.api;`
    - `grep -rln "package com.softropic.payam.email\b\|package com.softropic.payam.alert\b" src --include='*.java'` returns empty
    - `grep -q "import com.softropic.payam.platform.notification.service.MailManager;" src/main/java/com/softropic/payam/platform/notification/service/AlertNotificationListener.java` returns 0 (within-package import updated)
  </acceptance_criteria>
  <done>
    All 38 files (30 prod + 8 test) physically moved; package declarations updated; within-package imports rewritten from `email.` / `alert.` to `platform.notification.`; old directories deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update external caller imports and run mvn verify</name>
  <files>
    src/main/java/com/softropic/payam/infrastructure/config/AsyncConfig.java,
    src/main/java/com/softropic/payam/security/api/*.java (any importing email/alert),
    src/test/java/com/softropic/payam/config/TestConfig.java,
    src/test/java/com/softropic/payam/config/TestMailConfig.java,
    any other src/**/*.java still importing com.softropic.payam.email.* or com.softropic.payam.alert.*
  </files>
  <read_first>
    src/main/java/com/softropic/payam/infrastructure/config/AsyncConfig.java,
    src/test/java/com/softropic/payam/config/TestConfig.java,
    src/test/java/com/softropic/payam/config/TestMailConfig.java
  </read_first>
  <action>
    Discover all remaining external callers and rewrite their imports:

    1. Run `grep -rln "com.softropic.payam.email\.\|com.softropic.payam.alert\." src --include="*.java"` to enumerate every remaining stale import site.
    2. For each file in that list, replace every occurrence of `com.softropic.payam.email.` with `com.softropic.payam.platform.notification.` (including sub-package paths like `email.contract.Envelope` → `platform.notification.contract.Envelope`).
    3. For each file in that list, replace every occurrence of `com.softropic.payam.alert.` with `com.softropic.payam.platform.notification.` (including sub-package paths).
    4. Do NOT modify any other code in those files — only `import` statements and any fully-qualified-name references.

    Concrete sites (verified via grep):
    - `src/main/java/com/softropic/payam/infrastructure/config/AsyncConfig.java` — imports email.service.* for thread pool factory beans. Replace all `com.softropic.payam.email.` → `com.softropic.payam.platform.notification.`.
    - `src/main/java/com/softropic/payam/security/api/*.java` — security API resources publishing AccountChangeEmailListener events. Replace all `com.softropic.payam.email.contract.` → `com.softropic.payam.platform.notification.contract.`.
    - Any tenant/disbursement classes publishing email-listener events.
    - Test fixtures `TestConfig.java` and `TestMailConfig.java` referencing email/alert beans.

    After updates, run:
    1. `grep -rn "com.softropic.payam.email\.\|com.softropic.payam.alert\." src --include="*.java"` — must return zero results.
    2. `mvn -q clean compile` — must exit 0.
    3. `mvn -q test-compile` — must exit 0.
    4. `mvn verify` — should match Phase 61 baseline (green if Docker available; otherwise documented pre-existing 33 integration test failures from Testcontainers).

    Commit message: `refactor(62-02): PLAT-03 — merge email and alert into platform.notification`.
  </action>
  <verify>
    <automated>[ "$(grep -rn 'com.softropic.payam.email\.\|com.softropic.payam.alert\.' src --include='*.java' | wc -l | tr -d ' ')" = "0" ] && mvn -q clean compile && mvn -q test-compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "com.softropic.payam.email\." src --include="*.java"` returns no matches (zero stale email imports)
    - `grep -rn "com.softropic.payam.alert\." src --include="*.java"` returns no matches (zero stale alert imports)
    - `grep -q "import com.softropic.payam.platform.notification\." src/main/java/com/softropic/payam/infrastructure/config/AsyncConfig.java` returns 0 if AsyncConfig was previously importing email
    - `mvn -q clean compile` exits 0
    - `mvn -q test-compile` exits 0
    - `mvn verify` exits 0 if Docker available, OR matches Phase 61 baseline failure list (33 integration tests failing solely due to Testcontainers Docker absence) — no NEW failures
  </acceptance_criteria>
  <done>
    Zero `com.softropic.payam.email.*` or `com.softropic.payam.alert.*` references remain anywhere in `src/`. Compilation green. Verify matches baseline.
  </done>
</task>

</tasks>

<verification>
After both tasks: `find src -type d -name "email" -o -type d -name "alert" -path "*/com/softropic/payam/*"` returns nothing. `grep -rn "com.softropic.payam.\(email\|alert\)\." src --include="*.java"` returns zero matches.
</verification>

<success_criteria>
- 30 production classes (23 email + 7 alert) and 8 test classes consolidated under `platform/notification/` with preserved sub-package structure (`api/`, `config/`, `contract/`, `infrastructure/`, `infrastructure/listener/`, `repo/`, `service/`).
- Old `email/` and `alert/` directories deleted from both `src/main/java` and `src/test/java`.
- All external callers (production + test) updated to import `com.softropic.payam.platform.notification.*`.
- AlertNotificationListener still wires to MailManager (now via same-package or explicit `platform.notification` path).
- `mvn verify` exits 0 if Docker available; otherwise matches Phase 61 pre-existing baseline.
- PLAT-03 requirement satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/62-platform-layer-reorganization/62-02-SUMMARY.md` capturing the merge details: file counts moved per source package, external caller files updated, mvn verify result, and any deviations.
</output>

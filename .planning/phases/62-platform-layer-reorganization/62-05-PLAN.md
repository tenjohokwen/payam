---
phase: 62-platform-layer-reorganization
plan: 05
type: execute
wave: 4
depends_on: [62-04]
files_modified:
  - src/main/java/com/softropic/payam/security/**/*.java
  - src/main/java/com/softropic/payam/platform/security/**/*.java
  - src/test/java/com/softropic/payam/security/**/*.java
  - src/test/java/com/softropic/payam/platform/security/**/*.java
  - src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java
  - src/main/java/com/softropic/payam/infrastructure/web/LoggingFilter.java
  - src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java
autonomous: true
requirements: [PLAT-02]

must_haves:
  truths:
    - "platform.security package contains 145 production files preserving sub-package structure (api, audit, common, config, contract, infrastructure, repo, service)"
    - "platform.security package contains 19 test files (formerly under security/)"
    - "Old security/ source directory is deleted from both src/main and src/test"
    - "infrastructure.web.ApiKeyAuthenticationFilter imports updated for security.common.util.TenantContext and security.config.AppEndpoints (2 import lines)"
    - "infrastructure.web.LoggingFilter import updated for security.common.util.TenantContext (1 import line)"
    - "infrastructure.web.TenantSecurityConfig import updated for security.config.AppEndpoints (1 import line)"
    - "30 external callers (23 prod + 7 test files) updated to import from platform.security.*"
    - "FilterRegistrationBean(setEnabled=false) pattern still preserved on ApiKeyAuthenticationFilter (per Phase 61 carry-forward)"
    - "SecurityFilterChainIT, SecurityIT, TenantFilterChainIT all pass from updated locations"
    - "mvn verify exits 0 with no regressions vs Phase 61 baseline"
  artifacts:
    - path: "src/main/java/com/softropic/payam/platform/security/config/SecurityConfiguration.java"
      provides: "Spring Security filter chain configuration under new package"
      contains: "package com.softropic.payam.platform.security.config;"
    - path: "src/main/java/com/softropic/payam/platform/security/config/AppEndpoints.java"
      provides: "Endpoint allowlist constants under new package"
      contains: "package com.softropic.payam.platform.security.config;"
    - path: "src/main/java/com/softropic/payam/platform/security/common/util/TenantContext.java"
      provides: "Thread-local tenant context utility under new package"
      contains: "package com.softropic.payam.platform.security.common.util;"
    - path: "src/main/java/com/softropic/payam/platform/security/common/util/SecurityConstants.java"
      provides: "Security constants under new package"
      contains: "package com.softropic.payam.platform.security.common.util;"
  key_links:
    - from: "infrastructure.web.ApiKeyAuthenticationFilter"
      to: "platform.security.common.util.TenantContext, platform.security.config.AppEndpoints"
      via: "import statements (2 lines)"
      pattern: "import com.softropic.payam.platform.security\\."
    - from: "infrastructure.web.LoggingFilter"
      to: "platform.security.common.util.TenantContext"
      via: "import statement (1 line)"
      pattern: "import com.softropic.payam.platform.security.common.util.TenantContext;"
    - from: "infrastructure.web.TenantSecurityConfig"
      to: "platform.security.config.AppEndpoints"
      via: "import statement (1 line)"
      pattern: "import com.softropic.payam.platform.security.config.AppEndpoints;"
    - from: "platform.security.config.SecurityConfiguration"
      to: "infrastructure.web.LoggingFilter (already correct from Phase 61)"
      via: "import statement (no change needed)"
      pattern: "import com.softropic.payam.infrastructure.web.LoggingFilter;"
---

<objective>
Move all 145 production files and 19 test files in the `security/` package into `platform.security/`, preserving the 8 sub-packages (api, audit, common, config, contract, infrastructure, repo, service). Update 30 external callers (23 prod + 7 test) plus the critical infrastructure.web cascade (4 import lines across 3 files).

Purpose: PLAT-02 requirement — relocate `security` package to `platform.security`. This is the largest wave in Phase 62 (164 source files) with the highest infrastructure.web coupling because the 3 web filters moved in Phase 61 directly import `security.common.util.TenantContext` and `security.config.AppEndpoints`.

Output: Populated `platform.security` package; old `security/` directory deleted; infrastructure.web cascade complete; `mvn verify` green; Phase 62 ends with all 5 PLAT requirements satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-platform-layer-reorganization/62-RESEARCH.md
@.planning/phases/61-infrastructure-layer-creation/61-03-SUMMARY.md

<interfaces>
Source package being moved (145 production files across 8 sub-packages):
  - security/api/* (REST resources for auth, password reset, account changes)
  - security/audit/* (audit listeners, hash chain — note: filter/LoggingFilter already moved to infrastructure.web in Phase 61)
  - security/common/util/* (8 files including TenantContext.java, SecurityConstants.java, RequestIdProvider.java, RequestMetadataProvider.java, ClientContextProvider.java, CookieUtil.java, RequestMetadata.java, SessionIdAuditEntityListener.java)
  - security/config/* (5 files: AppEndpoints.java, CorsConfig.java, MvcConfig.java, SecurityConfiguration.java, SimpleGrantedAuthorityMixin.java)
  - security/contract/* (DTOs and event records)
  - security/infrastructure/* (JWT filters, auth handlers, principal resolvers — NOT including web filters which already moved)
  - security/repo/* (User, Role, Authority entities and repositories)
  - security/service/* (UserService, AuthenticationService, JwtTokenProvider, etc.)

Test files (19 from src/test/java/com/softropic/payam/security/).

External callers (verified by `grep -rl "com.softropic.payam.security\." src --include="*.java" | grep -v "/security/"`):
  - 23 production files outside security/ — including admin/ files (importing SecurityConstants — already updated in Plan 03), email-related security.api references (already updated in Plan 02), platform-flat references (already updated in Plan 03), common.client.RestRequestInterceptor, plus the 3 infrastructure.web files.
  - 7 test files outside security/.

CRITICAL infrastructure.web cascade (research Pattern 2):
After Phase 61 moved 3 filter classes to infrastructure.web, those classes still import from security.common.util and security.config. When PLAT-02 moves security to platform.security, these 4 import lines MUST be updated in the SAME commit:

  infrastructure/web/ApiKeyAuthenticationFilter.java:
    - `import com.softropic.payam.security.common.util.TenantContext;`  →  `import com.softropic.payam.platform.security.common.util.TenantContext;`
    - `import com.softropic.payam.security.config.AppEndpoints;`         →  `import com.softropic.payam.platform.security.config.AppEndpoints;`

  infrastructure/web/LoggingFilter.java:
    - `import com.softropic.payam.security.common.util.TenantContext;`  →  `import com.softropic.payam.platform.security.common.util.TenantContext;`

  infrastructure/web/TenantSecurityConfig.java:
    - `import com.softropic.payam.security.config.AppEndpoints;`         →  `import com.softropic.payam.platform.security.config.AppEndpoints;`

NON-CASCADE: platform.security.config.SecurityConfiguration imports `com.softropic.payam.infrastructure.web.LoggingFilter` (already correct from Phase 61). After moving SecurityConfiguration to platform.security.config, that import line stays unchanged.

Phase 61 carry-forward to preserve: FilterRegistrationBean(setEnabled=false) on ApiKeyAuthenticationFilter — preventing global servlet auto-registration. Do NOT modify any annotation, @Bean method body, or registration logic in infrastructure.web filter files; only import lines change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move security production and test files into platform.security (preserve all 8 sub-packages)</name>
  <files>
    src/main/java/com/softropic/payam/platform/security/** (145 new production files),
    src/test/java/com/softropic/payam/platform/security/** (19 new test files),
    src/main/java/com/softropic/payam/security/** (delete all),
    src/test/java/com/softropic/payam/security/** (delete all)
  </files>
  <read_first>
    src/main/java/com/softropic/payam/security/config/SecurityConfiguration.java,
    src/main/java/com/softropic/payam/security/config/AppEndpoints.java,
    src/main/java/com/softropic/payam/security/common/util/TenantContext.java,
    src/main/java/com/softropic/payam/security/common/util/SecurityConstants.java,
    src/main/java/com/softropic/payam/security/audit,
    .planning/phases/62-platform-layer-reorganization/62-RESEARCH.md
  </read_first>
  <action>
    Create `src/main/java/com/softropic/payam/platform/security/` and `src/test/java/com/softropic/payam/platform/security/` directories.

    Move ALL files under `src/main/java/com/softropic/payam/security/` to `src/main/java/com/softropic/payam/platform/security/`, preserving every sub-directory structure verbatim. The 8 sub-packages (api, audit, common, config, contract, infrastructure, repo, service) all keep their internal layout — including any nested sub-paths like `audit/listener/*.java`, `common/util/*.java`, `infrastructure/jwt/*.java`, etc.

    For each moved file:
    - Replace the package declaration line. Examples:
      - `package com.softropic.payam.security.config;`            →  `package com.softropic.payam.platform.security.config;`
      - `package com.softropic.payam.security.common.util;`       →  `package com.softropic.payam.platform.security.common.util;`
      - `package com.softropic.payam.security.audit.listener;`    →  `package com.softropic.payam.platform.security.audit.listener;`
      - `package com.softropic.payam.security.api;`               →  `package com.softropic.payam.platform.security.api;`
    - Replace EVERY occurrence of `import com.softropic.payam.security.` (within the moved file's own source) with `import com.softropic.payam.platform.security.` (preserving sub-package suffix).
    - Preserve all other imports byte-for-byte. In particular: `import com.softropic.payam.infrastructure.web.LoggingFilter;` in `SecurityConfiguration.java` stays as-is.

    Same operation for test files: move all 19 files from `src/test/java/com/softropic/payam/security/` to `src/test/java/com/softropic/payam/platform/security/`, preserving sub-paths and updating package declarations.

    Annotation/identity preservation:
    - All `@Component`, `@Service`, `@RestController`, `@Configuration`, `@Bean`, `@Order`, `@Conditional*`, `@EnableWebSecurity`, `@EnableMethodSecurity` annotations stay byte-for-byte identical.
    - All bean names (string arguments to `@Component("name")`, `@Bean(name="name")`, `@Qualifier("name")`) stay identical.
    - All JPA annotations (`@Entity`, `@Table`, `@Column`, `@Id`, `@GeneratedValue`) stay identical.

    Directory deletion: After moves, delete `src/main/java/com/softropic/payam/security/` (entire tree) and `src/test/java/com/softropic/payam/security/` (entire tree). Verify each is empty before deletion.

    Do NOT update infrastructure.web or other external callers in this task — that is Task 2.
  </action>
  <verify>
    <automated>[ "$(find src/main/java/com/softropic/payam/platform/security -name '*.java' | wc -l | tr -d ' ')" = "145" ] && [ "$(find src/test/java/com/softropic/payam/platform/security -name '*.java' | wc -l | tr -d ' ')" = "19" ] && ! test -d src/main/java/com/softropic/payam/security && ! test -d src/test/java/com/softropic/payam/security && grep -q "^package com.softropic.payam.platform.security.config;" src/main/java/com/softropic/payam/platform/security/config/SecurityConfiguration.java && grep -q "^package com.softropic.payam.platform.security.config;" src/main/java/com/softropic/payam/platform/security/config/AppEndpoints.java && grep -q "^package com.softropic.payam.platform.security.common.util;" src/main/java/com/softropic/payam/platform/security/common/util/TenantContext.java</automated>
  </verify>
  <acceptance_criteria>
    - `find src/main/java/com/softropic/payam/platform/security -name '*.java' | wc -l` outputs `145`
    - `find src/test/java/com/softropic/payam/platform/security -name '*.java' | wc -l` outputs `19`
    - `test -d src/main/java/com/softropic/payam/security` returns false
    - `test -d src/test/java/com/softropic/payam/security` returns false
    - `head -1 src/main/java/com/softropic/payam/platform/security/config/SecurityConfiguration.java` outputs `package com.softropic.payam.platform.security.config;`
    - `head -1 src/main/java/com/softropic/payam/platform/security/config/AppEndpoints.java` outputs `package com.softropic.payam.platform.security.config;`
    - `head -1 src/main/java/com/softropic/payam/platform/security/common/util/TenantContext.java` outputs `package com.softropic.payam.platform.security.common.util;`
    - `head -1 src/main/java/com/softropic/payam/platform/security/common/util/SecurityConstants.java` outputs `package com.softropic.payam.platform.security.common.util;`
    - `grep -q "import com.softropic.payam.infrastructure.web.LoggingFilter;" src/main/java/com/softropic/payam/platform/security/config/SecurityConfiguration.java` returns 0 (Phase 61 import preserved)
    - All 8 sub-package directories exist under `src/main/java/com/softropic/payam/platform/security/`: api, audit, common, config, contract, infrastructure, repo, service (verify with `find src/main/java/com/softropic/payam/platform/security -maxdepth 1 -mindepth 1 -type d | wc -l` outputs at least 8)
    - `grep -q "@EnableWebSecurity" src/main/java/com/softropic/payam/platform/security/config/SecurityConfiguration.java` returns 0 (annotation preserved)
  </acceptance_criteria>
  <done>
    All 145 production + 19 test files moved into platform.security; sub-package structure preserved; package declarations and within-package imports rewritten; old security/ directories deleted; infrastructure.web import to LoggingFilter unchanged in SecurityConfiguration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update infrastructure.web cascade (4 imports across 3 files) and 30 external callers, run mvn verify</name>
  <files>
    src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java,
    src/main/java/com/softropic/payam/infrastructure/web/LoggingFilter.java,
    src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java,
    All other .java files under src/ that import com.softropic.payam.security.* — discovered via grep
  </files>
  <read_first>
    src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java,
    src/main/java/com/softropic/payam/infrastructure/web/LoggingFilter.java,
    src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java
  </read_first>
  <action>
    Update all stale `com.softropic.payam.security.*` imports across the codebase, with extra care for the infrastructure.web cascade.

    Step A — infrastructure.web cascade (4 specific lines, MUST happen in this commit):

    1. In `src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java`:
       - Replace `import com.softropic.payam.security.common.util.TenantContext;` with `import com.softropic.payam.platform.security.common.util.TenantContext;`
       - Replace `import com.softropic.payam.security.config.AppEndpoints;` with `import com.softropic.payam.platform.security.config.AppEndpoints;`
       - Preserve the FilterRegistrationBean(setEnabled=false) pattern verbatim (only the import lines change; @Component, @Order, doFilterInternal, security check logic remain byte-for-byte identical).

    2. In `src/main/java/com/softropic/payam/infrastructure/web/LoggingFilter.java`:
       - Replace `import com.softropic.payam.security.common.util.TenantContext;` with `import com.softropic.payam.platform.security.common.util.TenantContext;`
       - Preserve all other code byte-for-byte.

    3. In `src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java`:
       - Replace `import com.softropic.payam.security.config.AppEndpoints;` with `import com.softropic.payam.platform.security.config.AppEndpoints;`
       - Preserve `FilterRegistrationBean(registration.setEnabled(false))` pattern, `@Bean`, `@Order(1)`, `securityMatcher(...)`, and `addFilterBefore(...)` calls byte-for-byte.

    Step B — sweep all other external callers:

    4. Run `grep -rln "com.softropic.payam.security\." src --include="*.java"` to find every remaining file with stale security imports (after Plans 02 and 03, the count should be approximately 30 minus any already-updated; in any case all matches must be eliminated).
    5. For each file in that list, replace EVERY occurrence of `com.softropic.payam.security.` with `com.softropic.payam.platform.security.` (preserve sub-package path: `security.common.util.SecurityConstants` becomes `platform.security.common.util.SecurityConstants`).
    6. This applies to both `import` statements and any fully-qualified-name references in code/Javadoc.

    Step C — verify and build:

    7. `grep -rn "com.softropic.payam.security\." src --include="*.java"` must return zero matches.
    8. `mvn -q clean compile` must exit 0.
    9. `mvn -q test-compile` must exit 0.
    10. `mvn verify` must match Phase 61 baseline (green if Docker available; otherwise pre-existing 33 integration test failures only — no NEW failures).

    Step D — Phase completion verification (Phase 62 ends with this plan):

    11. `find src/main/java/com/softropic/payam -maxdepth 1 -type d -name "tenant" -o -name "security" -o -name "email" -o -name "alert" -o -name "health" -o -name "ops" -o -name "admin"` must return nothing — all 7 source flat-package directories deleted.
    12. `find src/main/java/com/softropic/payam/platform -maxdepth 1 -type d -mindepth 1` must list exactly 5 directories: tenant, security, notification, monitoring, admin.
    13. STATE.md key carry-forward verified: FilterRegistrationBean(setEnabled=false) still present in TenantSecurityConfig.

    Commit message: `refactor(62-05): PLAT-02 — move security to platform.security with infrastructure.web cascade`.
  </action>
  <verify>
    <automated>[ "$(grep -rn 'com.softropic.payam.security\.' src --include='*.java' | wc -l | tr -d ' ')" = "0" ] && grep -q "import com.softropic.payam.platform.security.common.util.TenantContext;" src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java && grep -q "import com.softropic.payam.platform.security.config.AppEndpoints;" src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java && grep -q "import com.softropic.payam.platform.security.common.util.TenantContext;" src/main/java/com/softropic/payam/infrastructure/web/LoggingFilter.java && grep -q "import com.softropic.payam.platform.security.config.AppEndpoints;" src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java && grep -q "registration.setEnabled(false)" src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java && mvn -q clean compile && mvn -q test-compile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "com.softropic.payam.security\." src --include="*.java"` returns no matches (zero stale imports)
    - `grep -q "import com.softropic.payam.platform.security.common.util.TenantContext;" src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java` returns 0
    - `grep -q "import com.softropic.payam.platform.security.config.AppEndpoints;" src/main/java/com/softropic/payam/infrastructure/web/ApiKeyAuthenticationFilter.java` returns 0
    - `grep -q "import com.softropic.payam.platform.security.common.util.TenantContext;" src/main/java/com/softropic/payam/infrastructure/web/LoggingFilter.java` returns 0
    - `grep -q "import com.softropic.payam.platform.security.config.AppEndpoints;" src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java` returns 0
    - `grep -q "registration.setEnabled(false)" src/main/java/com/softropic/payam/infrastructure/web/TenantSecurityConfig.java` returns 0 (Phase 61 carry-forward preserved)
    - `mvn -q clean compile` exits 0
    - `mvn -q test-compile` exits 0
    - `mvn verify` exits 0 if Docker available, OR matches Phase 61 baseline (33 known Testcontainers failures only — no NEW failures)
    - Phase 62 closure check: `for d in tenant security email alert health ops admin; do test ! -d src/main/java/com/softropic/payam/$d; done` all return success (none of the old flat dirs remain)
    - `find src/main/java/com/softropic/payam/platform -maxdepth 1 -mindepth 1 -type d | sort | tr '\n' ' '` outputs paths ending with `admin monitoring notification security tenant` (5 platform sub-packages)
  </acceptance_criteria>
  <done>
    Zero stale `com.softropic.payam.security.*` imports anywhere. infrastructure.web cascade (4 lines across 3 files) verified explicitly. FilterRegistrationBean(setEnabled=false) preserved. Compilation green. Phase 62 reaches its closure state with all 5 platform sub-packages populated and all 7 source flat packages deleted.
  </done>
</task>

</tasks>

<verification>
After both tasks: `find src -type d -name "security" -path "*/com/softropic/payam/security"` returns nothing. `grep -rn "com.softropic.payam.security\." src --include="*.java"` returns zero matches. `find src/main/java/com/softropic/payam/platform -maxdepth 1 -mindepth 1 -type d` lists exactly 5 directories (admin, monitoring, notification, security, tenant). FilterRegistrationBean(setEnabled=false) still present in `infrastructure/web/TenantSecurityConfig.java`.
</verification>

<success_criteria>
- 145 production files and 19 test files relocated under `platform/security/` with all 8 sub-packages preserved (api, audit, common, config, contract, infrastructure, repo, service).
- Old `security/` directories absent from `src/main/java` and `src/test/java`.
- infrastructure.web cascade complete: 4 import lines updated across 3 files (ApiKeyAuthenticationFilter, LoggingFilter, TenantSecurityConfig).
- FilterRegistrationBean(setEnabled=false) on ApiKeyAuthenticationFilter registration preserved verbatim.
- All 30 external callers (23 prod + 7 test) updated.
- `mvn verify` matches Phase 61 baseline (green if Docker available).
- PLAT-02 requirement satisfied.
- **Phase 62 complete:** 5 platform sub-packages (admin, monitoring, notification, security, tenant) all populated; 7 source flat packages (admin, alert, email, health, ops, security, tenant) and the old flat platform/api+config+contract+repo+service all deleted.
</success_criteria>

<output>
After completion, create `.planning/phases/62-platform-layer-reorganization/62-05-SUMMARY.md` capturing the move details: file counts (145 prod + 19 test), infrastructure.web cascade lines updated (4 lines across 3 files), Phase 61 carry-forward preservation (FilterRegistrationBean), Phase 62 final state (5 platform sub-packages populated, 7 flat dirs deleted), and any deviations.
</output>

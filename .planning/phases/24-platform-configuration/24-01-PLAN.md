---
phase: 24-platform-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/db/migration/V17__platform_config_schema.sql
  - src/main/java/com/softropic/payam/platform/repo/PlatformConfig.java
  - src/main/java/com/softropic/payam/platform/repo/PlatformConfigRepository.java
  - src/main/java/com/softropic/payam/platform/contract/PlatformConfigDto.java
  - src/main/java/com/softropic/payam/platform/contract/event/PlatformConfigChangedEvent.java
  - src/main/java/com/softropic/payam/platform/config/PayamPlatformProperties.java
  - src/main/java/com/softropic/payam/platform/service/PlatformConfigService.java
  - src/main/java/com/softropic/payam/platform/api/PlatformConfigAdminResource.java
  - src/main/resources/application.yaml
  - src/main/resources/application-dev.yaml
autonomous: true

must_haves:
  truths:
    - "GET /v1/admin/platform-config returns a list of two objects, one for ORANGE and one for MTN"
    - "PUT /v1/admin/platform-config/ORANGE with a new MSISDN updates the Orange row and returns the updated object"
    - "PUT /v1/admin/platform-config/MTN with a new MSISDN updates the MTN row and returns the updated object"
    - "A non-admin JWT receives 403 from both GET and PUT endpoints"
    - "Application starts without error (payam.platform.notification-email resolves from YAML)"
  artifacts:
    - path: "src/main/resources/db/migration/V17__platform_config_schema.sql"
      provides: "platform_config table DDL + seed rows for ORANGE and MTN"
      contains: "CREATE TABLE IF NOT EXISTS main.platform_config"
    - path: "src/main/java/com/softropic/payam/platform/repo/PlatformConfig.java"
      provides: "JPA entity mapped to main.platform_config"
      exports: ["PlatformConfig"]
    - path: "src/main/java/com/softropic/payam/platform/repo/PlatformConfigRepository.java"
      provides: "Spring Data JPA repository with findByProvider"
      exports: ["PlatformConfigRepository"]
    - path: "src/main/java/com/softropic/payam/platform/contract/PlatformConfigDto.java"
      provides: "Request/response record for GET and PUT"
      exports: ["PlatformConfigDto"]
    - path: "src/main/java/com/softropic/payam/platform/contract/event/PlatformConfigChangedEvent.java"
      provides: "Spring ApplicationEvent published on MSISDN update"
      exports: ["PlatformConfigChangedEvent"]
    - path: "src/main/java/com/softropic/payam/platform/config/PayamPlatformProperties.java"
      provides: "@ConfigurationProperties(prefix=payam.platform) with notificationEmail"
      exports: ["PayamPlatformProperties"]
    - path: "src/main/java/com/softropic/payam/platform/service/PlatformConfigService.java"
      provides: "findAll() and update(provider, msisdn) business logic"
      exports: ["PlatformConfigService"]
    - path: "src/main/java/com/softropic/payam/platform/api/PlatformConfigAdminResource.java"
      provides: "GET /v1/admin/platform-config and PUT /v1/admin/platform-config/{provider}"
      exports: ["PlatformConfigAdminResource"]
  key_links:
    - from: "PlatformConfigAdminResource"
      to: "PlatformConfigService"
      via: "constructor injection, calls findAll() and update()"
      pattern: "platformConfigService\\.findAll|platformConfigService\\.update"
    - from: "PlatformConfigService.update()"
      to: "PlatformConfigChangedEvent"
      via: "applicationEventPublisher.publishEvent(new PlatformConfigChangedEvent(...))"
      pattern: "publishEvent.*PlatformConfigChangedEvent"
    - from: "PlatformConfigService"
      to: "PlatformConfigRepository"
      via: "constructor injection, calls findByProvider()"
      pattern: "platformConfigRepository\\.findByProvider"
---

<objective>
Create the backend foundation for platform configuration: Flyway migration V17 that creates and seeds
the platform_config table, the JPA entity and repository, the DTO, the domain event, the
@ConfigurationProperties bean, the service, and the REST controller.

Purpose: This plan delivers the complete backend for PCONF-01 (view MSISDNs), PCONF-02 (update Orange
MSISDN), and PCONF-03 (update MTN MSISDN). PCONF-04 (email notification) is wired by the event
published here and handled in plan 24-02.

Output: A working admin REST API at /v1/admin/platform-config that admin users can call to read and
update platform MSISDNs. The application starts cleanly with the new payam.platform.notification-email
property.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-platform-configuration/24-RESEARCH.md

# Key existing patterns to follow exactly:
@src/main/resources/db/migration/V14__fee_rule_schema.sql
@src/main/java/com/softropic/payam/fee/repo/FeeRule.java
@src/main/java/com/softropic/payam/common/persistence/AbstractAuditingEntity.java
@src/main/java/com/softropic/payam/admin/api/AdminTransactionResource.java
@src/main/java/com/softropic/payam/orange/config/OrangeMoneyConfig.java
@src/main/java/com/softropic/payam/security/common/util/SecurityConstants.java
@src/main/resources/application.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway V17 migration + JPA entity + repository</name>
  <files>
    src/main/resources/db/migration/V17__platform_config_schema.sql
    src/main/java/com/softropic/payam/platform/repo/PlatformConfig.java
    src/main/java/com/softropic/payam/platform/repo/PlatformConfigRepository.java
  </files>
  <action>
Create V17__platform_config_schema.sql in src/main/resources/db/migration/:

```sql
CREATE TABLE IF NOT EXISTS main.platform_config (
    id                  BIGINT       NOT NULL PRIMARY KEY,
    version             BIGINT       NOT NULL DEFAULT 0,
    created_by          VARCHAR(50),
    created_date        TIMESTAMP,
    last_modified_by    VARCHAR(50),
    last_modified_date  TIMESTAMP,
    request_id          VARCHAR(255),
    session_id          TEXT,
    status              VARCHAR(20)  NOT NULL DEFAULT 'ACTIVE',

    provider            VARCHAR(20)  NOT NULL UNIQUE,   -- 'ORANGE' | 'MTN'
    platform_msisdn     VARCHAR(20)  NOT NULL DEFAULT ''
);

INSERT INTO main.platform_config (id, version, provider, platform_msisdn, status)
VALUES
    (1, 0, 'ORANGE', '', 'ACTIVE'),
    (2, 0, 'MTN',    '', 'ACTIVE')
ON CONFLICT DO NOTHING;

COMMENT ON TABLE main.platform_config IS 'Platform-owned MSISDNs per provider; editable by admin without app restart';
COMMENT ON COLUMN main.platform_config.provider IS 'Provider key: ORANGE or MTN';
COMMENT ON COLUMN main.platform_config.platform_msisdn IS 'Platform-owned MSISDN for this provider; empty string until configured by admin';
```

Create PlatformConfig.java in package com.softropic.payam.platform.repo:
- Extends AbstractAuditingEntity (same as FeeRule.java pattern)
- Annotate: @Entity, @Table(name = "platform_config", schema = "main"), @SuperBuilder, @NoArgsConstructor, @AllArgsConstructor, @Getter, @Slf4j
- Fields (private, no setter except via JPA):
  - @Column(name = "provider", nullable = false, unique = true) private String provider;
  - @Column(name = "platform_msisdn", nullable = false) private String platformMsisdn;
- Add a business method: public void updateMsisdn(String newMsisdn) { this.platformMsisdn = newMsisdn; }
  This allows service to mutate the entity cleanly without a setter.

Create PlatformConfigRepository.java in package com.softropic.payam.platform.repo:
- Interface extending JpaRepository&lt;PlatformConfig, Long&gt;
- Declare: Optional&lt;PlatformConfig&gt; findByProvider(String provider);

Note: @Tsid on BaseEntity generates the Long ID — the seeded rows use id=1 and id=2 (manual in Flyway).
New rows inserted through the service will have TSID-generated IDs (but admin will never create rows;
the table is pre-seeded, so update() always finds an existing row).
  </action>
  <verify>
Application starts without FlywayException. Run:
./mvnw spring-boot:run -pl . 2>&amp;1 | grep -E "Flyway|ERROR|Started" | head -20
Or just: ./mvnw compile -q (no compile errors)
  </verify>
  <done>
V17 migration file exists and contains CREATE TABLE + two INSERT rows.
PlatformConfig.java compiles — extends AbstractAuditingEntity, has provider + platformMsisdn fields and updateMsisdn() method.
PlatformConfigRepository.java compiles — has findByProvider() method.
  </done>
</task>

<task type="auto">
  <name>Task 2: DTO + Event + Properties + Service + Controller</name>
  <files>
    src/main/java/com/softropic/payam/platform/contract/PlatformConfigDto.java
    src/main/java/com/softropic/payam/platform/contract/event/PlatformConfigChangedEvent.java
    src/main/java/com/softropic/payam/platform/config/PayamPlatformProperties.java
    src/main/java/com/softropic/payam/platform/service/PlatformConfigService.java
    src/main/java/com/softropic/payam/platform/api/PlatformConfigAdminResource.java
    src/main/resources/application.yaml
    src/main/resources/application-dev.yaml
  </files>
  <action>
Create PlatformConfigDto.java in package com.softropic.payam.platform.contract:
```java
public record PlatformConfigDto(String provider, String platformMsisdn) {}
```
No @Valid annotations needed for GET response. For PUT body, the controller accepts this same record.

Create PlatformConfigChangedEvent.java in package com.softropic.payam.platform.contract.event:
```java
public record PlatformConfigChangedEvent(String provider, String oldMsisdn, String newMsisdn) {}
```
This is a plain record (not extending ApplicationEvent — Spring 4.2+ supports POJO events).

Create PayamPlatformProperties.java in package com.softropic.payam.platform.config:
- @ConfigurationProperties(prefix = "payam.platform")
- Field: private String notificationEmail; with getter and setter
- Matches OrangeMoneyConfig.java pattern exactly (no @Component — registered via @EnableConfigurationProperties
  or entry in META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports,
  OR add @Configuration to the class itself)
- Use @Configuration on the class (simplest — matches how OrangeMoneyConfig is registered; check
  src/main/java/com/softropic/payam/orange/config/OrangeMoneyConfig.java registration pattern to confirm)

Create PlatformConfigService.java in package com.softropic.payam.platform.service:
- @Service, @Transactional (class level), @Slf4j, @RequiredArgsConstructor
- Constructor-injected: PlatformConfigRepository platformConfigRepository, ApplicationEventPublisher eventPublisher
- Method findAll():
  - @Transactional(readOnly = true)
  - return platformConfigRepository.findAll().stream()
      .map(c -> new PlatformConfigDto(c.getProvider(), c.getPlatformMsisdn()))
      .toList();
- Method update(String provider, String newMsisdn):
  - String upper = provider.toUpperCase();
  - PlatformConfig config = platformConfigRepository.findByProvider(upper)
      .orElseThrow(() -> new IllegalArgumentException("Unknown provider: " + upper));
  - String oldMsisdn = config.getPlatformMsisdn();
  - config.updateMsisdn(newMsisdn);
  - platformConfigRepository.save(config);
  - eventPublisher.publishEvent(new PlatformConfigChangedEvent(upper, oldMsisdn, newMsisdn));
  - log.info("Platform MSISDN updated", kv("provider", upper), kv("event", "platform_config_updated"));
  - return new PlatformConfigDto(upper, newMsisdn);
  - NOTE: Publish the event INSIDE the @Transactional method so @TransactionalEventListener(AFTER_COMMIT)
    fires correctly. Do NOT call publishEvent outside a transaction.

Create PlatformConfigAdminResource.java in package com.softropic.payam.platform.api:
- Exact pattern of AdminTransactionResource.java
- @RestController, @RequestMapping("/v1/admin/platform-config"), @PreAuthorize(SecurityConstants.HAS_ADMIN_ROLE), @RequiredArgsConstructor
- GET mapping: returns ResponseEntity&lt;List&lt;PlatformConfigDto&gt;&gt; from platformConfigService.findAll()
- PUT mapping: @PutMapping("/{provider}"), @PathVariable String provider, @RequestBody PlatformConfigDto dto
  - returns ResponseEntity.ok(platformConfigService.update(provider, dto.platformMsisdn()))

Add to application.yaml (at root level, after the last top-level block):
```yaml
payam:
  platform:
    notification-email: ${PLATFORM_NOTIFICATION_EMAIL:admin@example.com}
```

Add the same to application-dev.yaml if it exists; if not, application.yaml is sufficient.
  </action>
  <verify>
./mvnw compile -q
No compilation errors. If compilation succeeds, the wiring is correct.
  </verify>
  <done>
All 5 Java classes compile without error.
application.yaml contains payam.platform.notification-email.
GET /v1/admin/platform-config (with valid admin JWT) returns HTTP 200 with JSON array of 2 objects
  (provider=ORANGE and provider=MTN) after running the application against a test DB.
PUT /v1/admin/platform-config/ORANGE with body {"provider":"ORANGE","platformMsisdn":"692000001"}
  returns HTTP 200 with updated platformMsisdn.
  </done>
</task>

</tasks>

<verification>
Run ./mvnw compile -q — zero errors.
Application starts (./mvnw spring-boot:run) — no BeanCreationException for PayamPlatformProperties,
no FlywayException for V17.
curl -H "Authorization: Bearer &lt;admin-jwt&gt;" http://localhost:9990/v1/admin/platform-config
  → 200 [{provider: ORANGE, ...}, {provider: MTN, ...}]
curl -H "Authorization: Bearer &lt;client-jwt&gt;" http://localhost:9990/v1/admin/platform-config
  → 403
</verification>

<success_criteria>
1. ./mvnw compile produces zero errors.
2. V17__platform_config_schema.sql exists with CREATE TABLE + two INSERT rows.
3. GET /v1/admin/platform-config returns 2-element list for admin; 403 for non-admin.
4. PUT /v1/admin/platform-config/ORANGE persists new MSISDN (visible on subsequent GET).
5. PlatformConfigChangedEvent is published inside the @Transactional update() method.
6. Application starts without errors when payam.platform.notification-email is absent from env
   (falls back to admin@example.com default).
</success_criteria>

<output>
After completion, create .planning/phases/24-platform-configuration/24-01-SUMMARY.md
</output>

---
phase: 53-e2e-test-suite
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "POST /v1/disbursements with Orange MSISDN (+237691234567) and amount 5000 XAF returns HTTP 202 with status=PROCESSING"
    - "POST /v1/callbacks/orange/disbursement with SUCCESSFULL (Orange double-L) transitions disbursement to SUCCESS via @TransactionalEventListener(AFTER_COMMIT) within 10s"
    - "POST /v1/disbursements with amount > wallet balance returns HTTP 422 with errorCode=INSUFFICIENT_BALANCE and ZERO Orange /cashout calls"
    - "Replayed identical Orange callback (same payToken) returns 200 but does not trigger second state transition or second double-check GET to /mp/paymentstatus/{payToken}"
  artifacts:
    - path: src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java
      provides: "Full HTTP-level Orange disbursement lifecycle E2E (initiate → callback SUCCESSFULL/insufficient/replay)"
      contains: "class OrangeDisbursementE2EIT"
      min_lines: 250
  key_links:
    - from: src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java
      to: "/v1/disbursements"
      via: "TestRestTemplate POST with X-Api-Key + Idempotency-Key headers"
      pattern: "POST.*\\/v1\\/disbursements"
    - from: src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java
      to: "/v1/callbacks/orange/disbursement"
      via: "TestRestTemplate.exchange POST"
      pattern: "POST.*\\/v1\\/callbacks\\/orange\\/disbursement"
    - from: src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java
      to: "Orange WireMock /cashout"
      via: "verify(0, postRequestedFor(/cashout)) for insufficient-balance test"
      pattern: "postRequestedFor.*cashout"
---

<objective>
Create `OrangeDisbursementE2EIT` — a self-contained E2E integration test class that proves the full Orange Money disbursement HTTP lifecycle for TEST-02: initiate via POST /v1/disbursements → PROCESSING → Orange callback SUCCESSFULL → SUCCESS, plus insufficient-balance early-fail (no provider call) and Orange callback replay protection.

Purpose: Close TEST-02 — machine-verified end-to-end correctness for Orange disbursements at the HTTP layer.

Output: One new test class with 3 test methods (Orange happy path, insufficient balance, callback replay) that runs green in `mvn test -Dtest=OrangeDisbursementE2EIT`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-e2e-test-suite/53-RESEARCH.md
@.planning/phases/53-e2e-test-suite/53-VALIDATION.md
@src/test/java/com/softropic/payam/disbursement/service/DisbursementOrchestratorIT.java
@src/test/java/com/softropic/payam/disbursement/api/OrangeDisbursementCallbackControllerIT.java
@src/test/java/com/softropic/payam/disbursement/api/DisbursementResourceIT.java

<interfaces>
<!-- Confirmed Orange disbursement contracts -->

Orange WireMock stubs (CONFIRMED FROM CODEBASE — do NOT assume /ic2c/pay):
- POST /token (with header `Authorization: Basic ...`) → bearer JSON
- POST /infos/subscriber/customer/{nationalMsisdn} → `{"data":{"firstname":"Jean"},"message":"OK"}` (validateAccountHolder; nationalMsisdn = +237691234567 stripped to 691234567)
- POST /cashout → `{"status":"SUCCESS"}` (initiateDisbursement — confirmed in OrangeMoneyClient.java line 164: `String url = buildClientURL("/cashout");`)
- GET /mp/paymentstatus/{payToken} → `{"status":"SUCCESSFULL","pay_token":"{payToken}"}` (callback double-check; confirmed in OrangeDisbursementCallbackControllerIT line 147)

Orange callback HTTP contract (from OrangeDisbursementCallbackControllerIT lines 151-163):
- POST /v1/callbacks/orange/disbursement
- Headers: Content-Type: application/json, X-Notif-Token: tok-abc
- Body shape:
  `{"payToken":"{payToken}","notif_token":"tok-abc","status":"SUCCESSFULL","txnid":"TXN001","msisdn":"237691111111","amount":"5000","createtime":"2026-03-24T10:00:00"}`
- Returns 200 always; transition is async via AFTER_COMMIT listener
- Orange uses `SUCCESSFULL` (double-L) — OrangeStatusMapper maps it to DisbursementStatus.SUCCESS

Orange MSISDN constant: `+237691234567` (Orange prefix 69x).

DisbursementOrchestratorError.INSUFFICIENT_BALANCE.getErrorCode() = "INSUFFICIENT_BALANCE" (see DisbursementOrchestratorError enum).

Orange platform config seeding (required by OrangeMoneyPort.validateAccountHolder for channelMsisdn):
`platformConfigService.update("ORANGE", "691000000");`

422 status mapping: DisbursementResource.resolveHttpStatus returns HttpStatus.UNPROCESSABLE_ENTITY (422) for INSUFFICIENT_BALANCE. Body is DisbursementResponse JSON with errorCode field populated.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create OrangeDisbursementE2EIT — full Orange happy path, insufficient balance, callback replay</name>
  <files>src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java</files>
  <read_first>
    - src/test/java/com/softropic/payam/disbursement/service/DisbursementOrchestratorIT.java (CRITICAL — copy WireMock topology + JWT secret seed + wallet seed verbatim. Confirms Orange uses POST /cashout, not /ic2c/pay.)
    - src/test/java/com/softropic/payam/disbursement/api/OrangeDisbursementCallbackControllerIT.java (Orange callback dispatch helper, double-check stub, SUCCESSFULL double-L spelling, replay dedup pattern)
    - src/test/java/com/softropic/payam/disbursement/api/DisbursementResourceIT.java (HTTP-level POST pattern with X-Api-Key + Idempotency-Key)
    - src/main/java/com/softropic/payam/orange/infrastructure/OrangeMoneyClient.java (line 162-164 confirms `/cashout` is the disbursement URL)
    - src/main/java/com/softropic/payam/disbursement/contract/DisbursementOrchestratorError.java (errorCode constant for INSUFFICIENT_BALANCE)
  </read_first>
  <action>
    Create new file `src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java` with:

    PACKAGE: `com.softropic.payam.e2e.disbursement`

    CLASS-LEVEL ANNOTATIONS (copy verbatim from DisbursementOrchestratorIT lines 53-71 — full mtn-collection + mtn-disbursement + orange topology):
    ```java
    @ActiveProfiles({"dev", "test"})
    @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT,
                    properties = {"enable.test.mail=true"})
    @Import(TestConfig.class)
    @TestPropertySource(properties = {
        "spring.cloud.compatibility-verifier.enabled=false",
        "mtn.callback-ip-whitelist=",
        "orange.callback-ip-whitelist=",
        "mtn.collection-token-url=http://localhost:${wiremock.mtn.port}/token/collection",
        "mtn.disbursement-token-url=http://localhost:${wiremock.mtn.port}/token/disbursement"
    })
    @EnableWireMock({
        @ConfigureWireMock(name = "mtn",
            baseUrlProperties = {"mtn.collection-base-url", "mtn.disbursement-base-url"},
            portProperties    = {"wiremock.mtn.port"}),
        @ConfigureWireMock(name = "orange",
            baseUrlProperties = {"orange.base-url", "orange.pay-url"},
            portProperties    = {"wiremock.orange.port"})
    })
    class OrangeDisbursementE2EIT { ... }
    ```

    AUTOWIRED FIELDS: same set as plan 53-01 (TestRestTemplate, @LocalServerPort serverPort, mtnServer + orangeServer @InjectWireMock, TenantService, DisbursementRepository, MerchantWalletBalanceRepository, JdbcTemplate, TransactionTemplate, StringRedisTemplate, TestDataCleaner, PlatformConfigService).

    CONSTANTS:
    - `private static final String ORANGE_MSISDN = "+237691234567";`
    - `private static final BigDecimal PRINCIPAL = new BigDecimal("5000");`

    INSTANCE STATE:
    - `private Long tenantId;`
    - `private String rawApiKey;`

    @BeforeEach setUp():
    1. `testDataCleaner.wipeAll();`
    2. Seed JWT secret — copy the `INSERT INTO main.sec ...` block from DisbursementOrchestratorIT.setUp() lines 96-107 verbatim.
    3. `var created = tenantService.createTenant("orange-e2e-" + UUID.randomUUID(), ApiKeyEnvironment.PROD); tenantId = created.tenant().getId(); rawApiKey = created.rawKey();`
    4. Seed wallet with 1,000,000 XAF — copy DisbursementOrchestratorIT lines 114-122 verbatim.
    5. Stub MTN tokens (still required because mtn-disbursement-base-url is configured even though we don't call MTN paths in this test): copy DisbursementOrchestratorIT lines 125-128.
    6. Stub Orange token: `orangeServer.stubFor(post(urlPathEqualTo("/token")).withHeader("Authorization", containing("Basic")).willReturn(okJson("{\"access_token\":\"orange-bearer\",\"token_type\":\"Bearer\",\"expires_in\":3600}")));`
    7. Seed Orange platform config: `platformConfigService.update("ORANGE", "691000000");`
    8. Flush Redis as in 53-01.

    @AfterEach tearDown(): same as 53-01 (resetAll on both servers, flushDb, wipeAll).

    HELPER METHODS (private):

    1. `postDisbursement(String msisdn, BigDecimal amount, String reference, String idempotencyKey)` — same as 53-01 helper.

    2. `parseDisbursementId(String responseBody)` and `parseStatus(String responseBody)` and `parseErrorCode(String responseBody)` using ObjectMapper (return null when JSON field absent).

    3. `private void stubOrangeSubscriberAndCashout()`:
       ```
       orangeServer.stubFor(post(urlPathMatching("/infos/subscriber/customer/.*"))
           .willReturn(okJson("{\"data\":{\"firstname\":\"Jean\"},\"message\":\"OK\"}")));
       orangeServer.stubFor(post(urlPathEqualTo("/cashout"))
           .willReturn(okJson("{\"status\":\"SUCCESS\"}")));
       ```

    4. `private void stubOrangePaymentStatus(String payToken, String status)`:
       ```
       orangeServer.stubFor(get(urlPathMatching(".*/mp/paymentstatus/" + payToken))
           .willReturn(okJson("{\"status\":\"" + status + "\",\"pay_token\":\"" + payToken + "\"}")));
       ```

    5. `private ResponseEntity<Void> postOrangeCallback(String payToken, String status)`:
       Body: `String.format("{\"payToken\":\"%s\",\"notif_token\":\"tok-abc\",\"status\":\"%s\",\"txnid\":\"TXN001\",\"msisdn\":\"237691111111\",\"amount\":\"5000\",\"createtime\":\"2026-03-24T10:00:00\"}", payToken, status)`.
       Headers: Content-Type JSON, `X-Notif-Token: tok-abc`.
       Return `testRestTemplate.exchange("http://localhost:" + serverPort + "/v1/callbacks/orange/disbursement", HttpMethod.POST, new HttpEntity<>(body, headers), Void.class);`

    6. `private String fetchProviderRef(String disbursementId)` — `SELECT provider_ref FROM main.disbursement WHERE disbursement_id = ?`

    TEST 1 — `orangeHappyPath_initiateThenCallbackSuccessful_transitionsToSuccess()`:
    - `stubOrangeSubscriberAndCashout();`
    - postDisbursement(ORANGE_MSISDN, PRINCIPAL, "REF-OR-HAPPY-" + UUID.randomUUID(), "IDEM-OR-HAPPY-" + UUID.randomUUID()) → assert HTTP 202.
    - Parse body. Assert status == "PROCESSING".
    - Get providerRef = fetchProviderRef(disbursementId). Assert not null.
    - `stubOrangePaymentStatus(providerRef, "SUCCESSFULL");` (DOUBLE L)
    - postOrangeCallback(providerRef, "SUCCESSFULL") → assert HTTP 200.
    - Awaitility await DisbursementStatus.SUCCESS within 10s.
    - Verify exactly 1 cashout call: `assertThat(orangeServer.findAll(postRequestedFor(urlPathEqualTo("/cashout")))).hasSize(1);`

    TEST 2 — `insufficientBalance_returns422_andOrangeCashoutNotCalled()`:
    - DO NOT stub orange cashout (asserts no call attempted).
    - Stub Orange subscriber endpoint only: `orangeServer.stubFor(post(urlPathMatching("/infos/subscriber/customer/.*")).willReturn(okJson("{\"data\":{\"firstname\":\"Jean\"},\"message\":\"OK\"}")));`
    - Use `RestTemplate` with error-handler override so 4xx does NOT throw:
      ```java
      RestTemplate rt = new RestTemplate();
      rt.setErrorHandler(new DefaultResponseErrorHandler() {
          @Override public boolean hasError(HttpStatusCode statusCode) { return false; }
      });
      ```
      Build same headers/body as postDisbursement helper but with amount = 2,000,000 (exceeds 1,000,000 wallet). Use `rt.exchange(url, HttpMethod.POST, entity, String.class)`.
    - Assert response.getStatusCode().value() == 422.
    - Parse body, assert errorCode == DisbursementOrchestratorError.INSUFFICIENT_BALANCE.getErrorCode() (i.e. "INSUFFICIENT_BALANCE").
    - Assert ZERO cashout calls: `orangeServer.verify(0, postRequestedFor(urlPathEqualTo("/cashout")));`

    TEST 3 — `orangeReplayedCallback_isDeduplicated()`:
    - stubOrangeSubscriberAndCashout();
    - postDisbursement → 202, parse disbursementId, fetch providerRef.
    - stubOrangePaymentStatus(providerRef, "SUCCESSFULL");
    - First callback: postOrangeCallback(providerRef, "SUCCESSFULL") → 200.
    - Awaitility await SUCCESS state.
    - Second IDENTICAL callback: postOrangeCallback(providerRef, "SUCCESSFULL") → assert 200.
    - Verify dedup: `orangeServer.verify(1, getRequestedFor(urlPathMatching(".*/mp/paymentstatus/" + providerRef)));` (exactly 1 double-check).

    IMPORTS: same set as plan 53-01, plus `org.springframework.web.client.DefaultResponseErrorHandler, RestTemplate; org.springframework.http.HttpStatusCode` for Test 2.

    PITFALLS TO AVOID:
    - Orange disbursement endpoint is `/cashout`, NOT `/ic2c/pay`. Confirmed in OrangeMoneyClient.java line 164.
    - Orange success status is `SUCCESSFULL` (double-L), NOT `SUCCESSFUL`. Mapped via OrangeStatusMapper.
    - Orange callback double-check is GET `/mp/paymentstatus/{payToken}`, NOT a status-info endpoint.
    - Even though we don't call MTN paths, the @ConfigureWireMock for mtn-disbursement-base-url is REQUIRED — Spring context will fail to start if mtn.disbursement-base-url is unresolved.
    - The X-Notif-Token header `tok-abc` must match what the controller expects (matches OrangeDisbursementCallbackControllerIT pattern).
    - For 4xx assertion, TestRestTemplate's default behavior may swallow body. Use a fresh RestTemplate with DefaultResponseErrorHandler override.

    TARGET LINE COUNT: ~290-340 lines.
  </action>
  <verify>
    <automated>mvn test -pl . -Dtest=OrangeDisbursementE2EIT -q 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File `src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java` exists
    - `grep -c "@Test" src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java` returns exactly 3
    - `grep -c "@EnableWireMock" src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java` returns 1
    - `grep "/cashout" src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java` returns ≥ 2 matches (stub + verify-zero)
    - `grep "SUCCESSFULL" src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java` returns ≥ 2 matches (proves double-L spelling used)
    - `grep "INSUFFICIENT_BALANCE" src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java` returns ≥ 1 match
    - `grep "/mp/paymentstatus/" src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java` returns ≥ 1 match
    - `grep "/v1/callbacks/orange/disbursement" src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java` returns ≥ 1 match
    - `grep "verify(0" src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java` returns ≥ 1 match (proves "no provider call" assertion)
    - `grep "DefaultResponseErrorHandler" src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java` returns ≥ 1 match (proves 4xx capture pattern)
    - `grep -c "AbstractPayamE2ETest" src/test/java/com/softropic/payam/e2e/disbursement/OrangeDisbursementE2EIT.java` returns 0
    - `mvn test -pl . -Dtest=OrangeDisbursementE2EIT -q` exits 0 with 3 tests passed
  </acceptance_criteria>
  <done>
    - All 3 test methods pass via `mvn test -Dtest=OrangeDisbursementE2EIT`
    - Tests cover: Orange happy path (TEST-02 part 1), insufficient balance returns 422 with no provider call (TEST-02 part 2), Orange callback replay (TEST-02 part 3)
    - Orange-specific quirks honored: `/cashout` endpoint, `SUCCESSFULL` (double-L) status, `/mp/paymentstatus/{payToken}` double-check
  </done>
</task>

</tasks>

<verification>
- `mvn test -pl . -Dtest=OrangeDisbursementE2EIT -q` reports 3 tests passed, 0 failed
- No regression in existing Orange ITs: `mvn test -pl . -Dtest='Orange*'` reports 0 failures
- File satisfies all `acceptance_criteria` greps above
</verification>

<success_criteria>
TEST-02 closed at HTTP layer:
1. POST /v1/disbursements (Orange MSISDN, 5000 XAF) → 202 with status=PROCESSING; Orange /cashout called exactly once [proven by Test 1]
2. POST /v1/callbacks/orange/disbursement with SUCCESSFULL → SUCCESS within 10s [proven by Test 1]
3. POST /v1/disbursements with amount > balance → 422 errorCode=INSUFFICIENT_BALANCE; ZERO Orange /cashout calls [proven by Test 2]
4. Replayed Orange callback → 200 + only 1 double-check GET to /mp/paymentstatus/{payToken} [proven by Test 3]
</success_criteria>

<output>
After completion, create `.planning/phases/53-e2e-test-suite/53-02-SUMMARY.md` documenting:
- Test class created and 3 test methods
- Confirmed Orange endpoint paths (`/cashout`, `/mp/paymentstatus/{payToken}`, SUCCESSFULL double-L)
- Files modified
- Commit SHAs
</output>

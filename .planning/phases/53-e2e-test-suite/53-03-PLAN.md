---
phase: 53-e2e-test-suite
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "POST /v1/disbursements with amount > 500,000 XAF returns HTTP 202 with status=PENDING_CONFIRMATION; ZERO MTN /v1_0/transfer calls"
    - "POST /v1/disbursements/{id}/confirm transitions PENDING_CONFIRMATION → PROCESSING and triggers exactly 1 MTN transfer call"
    - "GET /v1/disbursements/{id} on PENDING_CONFIRMATION row returns status=PENDING_CONFIRMATION (DISB-02 read consistency)"
    - "POST /v1/disbursements/{id}/confirm on already-PROCESSING disbursement returns 422 errorCode=INVALID_STATE; no extra provider call"
  artifacts:
    - path: src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java
      provides: "Step-up flow E2E covering PENDING_CONFIRMATION gate, confirm dispatch, GET visibility, and INVALID_STATE rejection"
      contains: "class StepUpConfirmationE2EIT"
      min_lines: 200
  key_links:
    - from: src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java
      to: "/v1/disbursements/{id}/confirm"
      via: "TestRestTemplate.exchange POST"
      pattern: "POST.*\\/v1\\/disbursements\\/.*\\/confirm"
    - from: src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java
      to: "MTN WireMock /v1_0/transfer"
      via: "verify(0, postRequestedFor) before confirm; verify(1, ...) after confirm"
      pattern: "verify\\([0-9],.*\\/v1_0\\/transfer"
---

<objective>
Create `StepUpConfirmationE2EIT` — a self-contained E2E integration test class that proves the step-up confirmation flow for TEST-03 (HTTP layer): large amounts (> 500,000 XAF) gate into PENDING_CONFIRMATION without provider call; confirm endpoint dispatches; invalid-state confirm rejected.

Purpose: Close TEST-03 part 1 — step-up gate + confirm-dispatch lifecycle proven via real HTTP traffic.

Output: One new test class with 3 test methods that runs green in `mvn test -Dtest=StepUpConfirmationE2EIT`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-e2e-test-suite/53-RESEARCH.md
@.planning/phases/53-e2e-test-suite/53-VALIDATION.md
@src/test/java/com/softropic/payam/disbursement/service/DisbursementOrchestratorIT.java
@src/test/java/com/softropic/payam/disbursement/api/DisbursementResourceIT.java

<interfaces>
<!-- Step-up gate: amount > 500,000 XAF -->

DisbursementOrchestratorError values used here:
- INVALID_STATE.getErrorCode() == "INVALID_STATE" (returned by confirm() when disbursement not in PENDING_CONFIRMATION)

DisbursementResource HTTP contract:
- POST /v1/disbursements with amount > 500_000 XAF → 202 status=PENDING_CONFIRMATION (provider NOT called, wallet IS reserved). See DisbursementOrchestratorIT lines 220-246.
- POST /v1/disbursements/{id}/confirm → 202 status=PROCESSING (dispatches to MTN). See DisbursementOrchestratorIT lines 252-282.
- POST /v1/disbursements/{id}/confirm on PROCESSING disbursement → 422 errorCode=INVALID_STATE. See DisbursementOrchestratorIT lines 288-315.
- GET /v1/disbursements/{id} → 200 with status field. See DisbursementResource line 94-105.

MTN MSISDN: `+237671234567`
Step-up amount: 600,000 XAF (> 500,000 threshold).
Wallet seed: 1,000,000 XAF (must accommodate the step-up reservation).

Confirm endpoint behavior on PENDING_CONFIRMATION:
- Stubs MUST be in place BEFORE calling confirm: GET /v1_0/accountholder/MSISDN/.* → 200 {} ; POST /v1_0/transfer → 202.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create StepUpConfirmationE2EIT — step-up gate, confirm dispatch, INVALID_STATE rejection</name>
  <files>src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java</files>
  <read_first>
    - src/test/java/com/softropic/payam/disbursement/service/DisbursementOrchestratorIT.java (lines 53-148 for topology and setUp; lines 220-315 for step-up + confirm + invalid-state assertion patterns)
    - src/test/java/com/softropic/payam/disbursement/api/DisbursementResourceIT.java (lines 277-302 for HTTP-level confirm pattern)
    - src/main/java/com/softropic/payam/disbursement/contract/DisbursementOrchestratorError.java (INVALID_STATE constant)
    - src/main/java/com/softropic/payam/disbursement/api/DisbursementResource.java (lines 67-91 for POST endpoint, lines 94-105 for GET endpoint, lines 134-146 for confirm endpoint)
  </read_first>
  <action>
    Create new file `src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java` with:

    PACKAGE: `com.softropic.payam.e2e.disbursement`

    CLASS-LEVEL ANNOTATIONS: identical full topology block from DisbursementOrchestratorIT lines 53-71 (mtn-collection + mtn-disbursement + orange WireMock servers, both token URLs).

    AUTOWIRED FIELDS: same as 53-01 plan (TestRestTemplate, @LocalServerPort serverPort, mtnServer, orangeServer, TenantService, DisbursementRepository, MerchantWalletBalanceRepository, JdbcTemplate, TransactionTemplate, StringRedisTemplate, TestDataCleaner, PlatformConfigService).

    CONSTANTS:
    - `private static final String MTN_MSISDN = "+237671234567";`
    - `private static final BigDecimal STEP_UP_AMOUNT = new BigDecimal("600000");` (above 500,000 threshold)
    - `private static final BigDecimal SMALL_AMOUNT = new BigDecimal("5000");` (below threshold for INVALID_STATE test)

    INSTANCE STATE: `private Long tenantId; private String rawApiKey;`

    @BeforeEach setUp():
    1. testDataCleaner.wipeAll();
    2. Seed JWT secret (copy from DisbursementOrchestratorIT verbatim).
    3. Create tenant: `var created = tenantService.createTenant("stepup-e2e-" + UUID.randomUUID(), ApiKeyEnvironment.PROD); tenantId = created.tenant().getId(); rawApiKey = created.rawKey();`
    4. Seed wallet: 1,000,000 XAF (large enough to reserve 600,000+).
    5. Stub MTN tokens (collection + disbursement) and Orange token. Note: do NOT pre-stub /v1_0/transfer yet — Tests 1 and 2 use it; Test 3 sets it up.
    6. Seed Orange platform config: `platformConfigService.update("ORANGE", "691000000");`
    7. Flush Redis.

    @AfterEach: same as 53-01 (resetAll on both servers, flushDb, wipeAll).

    HELPER METHODS (private):

    1. `postDisbursement(BigDecimal amount, String reference, String idempotencyKey)` — same pattern as 53-01 (returns ResponseEntity<String>).

    2. `parseDisbursementId(String body)`, `parseStatus(String body)`, `parseErrorCode(String body)` via ObjectMapper.

    3. `private ResponseEntity<String> postConfirm(String disbursementId)`:
       ```
       HttpHeaders headers = new HttpHeaders();
       headers.set("X-Api-Key", rawApiKey);
       return testRestTemplate.exchange(
           "http://localhost:" + serverPort + "/v1/disbursements/" + disbursementId + "/confirm",
           HttpMethod.POST, new HttpEntity<>(headers), String.class);
       ```
       For Test 3 (INVALID_STATE expecting 422), use a separate `RestTemplate` with `DefaultResponseErrorHandler` override that returns hasError=false on all status codes.

    4. `private ResponseEntity<String> getDisbursement(String disbursementId)`:
       ```
       HttpHeaders headers = new HttpHeaders();
       headers.set("X-Api-Key", rawApiKey);
       return testRestTemplate.exchange(
           "http://localhost:" + serverPort + "/v1/disbursements/" + disbursementId,
           HttpMethod.GET, new HttpEntity<>(headers), String.class);
       ```

    5. `private void stubMtnAccountAndTransfer()`:
       ```
       mtnServer.stubFor(get(urlPathMatching("/v1_0/accountholder/MSISDN/.*")).willReturn(okJson("{}")));
       mtnServer.stubFor(post(urlPathEqualTo("/v1_0/transfer")).willReturn(aResponse().withStatus(202)));
       ```

    TEST 1 — `stepUpAmount_returnsPendingConfirmation_andDoesNotCallProvider()`:
    - DO NOT stub /v1_0/transfer (asserts no call).
    - postDisbursement(STEP_UP_AMOUNT, "REF-STEPUP-1-" + UUID.randomUUID(), "IDEM-STEPUP-1-" + UUID.randomUUID()) → assert HTTP 202.
    - Parse body. Assert status == "PENDING_CONFIRMATION", disbursementId not blank, errorCode is null.
    - Verify ZERO transfer calls: `mtnServer.verify(0, postRequestedFor(urlPathEqualTo("/v1_0/transfer")));`
    - Verify wallet IS reserved (balance reduced from 1,000,000 to less): `BigDecimal balance = jdbcTemplate.queryForObject("SELECT balance FROM main.merchant_wallet_balance WHERE tenant_id = ?", BigDecimal.class, tenantId); assertThat(balance).isLessThan(new BigDecimal("1000000"));`
    - GET /v1/disbursements/{id} → assert HTTP 200, parsed status == "PENDING_CONFIRMATION".

    TEST 2 — `confirmPendingDisbursement_dispatchesToProvider_andTransitionsToProcessing()`:
    - postDisbursement(STEP_UP_AMOUNT, "REF-STEPUP-CONFIRM-" + UUID.randomUUID(), "IDEM-STEPUP-CONFIRM-" + UUID.randomUUID()) → 202 PENDING_CONFIRMATION; capture disbursementId.
    - Verify ZERO MTN transfer calls before confirm: `mtnServer.verify(0, postRequestedFor(urlPathEqualTo("/v1_0/transfer")));`
    - Now stub MTN endpoints for confirm: `stubMtnAccountAndTransfer();`
    - Call postConfirm(disbursementId) → assert HTTP 202.
    - Parse body. Assert status == "PROCESSING", errorCode is null.
    - Verify exactly 1 MTN transfer call AFTER confirm: `mtnServer.verify(1, postRequestedFor(urlPathEqualTo("/v1_0/transfer")));`
    - Verify DB row: `String dbStatus = jdbcTemplate.queryForObject("SELECT disbursement_status FROM main.disbursement WHERE disbursement_id = ?", String.class, disbursementId); assertThat(dbStatus).isEqualTo("PROCESSING");`

    TEST 3 — `confirmAlreadyProcessingDisbursement_returns422_invalidState_noExtraProviderCall()`:
    - stubMtnAccountAndTransfer();
    - Submit a SMALL_AMOUNT (5000) disbursement: postDisbursement(SMALL_AMOUNT, "REF-SMALL-" + UUID.randomUUID(), "IDEM-SMALL-" + UUID.randomUUID()) → 202 PROCESSING.
    - Capture disbursementId.
    - Reset MTN call recordings: `mtnServer.resetAll();` then re-stub:
      ```
      mtnServer.stubFor(post(urlPathEqualTo("/token/collection")).willReturn(okJson("{\"access_token\":\"t\",\"token_type\":\"Bearer\",\"expires_in\":3600}")));
      mtnServer.stubFor(post(urlPathEqualTo("/token/disbursement")).willReturn(okJson("{\"access_token\":\"t\",\"token_type\":\"Bearer\",\"expires_in\":3600}")));
      ```
    - Use a `RestTemplate` with DefaultResponseErrorHandler override (since 422 expected). Configure same X-Api-Key header. Call POST /v1/disbursements/{disbursementId}/confirm.
    - Assert response.getStatusCode().value() == 422.
    - Parse body. Assert errorCode == DisbursementOrchestratorError.INVALID_STATE.getErrorCode() (i.e. "INVALID_STATE").
    - Verify no /v1_0/transfer call after the confirm attempt: `mtnServer.verify(0, postRequestedFor(urlPathEqualTo("/v1_0/transfer")));`

    IMPORTS: same import set as 53-01 + `org.springframework.web.client.{RestTemplate, DefaultResponseErrorHandler}` + `org.springframework.http.HttpStatusCode`.

    PITFALLS TO AVOID:
    - Do NOT pre-stub /v1_0/transfer in @BeforeEach if Test 1 needs to assert ZERO calls — stub it inside Test 2 / Test 3 only.
    - The confirm endpoint requires only X-Api-Key header (no Idempotency-Key — confirm is not idempotent at HTTP layer; idempotency is enforced via state).
    - Use TestRestTemplate for 2xx flows but RestTemplate+DefaultResponseErrorHandler for the 422 INVALID_STATE assertion.
    - mtnServer.resetAll() between sub-cases erases ALL stubs AND request log — re-stub token endpoints after reset.

    TARGET LINE COUNT: ~280-330 lines.
  </action>
  <verify>
    <automated>mvn test -pl . -Dtest=StepUpConfirmationE2EIT -q 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File `src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java` exists
    - `grep -c "@Test" src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java` returns exactly 3
    - `grep "PENDING_CONFIRMATION" src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java` returns ≥ 2 matches
    - `grep "/confirm" src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java` returns ≥ 2 matches
    - `grep "INVALID_STATE" src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java` returns ≥ 1 match
    - `grep "verify(0" src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java` returns ≥ 2 matches (proves no-provider-call assertions)
    - `grep "verify(1" src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java` returns ≥ 1 match (proves confirm-dispatched 1 transfer)
    - `grep "600000\|new BigDecimal..\"600000" src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java` returns ≥ 1 match (step-up amount above 500,000)
    - `grep -c "AbstractPayamE2ETest" src/test/java/com/softropic/payam/e2e/disbursement/StepUpConfirmationE2EIT.java` returns 0
    - `mvn test -pl . -Dtest=StepUpConfirmationE2EIT -q` exits 0 with 3 tests passed
  </acceptance_criteria>
  <done>
    - All 3 test methods pass
    - Tests cover: step-up gate (no provider call) [TEST-03 part 1], confirm dispatch (PENDING → PROCESSING + 1 transfer call) [TEST-03 part 2], INVALID_STATE on already-processed [TEST-03 part 3]
    - Wallet reservation observed pre-confirm (non-zero deduction) but provider not called
  </done>
</task>

</tasks>

<verification>
- `mvn test -pl . -Dtest=StepUpConfirmationE2EIT -q` reports 3 tests passed, 0 failed
- File satisfies all `acceptance_criteria` greps above
</verification>

<success_criteria>
TEST-03 part 1 closed (step-up gate + confirm dispatch + invalid state):
1. POST with amount=600,000 → 202 PENDING_CONFIRMATION; provider NOT called; wallet IS reserved [Test 1]
2. POST /confirm → 202 PROCESSING + exactly 1 MTN transfer call [Test 2]
3. POST /confirm on PROCESSING → 422 INVALID_STATE; no extra transfer call [Test 3]
</success_criteria>

<output>
After completion, create `.planning/phases/53-e2e-test-suite/53-03-SUMMARY.md` documenting:
- Test class created and 3 test methods
- Files modified
- Commit SHAs
</output>

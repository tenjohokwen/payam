---
phase: 35-idempotency-correctness
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified: []
autonomous: true
requirements:
  - IDEM-01
  - IDEM-02
must_haves:
  truths:
    - "Full mvn verify passes green — no regressions introduced by the IdempotencyService.store() rewrite"
    - "ConcurrentIdempotencyRaceTest still passes (existing 20-thread checkAndReserve race is unaffected)"
    - "PaymentIdempotencyE2ETest still passes (three-round idempotency scenario end-to-end)"
    - "All existing IdempotencyServiceIT tests plus the two new IDEM-01/IDEM-02 tests pass"
  artifacts:
    - path: ".planning/phases/35-idempotency-correctness/35-02-SUMMARY.md"
      provides: "Regression verification record — listing the commands run, test counts, pass/fail, and any flakes observed"
  key_links:
    - from: "IdempotencyService.store() (rewritten in 35-01)"
      to: "ConcurrentIdempotencyRaceTest (existing E2E race against checkAndReserve)"
      via: "full mvn verify runs both together"
      pattern: "checkAndReserve"
    - from: "IdempotencyService.store() (rewritten in 35-01)"
      to: "PaymentIdempotencyE2ETest (three-round idempotency scenario)"
      via: "full mvn verify"
      pattern: "PaymentIdempotencyE2ETest"
---

<objective>
Regression verification for Phase 35. Run the full test suite after plan 35-01 lands to prove the store() rewrite does not break any existing concurrency, idempotency, webhook, payment orchestration, or E2E test. This is a verification-only plan with no code changes.

Purpose: Plan 35-01 fixes a hot path that is touched by concurrent race tests, the end-to-end payment flow, and the three-round idempotency E2E test. The single-IT sampling in 35-01 proves the new behaviour but cannot prove the absence of regressions in callers of store(). A full `mvn verify` pass is required before phase sign-off.

Output: `35-02-SUMMARY.md` recording the mvn verify command, timestamp, total test count, pass/fail status, and any flaky or skipped tests observed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/35-idempotency-correctness/35-RESEARCH.md
@.planning/phases/35-idempotency-correctness/35-VALIDATION.md
@.planning/phases/35-idempotency-correctness/35-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full mvn verify and record results</name>
  <files>.planning/phases/35-idempotency-correctness/35-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/35-idempotency-correctness/35-01-PLAN.md (must_haves and verification section — the tests that must stay green)
    - .planning/phases/35-idempotency-correctness/35-VALIDATION.md (sampling policy — full suite is the phase gate)
  </read_first>
  <action>
Step 1 — Run the full test suite:
```
mvn -q verify
```
Capture exit code, total time, total tests, failures, errors, skipped counts from the Maven Surefire/Failsafe output summary lines.

Step 2 — Spot-check targeted tests (they are included in the full run above but verify their individual results in the surefire reports):
- `IdempotencyServiceIT` — must show 5 tests, 0 failures, 0 errors
- `ConcurrentIdempotencyRaceTest` — must show 0 failures, 0 errors (existing 20-thread race against checkAndReserve)
- `PaymentIdempotencyE2ETest` — must show 0 failures, 0 errors (three-round idempotency scenario)

If any of the three above fail or any NEW failure appears compared to the phase baseline (before 35-01), STOP and report — do not write the summary as a success.

Step 3 — Write `.planning/phases/35-idempotency-correctness/35-02-SUMMARY.md` with the following exact structure:

```markdown
# Phase 35-02 — Regression Verification Summary

**Date:** <ISO-8601 timestamp of the mvn verify run>
**Command:** `mvn -q verify`
**Exit code:** <0 or non-zero>
**Duration:** <from Maven output>

## Aggregate results
- Tests run: <N>
- Failures: <N>
- Errors: <N>
- Skipped: <N>

## Targeted tests (plan 35-01 blast radius)
| Test class | Tests | Failures | Errors | Status |
|---|---|---|---|---|
| IdempotencyServiceIT | 5 | 0 | 0 | PASS |
| ConcurrentIdempotencyRaceTest | <N> | 0 | 0 | PASS |
| PaymentIdempotencyE2ETest | <N> | 0 | 0 | PASS |

## New failures vs. baseline
<"None" if clean; otherwise list each failure with class, method, and message>

## Flakes / skips of note
<"None" or list>

## Sign-off
- [x] mvn verify exit 0
- [x] IdempotencyServiceIT has 5 tests (3 existing + 2 new IDEM-01/IDEM-02)
- [x] No new failures introduced by plan 35-01
```

Do not modify any production or test source code in this plan. If mvn verify fails, report the failure and stop; do not attempt to patch — any fixes require a new gap-closure plan.
  </action>
  <verify>
    <automated>mvn -q verify</automated>
  </verify>
  <acceptance_criteria>
    - `mvn -q verify` exits 0
    - File `.planning/phases/35-idempotency-correctness/35-02-SUMMARY.md` exists
    - Summary file contains exact substring `**Command:** ` + backtick + `mvn -q verify` + backtick
    - Summary file contains exact substring `**Exit code:** 0`
    - Summary file contains exact substring `IdempotencyServiceIT | 5 | 0 | 0 | PASS`
    - Summary file contains exact substring `ConcurrentIdempotencyRaceTest`
    - Summary file contains exact substring `PaymentIdempotencyE2ETest`
    - Summary file contains exact substring `## Sign-off`
    - IdempotencyServiceIT test count in surefire reports equals 5
  </acceptance_criteria>
  <done>
    Full mvn verify is green, the three target test classes are all green, and 35-02-SUMMARY.md records the run with the exact structure above.
  </done>
</task>

</tasks>

<verification>
- `mvn -q verify` exits 0
- `.planning/phases/35-idempotency-correctness/35-02-SUMMARY.md` exists and contains the sign-off block
- IdempotencyServiceIT has 5 tests (not 3) — proves Task 2 of 35-01 landed
- ConcurrentIdempotencyRaceTest still green — proves checkAndReserve race is unaffected by store() rewrite
- PaymentIdempotencyE2ETest still green — proves three-round idempotency end-to-end flow still works with new store() ordering
</verification>

<success_criteria>
Full suite green, summary file written, no new failures compared to the pre-35-01 baseline. Phase 35 is ready for `/gsd:verify-work`.
</success_criteria>

<output>
`.planning/phases/35-idempotency-correctness/35-02-SUMMARY.md` per the exact structure in Task 1 step 3.
</output>

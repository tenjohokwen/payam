---
phase: 17-code-standards-enforcement
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/softropic/payam/email/infrastructure/EmailRetryScheduler.java
  - src/main/java/com/softropic/payam/email/infrastructure/listener/AccountChangeEmailListener.java
  - src/main/java/com/softropic/payam/security/infrastructure/jwt/filter/JWTAuthenticationFilter.java
  - src/main/java/com/softropic/payam/security/service/ClientIdAccessDecisionManager.java
  - src/main/java/com/softropic/payam/security/infrastructure/audit/SpringSecurityAuditorAware.java
  - src/main/java/com/softropic/payam/security/infrastructure/FraudAwareAuthenticationManager.java
  - src/main/java/com/softropic/payam/common/validation/PhoneNumberValidator.java
  - src/main/java/com/softropic/payam/security/infrastructure/listener/HttpRequestCxtListener.java
  - src/main/java/com/softropic/payam/security/api/AccountResource.java
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "No log call in src/main/java uses {} placeholder interpolation (LOG-CODE-01)"
    - "No log call in src/main/java is a code-flow trace or ##### decorative statement (LOG-CODE-02)"
  artifacts:
    - path: "src/main/java/com/softropic/payam/email/infrastructure/EmailRetryScheduler.java"
      provides: "kv() structured arguments on all 4 formerly-{} log calls"
    - path: "src/main/java/com/softropic/payam/email/infrastructure/listener/AccountChangeEmailListener.java"
      provides: "kv() structured argument on formerly-{} log call"
    - path: "src/main/java/com/softropic/payam/security/infrastructure/jwt/filter/JWTAuthenticationFilter.java"
      provides: "Both ##### decorative log calls deleted; @Slf4j annotation and import removed"
    - path: "src/main/java/com/softropic/payam/security/service/ClientIdAccessDecisionManager.java"
      provides: "##### decorative log call deleted; @Slf4j annotation and import removed"
    - path: "src/main/java/com/softropic/payam/security/infrastructure/audit/SpringSecurityAuditorAware.java"
      provides: "##### decorative log call deleted; @Slf4j annotation and import removed"
    - path: "src/main/java/com/softropic/payam/security/infrastructure/FraudAwareAuthenticationManager.java"
      provides: "##### decorative log call deleted; @Slf4j annotation and import removed"
    - path: "src/main/java/com/softropic/payam/common/validation/PhoneNumberValidator.java"
      provides: "code-flow log.info deleted; valid log.warn retained"
    - path: "src/main/java/com/softropic/payam/security/infrastructure/listener/HttpRequestCxtListener.java"
      provides: "Both debug code-flow log calls deleted; @Slf4j annotation and import removed"
    - path: "src/main/java/com/softropic/payam/security/api/AccountResource.java"
      provides: "debug code-flow log call deleted; Logger field and both slf4j imports removed"
  key_links:
    - from: "EmailRetryScheduler.java"
      to: "net.logstash.logback.argument.StructuredArguments.kv"
      via: "static import"
      pattern: "import static net\\.logstash\\.logback\\.argument\\.StructuredArguments\\.kv"
---

<objective>
Close the two remaining LOG-CODE-01 and LOG-CODE-02 violations in 9 files that were outside the original audit scope.

Purpose: These 9 files were missed in the initial research audit. Fixing them brings the entire codebase into compliance with the logging standards defined for phase 17.

Output: Zero {} placeholder log calls remain anywhere under src/main/java. Zero code-flow or decorative ##### log calls remain anywhere under src/main/java.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-code-standards-enforcement/17-01-SUMMARY.md
@.planning/phases/17-code-standards-enforcement/17-02-SUMMARY.md
@.planning/phases/17-code-standards-enforcement/17-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert {} placeholder logs to kv() in email infrastructure</name>
  <files>
    src/main/java/com/softropic/payam/email/infrastructure/EmailRetryScheduler.java
    src/main/java/com/softropic/payam/email/infrastructure/listener/AccountChangeEmailListener.java
  </files>
  <action>
Read both files first.

**EmailRetryScheduler.java** — add `import static net.logstash.logback.argument.StructuredArguments.kv;` after the existing imports (no kv import exists yet). Then make these 4 changes:

Line 72: Replace
  `log.info("Retrying {} failed email(s)", candidates.size());`
With:
  `log.info("Retrying failed emails", kv("count", candidates.size()));`

Line 75: Replace
  `log.warn("Deadline expired for sendId='{}', marking as DEADLINE_EXPIRED", entity.getSendId());`
With:
  `log.warn("Email deadline expired", kv("sendId", entity.getSendId()), kv("newStatus", "DEADLINE_EXPIRED"));`

Lines 81-82: Replace
  `log.warn("Attempts exhausted for sendId='{}' (attempts={}), marking as ATTEMPTS_EXHAUSTED",`
  `        entity.getSendId(), entity.getAttempts());`
With:
  `log.warn("Email attempts exhausted", kv("sendId", entity.getSendId()), kv("attempts", entity.getAttempts()), kv("newStatus", "ATTEMPTS_EXHAUSTED"));`

Line 91: Replace
  `log.error("Retry attempt failed for sendId='{}': {}", entity.getSendId(), e.getMessage());`
With:
  `log.error("Email retry attempt failed", kv("sendId", entity.getSendId()), e);`

**AccountChangeEmailListener.java** — add `import static net.logstash.logback.argument.StructuredArguments.kv;` after the existing imports. Then:

Line 44: Replace
  `log.info("Sending notification email for account change: {}", event.getAction());`
With:
  `log.info("Sending account change notification email", kv("action", event.getAction()));`
  </action>
  <verify>
Run:
  grep -rn '{}' src/main/java/com/softropic/payam/email/infrastructure/EmailRetryScheduler.java
  grep -rn '{}' src/main/java/com/softropic/payam/email/infrastructure/listener/AccountChangeEmailListener.java
Both commands must return zero output.
  </verify>
  <done>
EmailRetryScheduler.java has zero {} placeholder log calls, has kv() static import, and all 4 log calls use kv() structured arguments. AccountChangeEmailListener.java has zero {} placeholder log calls and the single log call uses kv().
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete ##### and code-flow logs from security infrastructure — full cleanup</name>
  <files>
    src/main/java/com/softropic/payam/security/infrastructure/jwt/filter/JWTAuthenticationFilter.java
    src/main/java/com/softropic/payam/security/service/ClientIdAccessDecisionManager.java
    src/main/java/com/softropic/payam/security/infrastructure/audit/SpringSecurityAuditorAware.java
    src/main/java/com/softropic/payam/security/infrastructure/FraudAwareAuthenticationManager.java
    src/main/java/com/softropic/payam/security/infrastructure/listener/HttpRequestCxtListener.java
    src/main/java/com/softropic/payam/security/api/AccountResource.java
    src/main/java/com/softropic/payam/common/validation/PhoneNumberValidator.java
  </files>
  <action>
Read each file before editing. Apply the following changes:

**JWTAuthenticationFilter.java** — This class has exactly 2 log calls (both ##### decorative), both at lines 74 and 91. After deleting them no log calls remain. Steps:
1. Delete line 74: `log.info("################# Authentication Attempt.... [JWTAuthenticationFilter]");`
2. Delete line 91: `log.info("################# JWT creation Attempt.... [JWTAuthenticationFilter]");`
3. Remove the `@Slf4j` annotation (line 46).
4. Remove the `import lombok.extern.slf4j.Slf4j;` import line.

**ClientIdAccessDecisionManager.java** — This class has exactly 1 log call (line 41, ##### decorative). Steps:
1. Delete line 41: `log.info("################# Get info from 'RequestMetadataProvider' and check black lists and client type.");`
2. Remove the `@Slf4j` annotation (line 25).
3. Remove the `import lombok.extern.slf4j.Slf4j;` import line.

**SpringSecurityAuditorAware.java** — This class has exactly 1 log call (line 24, ##### decorative). Steps:
1. Delete line 24: `log.info("################# Audit current user name");`
2. Remove the `@Slf4j` annotation (line 16).
3. Remove the `import lombok.extern.slf4j.Slf4j;` import line.

**FraudAwareAuthenticationManager.java** — This class has exactly 1 log call (line 52, ##### decorative). Steps:
1. Delete line 52: `log.info("################# Do a fraud check then let 'AuthenticationManager' handle authentication or else throws a LockedException");`
2. Remove the `@Slf4j` annotation (on the line above `public class FraudAwareAuthenticationManager`).
3. Remove the `import lombok.extern.slf4j.Slf4j;` import line.

**HttpRequestCxtListener.java** — This class has exactly 2 log calls (both debug code-flow). Steps:
1. Delete line 27: `log.debug("RequestMetadataProvider cleanup. [HttpRequestCxtListener]");`
2. Delete line 41: `log.debug("User name taken from preAuthenticationEvent and set in RequestMetadataProvider. [HttpRequestCxtListener]");`
3. Remove the `@Slf4j` annotation.
4. Remove the `import lombok.extern.slf4j.Slf4j;` import line.

**AccountResource.java** — This class uses a manual Logger (not @Slf4j). It has exactly 1 log call (line 121). Steps:
1. Delete line 121: `log.debug("REST request to check if the current user is authenticated");`
2. Delete the Logger field declaration (line 49): `private final Logger log = LoggerFactory.getLogger(AccountResource.class);`
3. Remove the `import org.slf4j.Logger;` import line.
4. Remove the `import org.slf4j.LoggerFactory;` import line.

**PhoneNumberValidator.java** — This class has 2 log calls: the code-flow log.info (line 27, must be deleted) AND a valid structured log.warn (lines 34-37, must be KEPT). Steps:
1. Delete line 27 only: `log.info("Validating phone number...");`
2. Do NOT remove @Slf4j — the log.warn remains and requires it.
3. Do NOT remove the kv import — the log.warn uses it.
  </action>
  <verify>
Run these checks — all must return zero output:
  grep -rn '#####' src/main/java/com/softropic/payam/security/
  grep -rn 'log\.debug\|log\.info.*\[Http\|log\.debug.*HttpRequest' src/main/java/com/softropic/payam/security/infrastructure/listener/HttpRequestCxtListener.java
  grep -rn 'log\.debug.*REST request' src/main/java/com/softropic/payam/security/api/AccountResource.java
  grep -rn 'Validating phone number' src/main/java/com/softropic/payam/common/validation/PhoneNumberValidator.java

Then confirm the log.warn in PhoneNumberValidator is still present:
  grep -rn 'Invalid phone number' src/main/java/com/softropic/payam/common/validation/PhoneNumberValidator.java
(Must return 1 match.)
  </verify>
  <done>
All 5 security files (JWTAuthenticationFilter, ClientIdAccessDecisionManager, SpringSecurityAuditorAware, FraudAwareAuthenticationManager, HttpRequestCxtListener) have zero log calls remaining and their @Slf4j annotations and Slf4j imports removed. AccountResource has its debug log, Logger field, and both slf4j imports removed. PhoneNumberValidator has its code-flow log.info deleted while the structured log.warn is intact and @Slf4j remains.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-codebase verification — zero remaining violations</name>
  <files></files>
  <action>
Run all three verification grep commands against the entire src/main/java tree and confirm zero matches.

These commands verify no regressions exist and the must-haves for both LOG-CODE-01 and LOG-CODE-02 are satisfied across the whole codebase, not just the 9 files touched in this plan.
  </action>
  <verify>
Run each command and confirm zero output:

LOG-CODE-01 — no {} placeholder interpolation in any log call:
  grep -rn 'log\.\(trace\|debug\|info\|warn\|error\)("[^"]*{}"' src/main/java/

LOG-CODE-02 — no ##### decorative logs:
  grep -rn '#####' src/main/java/

LOG-CODE-02 — no "Validating phone number" code-flow log:
  grep -rn 'Validating phone number' src/main/java/

LOG-CODE-02 — no "REST request to check if" code-flow log:
  grep -rn 'REST request to check if' src/main/java/

All four commands must return zero output.
  </verify>
  <done>
Zero {} placeholder violations remain in src/main/java. Zero ##### decorative log statements remain in src/main/java. Zero code-flow-only log statements from the gap list remain. LOG-CODE-01 and LOG-CODE-02 are satisfied across the full codebase.
  </done>
</task>

</tasks>

<verification>
After all tasks complete, run the four grep commands from Task 3 as a final gate:

1. `grep -rn 'log\.\(trace\|debug\|info\|warn\|error\)("[^"]*{}"' src/main/java/` — must return zero output
2. `grep -rn '#####' src/main/java/` — must return zero output
3. `grep -rn 'Validating phone number' src/main/java/` — must return zero output
4. `grep -rn 'REST request to check if' src/main/java/` — must return zero output

Additionally verify PhoneNumberValidator still compiles with its log.warn intact:
5. `grep -rn 'Invalid phone number' src/main/java/com/softropic/payam/common/validation/PhoneNumberValidator.java` — must return 1 match
</verification>

<success_criteria>
- All 4 {} placeholder log calls in EmailRetryScheduler.java converted to kv() structured arguments with static import added
- The {} placeholder log call in AccountChangeEmailListener.java converted to kv() structured argument with static import added
- All 2 ##### decorative log calls in JWTAuthenticationFilter.java deleted; @Slf4j and import removed
- The ##### decorative log call in ClientIdAccessDecisionManager.java deleted; @Slf4j and import removed
- The ##### decorative log call in SpringSecurityAuditorAware.java deleted; @Slf4j and import removed
- The ##### decorative log call in FraudAwareAuthenticationManager.java deleted; @Slf4j and import removed
- The code-flow log.info in PhoneNumberValidator.java deleted; @Slf4j and kv import RETAINED for existing log.warn
- Both debug code-flow log calls in HttpRequestCxtListener.java deleted; @Slf4j and import removed
- The debug code-flow log call in AccountResource.java deleted; Logger field and both slf4j imports removed
- grep across full src/main/java confirms zero {} placeholder violations (LOG-CODE-01 satisfied)
- grep across full src/main/java confirms zero ##### decorative violations (LOG-CODE-02 satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/17-code-standards-enforcement/17-04-SUMMARY.md` with:
- frontmatter: phase, plan, subsystem (email.infrastructure, security.infrastructure, security.service, security.api, common.validation), files-modified list, requires: [17-01, 17-02, 17-03]
- What was done: gap closure for 9 files missed in original audit
- Files changed and exactly what changed in each
- Verification results confirming LOG-CODE-01 and LOG-CODE-02 are satisfied across full codebase
</output>
